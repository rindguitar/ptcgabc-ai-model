# STATUS

最終更新: 2026-07-25（セッション終了時に必ず更新）

## ブランチ状況
- `feature/fetch-priority` を `main` へマージ済み（§47/§48/§49/§50/§51/§52・mine-teachers）。
  ローカル・リモートとも削除済み。
- `feature/alphago-recycle`（`feature/fetch-priority` の分岐元・分岐後の追加コミットなし＝
  main に完全内包）も同時に削除済み。
- 次の作業は `main` から新しいブランチを切る（このブランチには戻らない）。

## 現在のフェーズ
**デッキA/B（枠A=965525fe／枠B=65c6b47e+attach）15時間で打ち切り・判定済み（§62）**:
枠A 0.395/506・枠B 0.489/559＝965525fe 乗り換え棄却・attach_priors は中立〜やや負圧
（ベンチ切れ負けは 33%→9% に激減も、ワンサイド負け率不変）。次は提出構成の決定（下記 0.）。

### 以前のフェーズ（v2 期・参考）
**65c6b47e（遅滞デッキ）両枠運用・v2（fetch_priors×ismcts／threat_bonus×nn）1日経過で決着**（§57）:
- 枠1（ismcts）: `submission_stall_ismcts_v2.tar.gz`（fetch_priors・TeamJ分）
  実戦 0.491→**0.567**（ベンチ切れ負け14%→3.3%）＝**継続**。レート692
  （※707は自枠の観測レンジ上限にすぎず天井ではない——上位帯は1000+・1位は1119〜1250。
  実力差は依然300〜500pt級）。
- 枠2（nn）: `submission_stall_v2.tar.gz`（threat_bonus α=0.1）実戦 0.531→0.489（改善なし）。
  **threat_bonus は不採用へ**（α=0 に戻す方向・次提出で反映）。レート556（中位）と整合。
  threat_bonus は NN の value evaluator ラップ実装のため**ISMCTS には注入不可**（§48 仕様）。
- §56 で提起した「ローカル判定は実戦の改善を過小評価するか」は今回のthreat_bonusでは
  不成立（ローカルもα=0.1が最悪＝実戦と一致）。乖離の主論点は §54 の pilot 間比較のまま。

## 今日やったこと（2026-07-22・本セッション）
- **v3 ビルド済み（nn 枠差し替え用）**: `models/submission_stall_v3.tar.gz`
  （nn＋floor8＋board0.2＋**threat_bonus=0（撤回）**＋**fetch_priors 同梱（カントー地方
  マスター分・ismcts_v2 と同一 JSON）**・65c6b47e・相手候補600デッキ）。
  配線プローブ済み: tar 展開 cwd で make_kaggle_agent("nn") に fetch_priors 4エントリが
  実際に渡ること・α=0 で wrap_threat_bonus が完全スキップされることを確認。
  **アップロードはユーザー**。
- **提出順の確認結果（ユーザー）**: ismcts_v2 が古い側＝単発アップロードだと ismcts_v2
  （692）が落ちてしまう → **2連アップロードで両枠入れ替え**に方針決定:
  ① `submission_stall_v3.tar.gz`（nn）→ ② `submission_stall_ismcts_v3.tar.gz`（ismcts・後）。
  ismcts を最新側に置く＝次回以降 nn 枠だけを1回で差し替え可能（§39 の手筋）。
  ismcts_v3 は §34 規約に従い再ビルド（main.py/fetch_priors/deck は v2 と完全同一・
  相手候補プールのみ 545→600 に成長を diff で確認済み）。
  副産物: 両枠 fetch_priors 入り・差は操縦のみ・同時スタート＝§54 の pilot 間比較を
  交絡なしで測る公平 A/B になる。レート692 は手放すが §46 の実測より1日で平衡に戻る。
- **上位帯の思考時間分析**: 上位帯は1試合の思考時間 中央値 ≈15s（TeamB 2.4s・
  TeamC 138s）に対し自チーム ≈354s。試合の実時間が数倍違う＝上位帯の消化試合数~3倍の
  主因。ただし §41/§46 の通り対戦数はレートの主因ではなく、方策の質が主戦線（再確認）。
- **replay 収集方針の転換**: 「1位のみ」→「**自デッキ 65c6b47e 一致×上位帯**」
  （TeamJ重点）へ。集まったら behavior_diff で同一デッキ操縦差分
  （§40 の 65c6b47e 版）→ 次の「マイニング→注入」対象を決める。
- **（同日追記・上記の収集方針を再転換）**: レート認識の訂正（1位1200・上位帯1000+）を受け
  「65c6b47e 一致教師探し」は取り下げ、**上位帯の把握を優先**（ユーザー提案）。
  現2位 TeamD ~300ep＋現1位 4h分を DL → スナップショット比較で**王者の2〜3日回転を確定**・
  乗り換え候補 965525fe（本命・王者キラー常在・探索型）/ 4041254f（対抗・教師430ep）・
  操縦の帯共通項（ベンチエネ投資・特性回転・敗時サイド）を特定（**§58**）。
- **v3 2連アップロード完了（ユーザー）**: nn_v3 → ismcts_v3 の順で両枠リスタート。
  両枠 fetch_priors 入り・差は操縦のみ＝pilot 公平 A/B が進行中（明日1日経過で判定）。
- **attach_priors 実装（`feature/attach-priors`・§59）**: 操縦の帯共通項の最普遍
  「エネ付与先」を採掘して heuristic に注入（全系統に波及・未指定なら挙動不変）。
  E2E: ベンチ付与 0→4.9回/戦（帯実測と一致）・heuristic ミラー 0.72/0.67。
  採否は実戦 A/B（次回 nn 枠差し替えに同梱）で判断。マージ待ち。
- **エンジン回転の診断と end_margin 実装（同ブランチ・§60）**: 採択率分析で §58 仮説を修正
  ——「探索が特性を蹴る」は誤り（採択率は帯同水準）。真因は ①提示回数1/4〜1/6（盤面の薄さ
  ＝デッキの領分）②打ち切り採択率が帯の2〜3倍（終了への探索上書き1.4回/戦）。後者への
  1行レバーとして END 逸脱専用マージン `--end-margin`（ismcts のみ・既定off）を実装。
  ※ismcts 枠は最新側スロット＝投入はデッキ乗り換えか次の2連アップロード時。

## 前回やったこと（2026-07-21・feature/fetch-priority ブランチ）
- **§49 cardId 誤読バグを修正**: 山札サーチの `option` は cardId を持たず、実カードは
  `sel.deck[option.index].id` で解決する必要があった（旧実装は常に0落ち＝「たね優先→
  エネ優先」分岐が本番で一度も機能していなかった可能性）。`_generic_select`/
  `mine_fetch_priorities` 両方を修正。§31 と同型の再発（実データ未確認のバグ）。
- **§49 教師プールの誤りを是正**: cardId 完全一致（65c6b47e）に釣られ、**自チームの
  対戦ログ（マッチメイキング相手＝実力の裏付けなし）を教師扱いしていた**とユーザー指摘で発覚
  （TeamL/TeamK は実は自分の過去対局の相手）。`mine_fetch_priorities.py` の
  既定 `--dir` を `data/replays/others`（意図的に DL した上位帯のみ）に変更。
- **§50/§51 役割別優先度マイニング新設**（`mine_search_role_priors.py`）: cardId でなく
  役割カテゴリで集計しデッキ非依存にプール（others/ 36チーム・局面数1262）。
  `sel.context`（TO_HAND/TO_BENCH/ATTACH_TO）× 型混在有無 × `supporterPlayed` の
  2軸4バケットに分離。候補数正規化（選好指数 lift）と、`sel.effect.id` で判定した
  サーチ効果コストの捨て札優先度も追加。
- **主要発見**: ① overall の「basic_energy 最優先」は TO_HAND 以外のコンテキスト混入の
  アーティファクト（TO_HAND 内では energy と pokemon はほぼ拮抗）。② lift 正規化で
  トレーナー各種の優先度差はほぼ幻（lift≈1.0）と判明、**basic_pokemon だけ明確に忌避
  （lift=0.46・局面数136で頑健）**。
- **§52 `_generic_select` を修正**: 山札サーチのカテゴリ順を「たね>エネ>その他」→
  「エネ>その他（たね含む）」に変更（上記発見に基づく）。fetch_priors（tier0）は不変。
  影響は TO_HAND の型混在局面のみ（TO_BENCH/ATTACH_TO は構造的に無風）。
- 全テスト green（88 passed・3 skipped）・ruff clean。§47/§48（fetch_priors/threat_bonus
  本体実装）は前回セッションで完了済み・データ待ちのまま。
- **§53 threat_bonus α=0 対照実測**（ユーザー実行・同ブランチ状態＝§49/§52 込み）: 最悪
  0.225/平均0.399。α=0.1（最悪0.150/平均0.386）を同条件で上回る＝α=0.1 は悪化と判明。
  ユーザー指摘により、単発レバーの α 単独スイープだけで採否を断定せず、fetch_priors
  （§47）が揃った段階で蓄積変更の合成構成も別途評価する方針に。
- **§54 現提出物（65c6b47e）1日目の replay 分析**: variant フォルダ名（ismcts/nn）は
  旧旗艦との混在に注意が必要と判明（raw JSON からデッキ hash 再計算で分離）。
  65c6b47e 限定で ismcts 28/57（0.491）・nn 26/49（0.531）＝ほぼ拮抗（n少で有意差なし）。
  デッキ切れは両枠とも低水準。天敵 9e3ece3f は nn 0/4・ismcts 遭遇なし（n=4で要継続監視）。
  `make gauntlet-real` で判定プールを実メタ544デッキから再選抜（カバー2370/3374）。
  **事故**: `make replays-daily` の prune が `others/`（§47 cardId 単位マイニングの教師
  プール・58 episode）を、cardId 単位マイニング未実行のまま削除した（詳細 decisions.md §54）。
  §47 をさらに進めるには others/ の再 DL が必要。
  **再発防止済み**: `prune_replays.py` の既定 `--keep-variants` に `others` を追加
  （自チーム試合を others/ に置くことはない前提のため独立ライフサイクルで温存・
  破棄したい場合のみ `--include-own`）。今後は replays-daily を回しても others/ は消えない。
- **§55 fetch_priors 初回マイニング**: 追加 DL 分（others/ 241 episode・新チーム）を
  `mine_fetch_priorities.py` で初回マイニング（state.json 新規作成・78 team×deck 組）。
  **提出デッキ（65c6b47e）完全一致の教師が2チーム出現**: TeamJ（3ep・
  id344=0.67/id18=0.43）・TeamK（1ep・id345=0.50）。単体ではまだ薄いが前進。
  次提出ではTeamJ側
  （`data/fetch_priors/___________65c6b47e.json`）を採用候補とする（詳細 decisions.md §55）。

## 2026-07-20
- **champion-gate（新プール＝9e3ece3f 入り）**: new 最悪0.250/平均0.458 < best 最悪0.375/平均0.641
  → 据え置き（ノイズドリフト阻止）。best の最悪 0.375 は天敵 9e3ece3f 相当＝champion の弱点が
  ローカル数値でも裏付けられた（実戦 alphago 系 0.22 と整合）。
- **確定版天敵チェック合格**: 65c6b47e×測定構成（operative＋floor8＋盤面補正0.2・Docker・
  20試合席入替）で 9e3ece3f に **0.65**。champion の 0.375 を明確に上回り、枠差し替えの
  前提クリア。ISMCTS 操縦の 0.85（保守基準線）との差は 20 試合では有意といえず、
  §44 の平均実測（operative 0.713 > ISMCTS 0.537）に従い構成は変更しない。
- **提出物ビルド済み**: `models/submission_stall_v1.tar.gz`（nn＋floor8＋board0.2＋65c6b47e・
  相手候補500デッキ同梱）。アップロードと枠選択（推奨: 弱い枠 v3.5=663）はユーザー。
- **§45 の横展開監査（ユーザー指摘・リポジトリ全域）**: off-by-one は**ゼロ**。
  episode JSON の steps を読むのは scripts 7本のみで、ペアリングする4本は全て `steps[i+1]`・
  残り3本は action 非依存 or 初手デッキ提出走査のみ。src/ は live 対局（obs に即時応答）で
  構造的に無関係、npz 消費系（teacher_tune 等）は正実装の teacher-extract 産で健全。
  ただし replay-format.md の樹形図が同一 step ペアと誤読できる記述だったため、
  t+1 規則の明記＋正実装再利用の指示を追記（再発防止）。

## 2026-07-19
- **【撤回】「誤デプロイ」は誤報＝§43 は本番で発火100%**: 正しいペアリング
  （obs[t] への応答は **steps[t+1].action**・既存スクリプトは元から正実装）で
  v4 14/14・v3.5 17/17 発火（v3 基準 8%）。誤報原因は Claude のアドホックプローブの
  off-by-one（§45・教訓: 行動ログ解析は既存実装を再利用）。
  実勢: 発火は完璧だが**機会が希少**（v4 14/64・v3.5 17/62 試合）＝律速は札の可用性
  （60枚中1枚・サイド落ち~10%・掘りで喪失）。deck-out は試合比 26%→15-16% に減少。
- **系統天敵 9e3ece3f の特定**（§46）: レートとローカル測定の乖離を追跡し、エネ厚エンジン
  （poke17/ene14・出現35件で増加中）に **alphago 系 4/18（0.22）**・旧 ismcts は 5/9 と判明。
  負け筋はデッキ切れ＝消耗戦で寝かされる。v4 の広レンジ（505-891）は概ね Elo ランダムウォーク
  ＋この天敵で、「v4 固有のメタられ」ではない（閾値差は試合の~25%にしか影響しない）。
- **判定プールの盲点を解消**: 9e3ece3f は16デッキプールに不在だった→ `make gauntlet-real` で
  再選抜し **9e3ece3f がプール入り**（カバー 2260/3152 遭遇）。ratchet は内側ループで
  gauntlet を読まない（league.py:338）ため走行中でも安全と確認の上、並行実行。
- **65c6b47e の天敵チェック（保守基準線）**: ISMCTS 操縦（--pilot ismcts・ホスト）で
  9e3ece3f に **0.85**・92985406 に **0.85**（各20試合）。遅滞はエンジンの天敵も食う。
- 運用: prune の keep-variants を**前方一致**に修正（alphago_v4 等の A/B 派生ディレクトリの
  誤削除防止・実運用で345件温存を確認）。replays-daily は ratchet と並行実行可と確認。

## 前回まで（要点のみ・詳細は decisions.md）
- **§44 replay-tune 3点比較**: tuned 0.599 < operative 0.713 → ロールバック。
  ISMCTS(tb0.3) 0.537 < operative → 評価は operative 継続・NN 凍結は実測根拠付きに。
- **§43**: リサイクル強制手を nn_mcts 探索前段へ注入・`--recycle-at` で閾値指定可。
- **§42**: replay OOM 修正（ストリーミング）＋消費済み JSON 自動破棄（replays-daily 運用）。
- **遅滞デッキ評価（計60試合/相手で再現）**: 65c6b47e 単純平均 0.728・頻度加重 0.748
  （champion best 0.631 超え）。弱点は a8c57d4b 0.15（出現0.9%）・同型 88cc 0.35（4.7%）。
  実メタ33%の a4066acd に 0.82。

## v3 ペア1日経過分析（2026-07-23・本セッション）
- **両枠とも水面下**: ismcts 0.33〜0.39・nn 0.38〜0.44（カットオフ 8744万/8747万/8750万の
  いずれでも同傾向・v2 期実測 0.587/0.489 から明確に低下。n=54/59 で ±13pt）。
- **原因は同梱物でなく帯の回転**（v2期118戦 vs v3期113戦の相手デッキ分布比較）:
  ①天敵 9e3ece3f（§46）の遭遇率 3%→**12%**（5勝9敗）②04621784 が両期12%で持続的カウンター
  （計10勝17敗）③**没落した旧王者 a4066acd に通算0勝7敗**＝上位帯の王者回転（§58）が
  我々の帯に降ってきている。カウンター群{9e3ece3f,04621784,a4066acd,a8c57d4b}で全体の1/3・
  勝率~0.30。ベンチ切れ負けも再燃（負け66中23件・うち8件が 9e3ece3f/04621784 に集中）＝
  §46 の機構そのもの。fetch_priors では相性は防げない。
- **pilot A/B（§54 の主論点）**: nn 0.441 vs ismcts 0.389＝有意差なし（判定続行）。ただし
  nn は時計 ~50s で ismcts の ~500s と同等＝効率では operative 優位の傾向。
  ※「v3 で ismcts が遅くなった」は誤検知（v2 期も overage 残 q50≈103s＝予算使い切り）。
- **Kaggle レート（ユーザー報告・リスタート後1日）**: nn 現656（600-751）・
  ismcts 現647（600-**825**）。①nn は v2 の556→656 と **+100**＝threat撤回+fetch_priors の
  同梱変更は効いた可能性が高い（勝率低下は帯が上がった分の対戦相手強化で説明可能）
  ②ismcts は 692→647 と低下＝遅滞デッキへのカウンター飽和が直撃 ③ismcts の瞬間825は
  「相性が噛み合えば800帯に届く」証拠＝制約は素の強さでなく相性構造 ④両枠がレートでも
  拮抗（647≒656）＝pilot A/B は時計1/10の nn が同着（§54 の効率論点を補強）。
- **含意**: 65c6b47e は現在の帯で三重にカウンターされており、デッキ乗り換え（§58）の緊急度が
  上がった。opp_decks は 663 件に成長（昨日の others 収穫込み）→ gauntlet-real 更新推奨。

## デッキA/B 提出（2026-07-24・本セッション）
- **eval-deck 3本の結果はデッキ選定に使えないと確定（§61）**: 現デッキ0.708/候補0.537・0.440
  だが、対カウンター相性が実戦と逆符号（vs 9e3ece3f 0.85↔実戦0.36 等）。旧自デッキ相手のみ
  完全一致＝共適応バイアスの実証。ローカル eval-deck はデッキ選定では動作確認へ格下げ。
- **方針（ユーザー提案）**: **nn 両枠でデッキだけ変える2連提出**＝pilot・レバー同一のデッキA/B。
  ①`submission_965525fe_nn_v1.tar.gz`（**先に提出**＝最古側・TeamC の常在王者キラーデッキ・
  fetch_priors TeamC分8種＋attach_priors）②`submission_stall_v4.tar.gz`（後に提出・
  65c6b47e 据え置き・fetch カントー分＋attach_priors）。
  読み方: **枠A vs 枠B＝デッキA/B**／**枠B vs v3 nn（656）＝attach_priors 単独A/B**。
  実験側を最古に置く＝外れたら単発で差し替え可能。ismcts＋end_margin はデッキ決着後に再登板。
  両 tar とも配線プローブ済み（fetch 8/4種・attach {0:0.49,1:0.57,2:0.79,3:0.77} が nn へ到達）。

## デッキA/B 15時間打ち切り分析（2026-07-25・本セッション・§62）
- **レート（ユーザー報告・15hで打ち切り）**: 枠A 965525fe_nn 現506（457〜600）・
  枠B stall_v4 現559（381〜600）。※600は提出直後のデフォルト値＝両枠とも一度も600を
  超えていない（v3ペアは1日で751/825到達）。replay は `data/replays/965525fe_nn/`（新設・43ep）
  と `nn/`（v4期は episode id ≥ 87780000 で切り出し・45ep）。
- **デッキA/B: 965525fe 移植は不成立**: 枠A 0.395 vs 枠B 0.489。カウンター群遭遇は
  枠A 5%（最大の追い風）なのに広い field に分散して負けた（その他21/26）＝「デッキが制約」
  仮説の予測（A≫B）と逆。王者キラーも我々の操縦では機能しない＝**乗り換え棄却**・
  デッキ評価軸に「操縦適合」を追加（§62）。
- **attach_priors 単独A/B（枠B vs v3nn 656）**: 配線は完全作動（P(ベンチ) 0.06→0.56・
  0.4→3.8回/戦・バケット別も採掘値に追随）。**ベンチ切れ負け 33%→9% と激減**（近因は修正）
  だが、敗時サイド0-1のワンサイド負け率は 78%→78% で不変＝仮説は成果面で反証。
  勝率 0.489 は v3nn の 0.441 より名目上界・レートは 559<656＝**強さは中立〜やや負圧**。
- **健全性確認**: 自枠同士の対戦0・枠A の自デッキ hash 再計算=965525fe（3/3）・
  overage 最小433s。episodes_log.csv は 88 行追記済み（`make replays` 実行済み・
  実メタデッキ 704 件に成長）。

## 次の提出（2026-07-25 決定・ビルド済み）
- **ユーザー決定**: デッキは 65c6b47e に復帰・attach_priors は継続（機構作動を確認済みのため）。
  枠B（stall_v4＝65c6+fetch+attach×nn）は**そのまま観測延長**（attach の判定材料を積み増す）。
- **枠A 差し替え用にビルド済み**: `models/submission_stall_ismcts_v4.tar.gz`
  （**ismcts＋end_margin=0.15（§60 初投入）**＋fetch カントー分（v3 ismcts と同一 JSON を
  diff 確認）＋attach_priors（heuristic 層注入＝ismcts にも波及・§59）＋65c6b47e・
  相手候補704デッキ）。配線プローブ済み: main.py 記載／make_ismcts_agent へ end_margin=0.15・
  fetch 4エントリ・attach {0:0.49,1:0.57,2:0.79,3:0.77} の到達を実測。
  **単発アップロードで最古側の枠A（965525fe・棄却済み）が落ちる＝アップロードはユーザー**。
- **読み方**: 枠A(ismcts+end_margin) vs 枠B(nn)＝同デッキ・同priors の操縦A/B（§54 続き）
  ＋end_margin の初実戦。判定手順: ①15h 時点で機構チェック（END への探索上書き回数/戦が
  1.4 から減るか・採択率・健全性）②強さ判定（レート・勝率）は破綻がない限り 24h 以上で
  （n≈40 の ±15pt では 15h 打ち切りは「明確な破綻」検知用）。

## 攻撃型デッキ候補リスト（2026-07-25・§63 レバー再試の土台）
- **判定基準**: 対自分の負け筋がサイド系（サイド負け＋その他グレー）比率×敗北の速さ
  （steps 中央値・全体中央値116）。episodes_log 全4938行＋opp_decks 構成
  （ポケ/エネ/トレーナー/たね・cards.py カテゴリ集計）。全30候補は
  `data/aggressive_candidates.csv`（追跡外）に保存・再現スクリプトは同集計をインライン実行。
- **主要候補（n・直近・対自勝率・サイド系・負けsteps・構成）**:
  - **a8c57d4b**（41戦・0.37・58%・77steps・10/9/41/3）＝**旧自デッキ**。攻撃型実証＋
    我々の操縦と共適応済み＝**threat_bonus 再試の第一候補**（§62 の移植問題を回避できる唯一枠）。
  - **04621784**（96戦・0.41・44%・100steps・16/13/31/10）＝持続カウンター・現行メタ12%・
    fetch_priors 教師2種あり。
  - **dbe52647**（15戦・0.27・55%・104steps）／**1364f42b**（19戦・0.37・50%・120steps）
    ＝ともに 13/11/36/5・直近5戦・fetch_priors 教師あり（TeamM/TeamN）。
  - **4041254f**（13戦・0.38・75%・154steps・18/10/32/10）＝現王者・教師430ep。サイド系だが
    やや遅め＝ミッドレンジ寄り。
  - **00df71b9**（13戦・0.38・62%・80steps・14/9/37/8）＝速い・直近4戦。
- **非攻撃型の確認**: 9e3ece3f はサイド系2%（ベンチ/デッキ切れ型の消耗デッキ）＝
  「エネ厚=攻撃型」ではないと数値で確認。a986f125（エネ35）も対自 0.64 で脅威度低。

## ismcts_v4 15h 機構チェック（2026-07-25 実施・結果 green）
- **end_margin=0.15 は設計通り作動**: END への探索上書き 1.52→**0.26回/戦**（v3期 54試合 vs
  v4期 53試合・heuristic 再提案との突き合わせで実測・§60 の 1.4 と v3 再計測 1.52 は整合）。
  ただし END 採択率の総量は 18.0%→20.6% と不変＝上書き分は消えたが、END の大半は
  heuristic 自身の正当な提案。総量を帯の 5% に近づけるには盤面の育ち（デッキの領分）が必要。
- **attach_priors の ismcts 波及を確認**: P(ベンチ付与|選択局面) 0.30→**0.68**（rollout の
  heuristic 経由・§59 の設計通り）。
- **健全性**: overage 最小 61s（v3 と同一水準＝end_margin による時間異常なし）。
- **中間成績（15h・n=53）**: **0.491**（26/53）vs v3期 0.389。カウンター群遭遇 36%（v3期 31%）
  と逆風下での改善＝方向は良いが ±14pt 幅・強さ判定は 24h+ で。デッキ切れ負け 3→1・
  相手デッキ切れ勝ち 10→14。**ベンチ切れ負けは 41% で不変**（nn 枠では 9% に激減したのと対照的
  ・カウンター群の遭遇率差＝§46 の機構が相手依存であることと整合）。
- **レート（ユーザー報告・15h時点）**: **現658（最低457〜最高736）**。①デッキA/B ペアが
  15h で一度も超えられなかった 600 を明確に突破（max 736）②v3 ismcts の1日値 647・
  v2 の 692 と同帯＝65c6b47e 復帰の妥当性を支持 ③同時点の枠B(nn) 15h 値 559 に対し
  約+100＝同デッキ・同 priors の操縦 A/B で ismcts+end_margin が先行（観測窓ずれあり・
  確定は 24h+）。
- **24h 強さ判定（2026-07-26 確定）**: レート **651**（15h の 658 から横ばい＝**平衡 ~650**・
  v2 ismcts の 692 帯に僅かに届かず）。勝率 0.475（28/59・前半0.55→後半0.40）。
  **構図が明確**: 対フィールド 26/39＝**0.67** に対しカウンター群 2/20＝**0.10**
  （遭遇率34%・04621784 だけで12戦2勝・9e3ece3f 0/5・a4066acd 0/3）。ベンチ切れ負け14/31。
  **結論**: ismcts＋end_margin＋attach＋fetch は 65c6b47e で出せる最良既知構成で、
  制約はデッキの相性構造（§46/§62 と同一）＝**F4 デッキ実験（4041254f）が正しい次レバー**
  であることを裏付け。65c6 側の構成いじりはこれ以上の主戦線にしない。

## F4 デッキ実験（2026-07-25 ユーザー決定・ビルド済み）
- **ユーザー決定**: 負け組合せの枠B（stall_v4＝nn×65c6・559）は残す意味なし→即差し替え。
- **Layer 2 プローブ（heuristic 同条件・40戦×3構成）**: 4041254f は我々の操縦で機構正常
  （特性 13.6提示/7.2採択/戦・攻撃発火・対65c6 0.35）。**陰性対照 965525fe は同条件 0.15**
  ＝プローブに弁別力あり・4041254f は「動かせない型」ではない。ローカル勝率 0.35 自体は
  §61 のバイアス（65c6 側が膨張）込みの数字なので veto 材料にしない。
- **ビルド済み**: `models/submission_4041254f_ismcts_v1.tar.gz`（**F4 現王者デッキ 4041254f**×
  ismcts＋end_margin=0.15＋fetch TeamO分17エントリ（6ep・現状最厚）＋attach_priors・
  相手候補714）。配線プローブ済み（デッキhash=4041254f・end_margin/fetch/attach 到達確認）。
- **読み方**: 枠A（65c6×ismcts_v4） vs 新枠（4041254f×ismcts）＝**操縦・レバー完全固定の
  純デッキA/B**（F0 vs F4・帯実測では F4 が 0.61 で優位）。
- **注意（枠順）**: この単発アップロード後、最古側は ismcts_v4（658・好調）になる。
  実験が外れた場合の復旧は2連アップロード（ismcts_v4 相当の再ビルド→実験枠差し替え）が必要。
- **改善余地**: fetch 教師 6ep は薄い。4041254f 旗艦チームの replay 追加 DL →
  mine_fetch_priorities で厚くして次版に反映可。

## 次の一手（優先順）
1. ~~v3 ペアの1日経過分析~~ ~~デッキA/B の判定~~ ~~ismcts_v4 15h 機構チェック~~ **完了**。
   次は **ismcts_v4 の 24h+ 強さ判定**（レート・勝率・対枠B）。
2. ~~型検証 Layer 1 実装~~ **完了**: `scripts/archetype_matrix.py`（714デッキ→k-means 6
   ファミリー × others/ 615 第三者対戦・操縦交絡ゼロ・テスト5本 green・
   出力 `data/archetype_matrix.json`）。**初回結果**: 現王者ファミリー F4
   （ポケ17/エネ12/たね11・4041254f 含む）が総合 0.625(n=331) で全ファミリーに勝ち越し。
   自デッキの型 F0（低エネ・a4066acd/965525fe も同族）は 0.396(n=321)・対 F4 0.39(n=294)。
   ダークホース F2（ポケ20/エネ8・盤面厚）が対 F0 0.76(n=17)・対 F4 0.43(n=21)。
   **注意**: others/ は王者狙い DL のため F0×F4 セルに標本が集中（他セルは n<25 で参考値）・
   k=6 は粗い。次: 鮮度分割（--min-episode）・k スイープ・F4/F2 から Layer 2（操縦適合）候補
   選定（4041254f は教師430ep あり＝§58 対抗候補と合流）。
2. **attach_priors の実戦投入**: `feature/attach-priors` をマージし、次回 nn 枠差し替え（v4）
   に fetch_priors＋attach_priors を同梱。デッキを変えるかは 3. の結果とセットで決める。
3. **デッキ乗り換え評価（§58）**: 965525fe（本命・王者キラー常在・探索型実証）/
   4041254f（対抗・現王者・教師430ep）を eval-deck／天敵チェック（ユーザー GPU）。
   王者回転（2〜3日）があるため「常在性×対王者相性」を優先・王者クローンはしない。
3. **§56 の保留論点（eval-net 的ローカル判定の格下げ）**: 今回の threat_bonus はローカルと
   実戦が一致したため決定打にならず。乖離の主論点は §54 の pilot 間比較（ローカル
   operative≫ISMCTS だが実戦拮抗）のまま。次に判断材料が増えたタイミングで再検討。
4. **§52 の効果検証**: `_generic_select` のたね優先撤回（エネ>その他）が実戦のデッキ切れ率・
   ベンチ切れ率（§31 の当初の狙い）にどう効くか、継続観測。
   根拠が TO_HAND 型混在局面（局面数136）に偏っているため一般化は未検証。
5. gate の判定基準に頻度加重平均の併記を検討（最悪ケース基準は出現0.9%の天敵に引きずられ
   遅滞系を不当に棄却する・要議論）。
6. replay 運用は `make replays-daily` に統一（2026-07-22 実行済み・others/ は keep-variants
   化で自動破棄されなくなった＝データ量は `du -sh data/replays/` で定期確認・現状2.8GB）。
   プール刷新は `make gauntlet-real`（ratchet 並行可・gate/eval の起動前に）。

## 未解決・保留中の問題
- v4/v3.5 の閾値 A/B はレート平衡により早期収束見込み（同一提出物の継続収集はしない方針）。
  現時点の読み: 閾値差の実戦効果は小（発火機会が~25%の試合にしかない）。
- クローンの floor 抑圧疑い（floor0=0.681 > floor8=0.588）——v4/v3.5 は floor0 採用済み。
- nn 学習は凍結中（§44 で実測根拠付き）。解除条件は「ISMCTS 実行を超える具体仮説」。
  value_samples は 220,920 まで蓄積済み（replay-tune は closed・他用途は自由）。
- §47 cardId 単位フェッチ優先度の教師プール（others/）が §54 の prune で消失。再開には
  再 DL が必要（下記参照）。

## 直近の決定事項
- 2026-07-22: v2提出（fetch_priors×ismcts／threat_bonus α=0.1×nn）の1日経過結果（§57）:
  ismcts 0.491→0.567（ベンチ切れ負け14%→3.3%）で**fetch_priors継続**。nn 0.531→0.489で
  **threat_bonus不採用**（次提出で α=0 に戻す）。threat_bonus は ISMCTS 不可（実装仕様）を
  再確認。今回は実戦がローカル判定と一致し、§56 の「ローカル格下げ」論点は決定打なし。
  `make replays-daily` 実行済み（others/ は keep-variants 化で自動破棄されなくなった）。
- 2026-07-21: cardId 誤読バグ修正（§49）＋教師プールを others/ 限定に是正（自チーム対戦
  ログを教師扱いしていた誤りをユーザー指摘で発見）。役割別優先度マイニング新設（§50/§51）で
  「たね優先」が上位帯実測（lift=0.46）で否定されたため `_generic_select` を修正（§52・
  エネ>その他のみに）。効果検証は次の a/b・提出で。
- 2026-07-21（同日・続き）: threat_bonus α=0 対照実測（§53・α=0.1 は悪化と判明）。
  現提出物（65c6b47e）1日目 replay 分析（§54）: ismcts 0.491・nn 0.531 で拮抗、
  gauntlet-real で判定プール実メタ同期。**replays-daily の prune が others/（§47 教師
  プール・58 episode）を cardId 単位マイニング未実行のまま削除**（事故）＝再 DL 待ち。
  ユーザー指摘で `prune_replays.py` の既定 keep-variants に others を追加（再発防止済み）。
- 2026-07-20: 新プール gate は据え置き（new 0.458 < best 0.641）。天敵チェック合格
  （65c6×nn 構成 0.65 vs 9e3ece3f）→ submission_stall_v1 ビルド完了。
  **両枠を遅滞デッキへ差し替え決定（ユーザー）**: 枠1=ISMCTS 操縦・枠2=nn 測定構成の
  同デッキ操縦 A/B。alphago 系は退役（閾値 A/B 打ち切り・フェッチ優先度は並行実装へ）。
- 2026-07-19: 誤デプロイ誤報を撤回（§43 発火100%・原因は測定側 off-by-one §45）。
  gauntlet を実メタ同期し天敵 9e3ece3f をプール入り（§46）。prune keep-variants 前方一致化。
  平衡打開はデッキ交換（65c6×測定構成）で行く方針・枠選択はユーザー決定待ち。
- 2026-07-18: v4（閾値15）/v3.5（閾値25）並走 A/B のため旗艦 ismcts を退役（ユーザー決定）。
  replay-tune ロールバック・評価は operative 継続（§44）。replay OOM 修正＋自動破棄（§42）。
- 2026-07-17: gate/ratchet 分離・リサイクル§39・特性解放§40・型と実行度§41・
  マージ済みブランチ削除ルール。
- 2026-07-16: クローン操縦は撤収（§37）→ 資産は事前分布へ（§38）。
