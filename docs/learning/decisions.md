# 設計判断の記録（design decisions）

「なぜそうしたか」を理由・利点・欠点つきで残す。用語は [glossary.md](glossary.md)、コードの場所は
[architecture.md](architecture.md) を参照。各判断は時系列でなくテーマ別。

**記入様式**（テンプレ decisions.md 準拠・§35 以降の新規エントリに適用）:
`## N. タイトル（YYYY-MM-DD）` の見出しで、本文に **決定**（何を選んだか）・**理由**（なぜか）・
**却下した代替案**（あれば）を含める。既存 §1〜34 は参照が張られているため見出しは変更しない。

## 1. 操縦に ISMCTS を使う（heuristic ではなく）
- **背景**: heuristic 操縦は**特性・効果・トレーナーを使わない**（攻撃ダメージで殴るだけ）。エンジンは
  それらを合法手として出すのに heuristic は拾えず、デッキ評価が「殴るデッキ」しか正しく測れない。
- **判断**: 合法手を列挙して価値で選ぶ **ISMCTS** を操縦にする（元から特性対応）。
- **利点**: 特性/効果駆動のデッキも正当に評価できる。**欠点**: 遅い。
- 関連: 探索(league)は速度優先で弱め設定、判定(gate/eval)は強め設定、と使い分ける。

## 2. NN 操縦を「蒸留 → improve」の2段で育てる
- **蒸留(distill)**: ISMCTS 教師を真似て**安定した床**（≈ISMCTS）を作る。
  - 利点: 弱いネットからの自己対戦崩壊を回避。欠点: **教師を超えられない**（天井＝五分）。
- **improve(self-play)**: 蒸留床を**種**に自己対戦。MCTS(ネット) はネット単体より強い（方策改善演算子）ので、
  その手を学べば**教師=ISMCTS を超えられる**。
  - なぜ以前の自己対戦は崩壊したか＝**弱いネットから始めたから**。≈ISMCTS の種＋best保存＋低LR＋
    replay＋drift安全弁で安定させる。
- **改善ループは `improve ↔ ratchet-nn`**（`distill ↔ improve` ではない）。
  - 理由: improve で ISMCTS を超えた後に distill へ戻すと、**教師=ISMCTS の天井(五分)へ引き戻して**
    improve の成果を消すから。distill は最初の土台作りの一度きり。

## 3. 探索は少数メタ・判定は gauntlet（過学習対策）
- **背景**: 少数のサンプルメタだけに最適化したら Kaggle 順位が悪化＝**過学習**。
- **判断**: 探索(league)は速い少数メタ、**判定(gate/eval-deck)は多様な gauntlet**（メタ＋チャンピオン系＋
  全色 mono-type）に分離。過学習の歯止めは判定側で掛ける。
- **限界**: gauntlet も**代理指標**。実戦(Kaggle)で単調改善する保証はない（issue #3 で監視）。

## 4. keep-best ラチェット（後戻りしない蓄積）
- **背景**: league 内部評価は小サンプルでノイズが大きく「より弱いデッキへ更新」してしまう。
- **判断**: league 出力を多めの試合のゲートに通し、**現ベストを確実に上回った時だけ** `champion_best` を更新。
- **効果**: 回すほど単調改善・ノイズで劣化しない。提出は `champion_best` を使う。

## 5. 蒸留の方策ターゲットを温度で one-hot↔soft 1本化
- **背景**: soft-π（訪問分布）を一律に使ったら**逆効果**だった（あるデッキ 0.45→0.20）。原因は蒸留の
  教師探索が**浅く訪問が平坦**で、soft-π が無情報な的になったこと。
- **判断**: `π ∝ visits^(1/τ)` の**温度 τ** で統一。**既定 τ=0（one-hot・安全）**、教師を深くしたとき
  `τ>0` で soft を解放。one-hot を特例として内包＝「悪くなりようがない」。
- **要点**: soft-π は **深い教師（time_budget↑ / iters_per_det↑）とセットでないと効かない**。
- **one-hot の欠点も理解しておく**: 迷う局面（僅差）を捨てて過信しやすい・tie-break ノイズを確信として
  焼き込む・情報量が少ない。あくまで「浅い教師での安全な既定」であって最良ではない。

## 6. best 選抜を全学習デッキの平均で行う
- **背景**: NN が「ある学習デッキでは ISMCTS 超え、別デッキでは負け越し」＝**汎化不足**。原因の一つは
  best 選抜が**eval 固定1デッキだけ**で、その1デッキに偏った snapshot を選んでいたこと。
- **判断**: eval/best 選抜を**全学習デッキの平均勝率**に（1デッキ最低10試合でノイズも抑制）。
- 関連: issue #2。

## 7. 収集の CPU 並列化（GPU ではなく）
- **背景**: 「1回の学習でもっと試行回数を増やしたい」。
- **判断**: 各対戦は独立なので **ProcessPoolExecutor で CPU コアに分散**（≈コア数倍）。
- **なぜ CPU か**: NN-MCTS は **batch=1 の小さなネット推論＋エンジンのフォワードシミュレーション**で
  CPU 寄り。GPU の batch=1 は転送オーバヘッドでむしろ遅い。学習（大バッチ）だけ GPU。
- **なぜ spawn か**: 親が CUDA を初期化済みだと **fork はハング**しうる → **spawn** で回避。
- **worker 数の既定 = nproc-1**: 1コアを OS/GPU供給に空けて機械の無反応を防ぐ（HT環境なら物理コア数推奨）。

## 8. run を跨いで蓄積する（best の永続化）
- **判断**: `--resume` で作業ネットを継ぎ足し、best の基準勝率を `*.meta.json` に**永続化**。
  - これが無いと再開のたびに best 基準が -1 にリセットされ、**劣化モデルで best を上書き**してしまう。
- **蓄積の2種類**: `--resume` は**作業ネット**を継ぐ（最新の学習を活かす＝普通はこれが正しい）。
  `*_best.pt` は**選抜専用の安全な snapshot**で、学習の継ぎ足しには使わない。

## 9. improve の drift 安全弁（`--resume-from-best`）
- **背景**: 自己対戦は作業ネットが drift（悪化）しうる。`--resume` は作業ネット継続なので悪化も蓄積しうる。
- **判断**: 再開時、作業ネットの直近 eval が best を margin 超で下回っていたら **best から再開**
  （人の「悪い癖がついたら良かった頃へ戻す」）。improve のみ有効、distill は安定なので対象外。

## 10. 蓄積（継続学習）のデメリット
- ネットは固定サイズ＝**容量飽和**でいつか頭打ち（人と違い脳は増えない）。
- 同じ学習デッキへの**過学習**（→複数デッキ選抜で緩和）。
- デッキ軸の単調性は**gauntlet 基準**であって**実戦で単調とは限らない**。
- replay buffer は直近のみ保持＝**サンプルは新陳代謝・重みは蓄積**（意図的）。

## 11. 提出物には「操縦」も含まれる
- 提出は「デッキ持参＋毎手プレイするエージェント」＝**操縦は提出の中核**（デッキだけではない）。
- 現状は ISMCTS 操縦で固定。**NN が ISMCTS を安定して超えたら**、提出操縦を NN に切替えられる
  （build_submission に nn モジュール＋`.pt` を同梱・要 torch）。だから NN 強化は提出に活きる（issue #4）。
- 公式サンプルに AlphaZero 型 MCTS があり、提出環境で torch/NN が使える見込み（要最終確認）。

## 12. 速度の正しい理解（誇張しない）
- `ratchet-nn`（NN 操縦の探索）は「**速度**」ではなく「**探索の質**」の改善。現行 ratchet の ISMCTS は
  速度優先で弱い設定（探索が雑）なので、NN-MCTS のほうが per-move では遅くても**評価が賢い**。
- 「NN は ISMCTS の約1/4時間」は**同じ強さの ISMCTS と比べた話**。弱い設定の ISMCTS とは前提が違う。

## 13. 効果文を「読んで数値カテゴリ化」する（NN v2・規約上OK）
- **背景**: 当初「効果文（Pokémon Element）は読まない」と過剰に自主規制し、NN が手の効果を理解できず
  policy が手を順位付けできなかった（champion で 0.15 など）。
- **規約の再確認**: 効果文を**モデルの学習/推論に使うのは許諾の範囲内**（ルール L73）。禁止は
  **公開・再配布**（L81/L190）と競技外利用のみ。派生した数値や重み・embedding は参加者の生成物で
  Pokémon Elements ではない（L306）。→ **読んでよい。出さない（commit/公開しない）だけ。**
- **判断**: エンジン JSON の attack/skill `text` を**汎用ゲーム機構のキーワード**で効果カテゴリ
  （draw/heal/KO 等10種）のビットフラグに変換（`cards._effect_bitmask`）。生テキスト・カード名は保持/出力しない。
- **効果（実測）**: champion vs heuristic 0.15→0.4台・vs ismcts 0.15→0.45 と**カタストロフィ解消**。

## 14. cardId 埋め込み＋容量↑（NN v2・表現力を上げる）
- **背景**: 数値特徴だけでは「カード固有性」を粗くしか表せず、小さいネット（hidden256・2層）は
  ISMCTS を真似きれず**教師より下**（40試合で heuristic/ISMCTS 未満）で頭打ち＝**容量・表現の限界**。
- **判断**: 特徴ベクトル**末尾に整数 cardId** を載せ（features の `N_STATE_ID`/`N_ACTION_ID`）、net が
  分離して **Embedding**（`net.PVNet`・card_emb16・共有・pad0）。容量も hidden512・trunk3層に拡張。
  Sample/encode の型は不変（id は float 配列の末尾に同居）＝侵襲最小・warm-start 契約は廃止（再学習前提）。
- **狙い**: 学習でカード固有の有用性を捉え、ISMCTS を**真似きる→超える**表現力（prize-zone の核）。
- **訂正（2026-07-02・撤回）**: この拡張は**裏目**だった。診断で policy が一様（教師再現度 0.10≈偶然・
  max prior 0.16）＝**大容量が蒸留データ量（数デッキ・~1万サンプル・登場 cardId 数十種）に対して過大**で
  未学習と判明。1300 カード埋め込みの大半が初期値のまま。→ **埋め込み撤去・hidden256/2層に right-size**
  （`card_emb=0` で埋め込みは将来データ増時に解放可）。汎化する効果/KO/弱点 float 特徴は残す。
  **教訓: 容量はデータ量に合わせる**（[現在地](#現在地と次の判断2026-07-02)）。

## 15. 接地 floor（net 非依存で pilot ≥ heuristic を保証）
- **背景**: NN-MCTS は MAIN/ATTACK 以外を heuristic にフォールバック。半端な net は攻撃選択を悪く
  上書きし **heuristic を下回る**。しかも net の value が誤るデッキでは**探索を増やすほど悪化**（救えない）。
- **判断**: NN の手と heuristic の手が違うとき、両者を**heuristic ロールアウトで実地比較**
  （`ismcts.evaluate_actions_by_rollout`）し、**接地評価で heuristic を上回る時だけ NN 手を採用**
  （`nn_mcts` の `floor_rollouts`）。net が間違っても pilot は heuristic を下回らない。
- **効果（実測・champion・floor=8）**: 素 net 0.25→pilot **0.575**(vs heuristic) / 0.175→**0.45**(vs ismcts)。
  net 非依存の安全弁で**≥heuristic・≈ISMCTS の使える pilot**が即完成。推論/評価/提出向け（収集では未使用）。

## 16. 特徴 v2.1（サイド価値・有効ダメージ・tera）— ex の2枚取りを明示
- **背景**: Kaggle リプレイで気づいた欠落。**ex は KO でサイド2枚・megaEx は3枚**取られるが、特徴は
  `is_special`(ex/mega/tera/aceSpec 一括の bool)だけで**枚数を区別せず**、行動側に「この KO=何枚」も無かった。
  さらに **KO 可能フラグが基礎ダメージ**で、弱点×2/抵抗−30 を織り込まず「弱点で倒せる/抵抗で倒せない」を誤っていた。
- **判断（cards.py / features.py）**:
  - `prize_value`（**megaEx=3 / ex=2 / それ以外=1**・ex/megaEx フラグ由来）と `is_tera` を追加。tera は
    **prize ではなくベンチ無敵**（ベンチのワザダメージ無効）なので prize と**独立**に扱う（tera非ex でも prize は正しく1）。
  - ポケモン特徴（active/bench 両方）に prize_value/tera/appearThisTurn/maxHp絶対/抵抗有無 を追加。
  - 行動の相互作用を **有効ダメージ**（弱点×2・抵抗−30）で再計算し、**KOサイド報酬（=KO可能×相手サイド価値）**
    を明示。これで NN は「弱点を突いて ex を倒す2枚取り」を直接評価できる。
- **注意**: 効果は NN の特徴層のみ。**ISMCTS/エンジンは元から弱点・抵抗・サイドを正確に処理**（提出=ISMCTS は影響なし）。
  弱点×2/抵抗−30 は標準近似（エンジンが真値）。特徴次元が変わるため**旧 net は無効＝床を一度作り直す**。

## 17. best 選抜は「loss 最小」でなく「eval 勝率」で行う
- **疑問**: eval 間の iter から**loss 最小**を選んで eval すればよいのでは？ 最新 iter で eval する必要は？
- **却下理由（loss は選択基準に使えない）**:
  1. **loss は強さの弱い代理**。低 loss ＝教師 argmax への過学習で**むしろ弱くなりうる**。policy_loss には
     教師の決定エントロピー分の**下限**があり、value_loss は容易に 0.05 まで落ちる＝**loss 最小 ≠ 最強**。
  2. **iter 間で比較不能**。毎 iter リプレイバッファが入れ替わる＝**測っている問題（データ）が違う**。
     「loss 最小 iter」は「バッファがたまたま簡単だった iter」を拾っているだけかもしれない。
- **方針**: 選択は**実目的＝eval 勝率**で（best-gate・CRN＋確認評価）。loss は進捗の**安価な診断**にとどめる。
- **なぜ最新 iter で eval**: 学習は逐次で次は最新から続く＝**最新が軌跡の現在地**。非最新の「loss 最小 iter」を
  採るには全 iter 保存＋ロールバックが要り、基準も loss（弱い代理）＝二重に損。best-gate は eval で拾うので
  良い iter は失われない（loss が上がっても最新で eval するのは、**loss を選択に使っていない**から）。
- **iter ノイズの正しい対処**: eval を厚く（試合数↑・CRN・確認評価＝実装済）か、**重み平均（EMA/SWA）**で
  SGD ノイズを均す。loss 選抜ではない。関連 [glossary: 重み平均](glossary.md#重み平均ema--swa)

## 18. improve 改修（深い収集＋Dirichlet 根ノイズ）— 横ばいの対策
- **背景（2026-07-03 実測）**: improve 90 iters（~12h・sims=64/dets=2）で raw eval 0.40〜0.46 の横ばい。
  floored でも種（distill_best 0.575/0.600）を超えられず（0.450/0.575）＝**利得ゼロで撤退基準に該当**。
- **診断**: improve の学習信号は「**探索後の soft-π − 素の policy**」の差分。素の policy が平坦なうえ
  sims=64/dets=2 の浅い探索では π も大して賢くならず、**「ほぼ自分を自分に蒸留」**して横ばいになる。
  相手も自分（弱い者同士）で value の勝敗ターゲットも情報が薄い。
- **改修（3点）**:
  1. **収集専用の探索深度** `--collect-sims/--collect-dets`（improve 既定 collect-sims=128）: 収集だけ
     探索を深くして教師の質＝差分を太らせる。eval/推論の深さは従来のまま。**iter は重くなる（質＞回数）**。
  2. **Dirichlet 根ノイズ** `--root-noise`（既定 ε=0.25・AlphaZero 標準）: 根の priors に
     `P←(1−ε)p+εη, η〜Dir(10/手数)`。self-play の探索が毎回同じ手に固まるのを防ぎ、データの多様性
     ＝改善オペレータの探索範囲を保つ。**推論/評価には掛からない**（強さを測る時は決定的）。
  3. **弱い improve 資産の退避**（models/attic/）: NN_NET/EVAL_NET が improve_best を自動優先するため、
     種より弱い improve_best が黙って使われる事故を防ぐ。次の improve は distill_best から再出発。
- **教訓**: self-play は回せば強くなるのではなく、**「探索が素の方策より賢い」条件が崩れると空転する**。
  空転の兆候は eval_winrate の横ばい（loss は下がり続けるので当てにならない・[§17](#17-best-選抜はloss-最小でなくeval-勝率で行う)）。

## 19. 提出操縦の NN 対応（issue #4・floored NN-MCTS＋時間ガード）
- **背景**: 提出アダプタは ISMCTS/heuristic/random のみ対応だった。floored NN が ISMCTS を超えたので、
  提出操縦も NN 化して実戦成績に反映する（[design 11](#11-提出物には操縦も含まれる)）。
- **判断**: improve の成否に関わらず**一度だけ実装**（現 floored NN が既に ISMCTS 超えのため作る価値がある）。
  - `make_kaggle_agent("nn")`＝**floored NN-MCTS**（[design 15](#15-接地-floornet-非依存で-pilot--heuristic-を保証)）。
  - **torch は nn 選択時のみ遅延 import**（ismcts/heuristic 提出は torch 不要のまま）。net は常に **CPU** 読み
    （batch=1 は CPU が速く、提出環境の GPU 有無に非依存）。
  - **時間ガード**: NN-MCTS は sims 固定で自前の時計を持たないため、累積消費が game_budget(540s) を超えたら
    heuristic（瞬時）へ退避＝**600秒クロックの時間切れ負けを防ぐ保険**（[glossary: 提出の時間管理](glossary.md#提出の時間管理game-budget--時間ガード--600秒クロック)）。
  - `make submission-nn`（最良 net = improve_best>distill_best 自動選択）・`make smoke-submission`（実対戦で
    時間実測）。NN 系モジュールは常に同梱（遅延 import なので ismcts 提出でも無害）。
- **注意**: 提出環境の CPU はローカルより遅い可能性。煙テストで 2倍以上の余裕を確認してから提出する。

## 20. gate の上振れ昇格対策（mean ガード＋確認評価）
- **実測の事故**: ratchet-nn の gate（20試合）が worst +0.05 / **mean −0.077** の候補を昇格させた。
  40試合・別シードの再戦では旧チャンピオンが明確に上（mean 0.697 vs 0.571）＝**ノイズドリフト昇格**だった。
- **原因**: worst（min-of-16）は最もノイズが乗る統計量（1相手 SE≈0.11）なのに、旧ルールは
  「worst が margin 超で勝てば mean を見ずに昇格」。さらに**エンジン内部のシャッフル乱数は外から
  シードできない**ため、同一シードのペア比較でも対戦条件は完全一致しない（run 間の揺れが大きい）。
- **対策（champion_gate.py）**:
  1. **mean ガード**（`--mean-guard` 既定 0.05）: worst で勝っても mean がこの幅を超えて劣化していたら
     不採用（今回の事故はこれだけで防げた）。
  2. **確認評価**（既定 on・`--no-confirm` で省略）: 昇格候補が出た時だけ**別シードで再評価**し、
     両方で勝った時のみ昇格。**コストは候補が出た時だけ2倍**＝据え置き run は従来コストのまま。
- **教訓**: [§17](#17-best-選抜はloss-最小でなくeval-勝率で行う) と同型。「1回の eval で意思決定しない。
  昇格・採用の判断には必ず独立な再現を要求する」。worst-case 目的は正しいが、**worst の数字は
  平均より桁違いに揺れる**ことを常に織り込む。

## 21. 大きな投資（GPU日単位）の前の判断基準
- **背景**: 「盤面特徴 v2.3 を実装すべきか」の議論で、慎重さの根拠を明文化した。
- **このプロジェクトの実績（基礎率）**: 「NN に足す」投資は勝率が悪い——埋め込み＋容量↑(悪化・撤回)、
  soft-π(悪化・撤回)、深い収集(実験で棄却)、improve 90iters(利得ゼロ)、混合プール(効果検出できず)。
  効いたのは**直す・測る・削る**——right-size、強教師、floor、根ノイズ＋fresh種、eval ノイズ除去、
  実メタ較正、gate 強化。「足せば良くなるはず」には自動的に懐疑をかける。
- **投資前チェックリスト**:
  1. **メカニズムレベルの診断があるか**（「なんとなく弱い」でなく「value が盤面に盲目」のような特定）。
  2. **因果の鎖を書き下し、各環の検証状態を確認**（例: 特徴追加→valueが学ぶ→**手が変わる**→勝つ。
     どこか一つでも未検証の環があれば、そこを先に安く検証できないか考える）。
  3. **もっと安い代替検証があるか**（例: 学習させる前に、手作りの補正を value に注入して効果の上限/下限を測る）。
  4. **成功基準と撤退基準を事前に決める**（何 iters・どの指標がいくつなら継続/打ち切り。
     sunk cost での grind を構造的に防ぐ＝improve 90iters の教訓）。
  5. **機会費用**: 同じ GPU 時間で他にできることが尽きているか（デッキ軸・計測系の安い改善が残っていないか）。
- **慎重≠不作為**: 証拠が揃えば（敗因分布の安定・他の投資先の頭打ち・診断の確定）、基準を満たした投資は
  迷わず実行する。このチェックリストは「やらない理由」でなく「やるなら勝てる形にする」ためのもの。

## 22. 盤面特徴 v2.3（board-blind の恒久修正・§21 の実践）
- **経緯**: value の盤面感度 ≈0（[§18 後の診断]）→ §21 のチェックリストに従い、**学習の前に注入テスト**
  （value に `α×(自駒−敵駒)/5` を外挿・40試合×α スイープ）で因果の最後の環を検証:
  **vs ismcts が α に厳密単調増加（0.450→0.500→0.525→0.575）・α=0.2 で平均 +0.075・0.3 で頭打ち** ＝ GO。
  即効処置として α=0.2 の注入を提出・判定に配線済み（[design 19](#19-提出操縦の-nn-対応issue-4floored-nn-mcts時間ガード)の拡張）。
- **v2.3 の設計**（このブランチ）:
  - **特徴 5 float**（`BOARD_FEAT`・**末尾 cardId の直前**に追加）: 自分/相手の場の駒数・**駒数差
    （注入で実証したシグナルそのもの＝value は重み1個で拾える）**・手札のたね数・総資源。
  - **warm-start が成立する配置**: id 列は net が捨てるため、id 直前への float 追加は旧 float 列の位置を
    変えない。旧重みを先頭コピー＋新列ゼロ初期化＝**学習開始時の挙動は現 net と同一**（床の再蒸留は不要）。
  - **学習経路**: 収集評価器にも α=0.2 を注入（`--collect-board-bonus`）＝「盤面を読む探索」の訪問分布を
    net に蒸留（AlphaZero の償却）。value は明示特徴＋勝敗ラベルから盤面感度を回帰。
- **事前登録基準（改修 improve 30〜60 iters 後）**:
  - 成功 = (a) diagnose_value_board で**盤面感度 ≥ +0.05**（現状 +0.012）かつ
    (b) **注入なしの新 net ≥ 注入ありの旧 net（vs ismcts 0.575 目安）** → 注入を撤去し学習版へ置換。
  - 撤退 = 感度 < +0.03 or 強さ改善なし → 注入運用を継続し v2.3 は打ち切り（注入が既に価値の大半を回収）。
- **検証1回目（2026-07-07）は無効試合**: improve_best(EMA) が旧 seed と小数第3位まで一致＝
  **EMA の減衰率バグ**（per-step 前提の 0.999 を per-iter 更新に適用→0.999^30≈0.97 で学習が実質凍結）。
  EMA 導入以降の improve_best は全て「ほぼ種」だった（過去のプラトーの一部を遡って説明）。decay を
  0.9 に修正して再検証。教訓: ①移植したハイパーパラメータは**更新頻度の前提**ごと確認する
  ②同一 net 疑いは「数値が一致しすぎる」ことから検出できる（差分 B−A を主指標にする）
  ③盤面感度の絶対値は進行役 heuristic の局面分布に交絡する＝**新旧の差分だけが有効な信号**。

## 23. self-play 崩壊の診断と対策（温度スケジュール）
- **実測（2026-07-07）**: EMA 凍結バグ（[§22 追記]）を剥がして生 net を測ると、v2.3 improve 30iters の
  生 net は**種より弱く（floored vs ismcts 0.675→0.425）**、盤面感度は**逆相関**（B−A: 盤面 −0.09/危険 −0.105）。
  self-play は「学ばない」でなく「壊していた」。
- **原因（データ生成の毒）**: 手選択を**全手 π サンプリング**していた。浅い探索（64sims/2dets）の平坦な
  π から終盤に悪手を引き、「勝勢だったのに乱心で負けた」試合の z が全局面に付く → value が
  「盤面が薄い側が勝つ」という**データに実在する偽相関**を正しく学習してしまう。ネットや符号の
  バグではなく**教材の質**の問題（だから「逆を実装」しても直らない）。
- **対策（AlphaZero 標準に揃える）**:
  1. **温度スケジュール**（`--opening-samples` 既定12）: サンプリングは序盤 N 決定のみ・以降 argmax
     ＝対局の質を保ち z の汚染を止める（本命）。
  2. **収集の盤面補正を既定 OFF**（`IMPROVE_COLLECT_BONUS=0`）: 未検証要素の切り分け（§21 一度に一変更）。
  3. **カナリア評価**: eval のたびに**生 net** も先頭デッキで測って表示＝EMA が劣化を隠す事故を構造的に防ぐ。
- **成立条件の言語化**: self-play が機能するには「探索 > 素の方策」（§18）に加えて
  **「対局の質が z を信頼できる水準に保たれる」**が必要。温度スケジュールは後者の担保。

## 24. NN 学習ラインの凍結 — 「データに無い信号は学べない」
- **4経路すべてが同じ結論**: 盤面感度（盤面差が勝敗に効くという value の学習）を、
  ①self-play ②混合プール ③v2.3 明示特徴 ④教師あり distill の**独立4経路すべてで動かせなかった**
  （B−A 感度差 ≈0、時に逆行）。強さ（floored vs ismcts 0.575〜0.6）は保たれるが、盤面理解は不変。
- **最終診断**: ベンチ切れは**実戦の相手分布の現象**（相手が薄い盤面を罰する）で、ISMCTS 同士の
  質の高い対局データには稀（互いに上手く補充する）＝**信号がデータに存在しない**。特徴を与えても
  教師ありでも、データに無い相関は学べない。self-play は加えて「量の壁」（z の試合相関・§23）で崩壊。
- **含意の逆転（§22 の前提を反転）**: 「注入は暫定・学習版で置換」ではなく、**注入（盤面補正α0.2）を
  恒久解として正式採用**する。**学習で獲得できないドメイン知識は、検証済みの事前分布として注入する**
  ——これが本プロジェクトで得た最大の一般教訓。
- **決定**: NN の学習ラインを凍結（operative net = seed 由来 v2.3 形式＋注入で運用）。ExIt 案は撤回
  （π/value は改善できても、データに無い盤面信号は ExIt でも作れない＝期限リスクに見合わない）。
  残り時間はデッキ軸（ratchet-nn・実メタ帯）と実戦 A/B（nn_repaired）と提出選択に集中。
- **再開条件（将来）**: 実戦 replay を**訓練データに混ぜる**（相手分布ごと学ぶ＝唯一データに信号が入る道）・
  試合数を桁で増やせる計算資源。

## 25. 実戦 replay を value 学習に混ぜる（データに信号を入れる唯一の道）
- **§24 の裏返し**: 「盤面信号はデータに無い」なら、**信号のあるデータ＝実戦 replay を訓練に入れれば学べる**。
  実戦の z（＝薄い盤面で実際に負けた結果）には信号が入っている。self-play/distill が全滅したのは
  ISMCTS 同士の対局に信号が稀だったからで、相手が本気で薄い盤面を罰す**実戦だけ**が例外。
- **設計（offline value fine-tune）**:
  - `extract_replay_samples.py`: episode JSON の**両席**・全決定局面から (state, z) を抽出し .npz に
    冪等永続化（JSON はこの後削除可）。**相手席も入れる**＝上手い相手が薄い盤面を罰した局面＝信号の本体。
  - `replay_value_tune.py`: operative net を warm-start し **`policy_weight=0` で value 頭だけ** fine-tune
    （我々の手のクローン=自己蒸留・相手の手=雑音で policy を汚さない）。holdout MAE で早期見切り。
- **訓練データ選択の逆説（重要な一般教訓）**: 「良いデッキ」と「良い訓練データ」は別基準。value に
  「薄い盤面＝負け」を教えるには**その故障モードが濃く起きた対局**が要る。故に**エネ過多の弱いデッキの
  replay（ベンチ切れ負け63%）の方が、修正済みデッキ（同25%）より教材として濃い**。value は z だけを学び
  デッキ構成は学ばない＝弱いデッキの対局で学習しても汚染されない。→ **全ソース（nn/nn_repaired/others）を
  混ぜる**（量と多様性が小サンプル value 回帰に効く）。
- **事前基準**: 盤面感度 B−A ≥ +0.05（4経路で一度も動かなかった数字が初めて動くか）かつ floored 強さ維持。
  成功→注入に頼らず value が本物の盤面判断を持つ（提出 net に採用）。失敗→実戦データでも学べない＝
  §24 の「注入が恒久解」が最終確定。**どちらでも決定的な知見**。
- **日次運用に組込**: DL → `make replay-extract` → JSON 削除。サンプルが積み上がるほど効く。
- **結果（2026-07-08・決着）**: 実戦47,625サンプルで value fine-tune。**検証MAE 0.41→0.12＝実戦データに
  信号は実在し value は学べた**（§24の「教師対局には無い」を精密化）。だが**強さは劣化**（vs ismcts
  0.575帯→0.500、穏やか tune でも 0.425）で、盤面感度も強さを犠牲にした時だけ動く用量反応（動かせば
  壊れ・動かさねば学ばない＝良い動作点なし）。**注入 α=0.2 が net を触らず強さを落とさず同じ認識を
  与える**ため実用では注入の完勝。→ **§24「注入が恒久解」が最終確定・NN は operative+注入で凍結**。
- **副産物の価値（低スペック戦略の実証）**: 「実戦データ×分単位の学習」で value が本当に学べたこと自体は
  重要（軍拡でない勝ち筋の存在証明）。今回は注入に負けたが、**将来 policy を実戦データで学ぶ**等の
  低計算・データ駆動の道が生きていることを示す。パイプライン（extract/tune）は資産として残す。
- **組み合わせも測定で棄却（2026-07-08・同一セッション比較）**: replay-tuned+注入0.2=0.450 /
  +注入0.1=0.400 vs **operative+注入0.2=0.675**（vs ismcts 40試合）。注入を弱めても悪化＝二重計上でなく
  **素の value の質が土台で、replay-tune がそれを崩すと注入では埋まらない**。注入は「board-blind な良い
  value」に足すから効く。→ **提出 NN = operative + 注入α0.2 で確定凍結**（測定裏づけ済み）。
- **【重要な訂正・2026-07-08】上記「棄却」はミラー-ISMCTS 測定のアーティファクトだった**: 「replay-tune が
  強さを劣化させる（0.675→0.500）」の根拠は `eval_net.py`＝**同一デッキで NN vs ISMCTS のミラー対戦**。
  この単一・非中立リファレンスを疑い、**外部基準（実メタ16デッキ・非ミラー・eval-deck）で net A/B を測り直した**:
  - champion_best: operative 平均0.469 / replay-tuned 平均0.494（ペア差+0.025・SE≈0.055＝**有意でない**）
  - champion_repaired: 両者とも平均**0.350（完全同点）**
  → **実メタでは operative と replay-tuned は統計的に区別できない**（ミラーで見えた operative 優位は消滅）。
  §25 の「replay-tune は劣化」「NN 章を測定でクローズ」は**誤った測定台に基づく誤結論**。replay-value 経路は
  死んでおらず、依然として生きた選択肢（少なくとも無害・実戦の盤面感度は本物）。
- **プロセスの教訓（デッキ軸の再発）**: **net の比較をミラー-ISMCTS（eval_net）で行ってはいけない**——
  デッキ軸で共適応ロック対策として導入した「外部基準（実メタ・実戦A/B）で測る」原則を、net 軸に当てて
  いなかったのが根因。net の判定も必ず実メタ相手プールで行う（[glossary: ミラー評価の罠]）。
- **恒久対策（警告でなく既定を変える）**: `eval_net.py` の `--vs` 既定を `meta`（実メタ相手プール・非ミラー・
  eval_deck_vs_meta 共用）に変更。ミラー（heuristic/ismcts）は「大まかな確認のみ」に降格し警告を出す。
  「気をつける」ではなく**道具の既定を正しい方に倒す**（受動的な注意書きは再発する）。
- **ただしローカル実メタでも十分ではない**: ローカルプールは実 Kaggle と**デッキも操縦相手も異なる代理指標**。
  net/デッキの最終裁定は依然 **Kaggle 実戦 A/B** に委ねる（gauntlet は proxy metric・issue #3）。
- **replay 収集は「デッキ軸」と「NN軸」両方に効く共通投資**: DL した replay は ①相手デッキ抽出
  （opp_decks・gauntlet-real・determinize 相手推定§26）と ②value 学習サンプル（value_samples・replay-tune§25）の
  **両方**を太らせる。実メタ情報が増えるほど「デッキ強化」だけでなく「NN 強化（盤面信号の学習）」も進む
  ＝日次 replay DL は軍拡でない共通の底上げレバー。詰まって NN 章を再訪する時、サンプルが桁で増えていれば
  replay-value の勝ち目も上がる（今回同点だったのはサンプル 47k・小規模ゆえの可能性）。
- **決定への影響**: 凍結という**行動は無害**（operative は実メタで replay-tuned と同等＝失ったものは無い）。
  提出 net は継続性で operative を維持。ただし理由は「operative が強い」ではなく「両者同等」に訂正。
  replay-tuned は実戦の盤面感度を持つため、**Kaggle A/B 枠が空けば第3変種として試す価値**がある
  （ローカル同点でも実戦のベンチ切れ故障モードでは差が出る可能性）。

## 26. determinize 相手デッキ推定 — 推論時×データ駆動の低計算レバー
- **戦略的位置づけ**: 低スペック環境で訓練の軍拡には勝てない（§25 副産物と同じ問題意識）。だが対局は
  **推論時に 600 秒予算**が全員平等に与えられる＝**推論アルゴリズムの改善は計算格差を中和する**。
  さらに相手推定は `data/replays/opp_decks/`（日次 replay で増える実メタ）を事前分布に使う＝
  **データが増えるほど強くなる**ループで、これも軍拡でない勝ち筋。NN 凍結後の主軸に据えた。
- **問題**: determinize は相手の隠れ札（山札・手札）を仮定してフォワードシミュレーションする。従来の
  `pick_opponent_deck` は候補デッキ群を**ハード整合フィルタ**（可視札を完全に含むデッキだけ残す）＋
  **一様抽選**。中終盤で可視札が増えると完全一致が消えやすく**ミラー仮定へ崩れる**脆さがあった
  （相手が変異 1 枚積んだだけで全候補が脱落）。
- **設計（ソフトなベイズ更新）**: 相手の可視札（捨て札・場・表サイド）を尤度の証拠に、候補 d を
  `weight(d)=exp(-β·misses(d))` で重み付けて比例サンプリング（misses＝d が説明できない可視札枚数、β=2.0）。
  ハード整合＋一様抽選の**厳密な一般化**で、①完全整合（misses=0）は従来どおり最優先、②序盤（可視札少・
  多数が misses=0）は広く一様＝不確実性を反映、③中終盤は自動で真アーキタイプへ収束（belief sharpening）、
  ④完全一致が無くても**最も近い実デッキへ滑らかに縮退**（ミラーへ崩れない）。各 determinization 抽選ごとに
  適用＝探索木全体が「ありえない相手山札」を辿らなくなる。訓練ゼロ・追加計算はデッキ枚数の Counter のみ。
- **配線ギャップの修正**: `build_submission` は候補を `data/*.csv`（5個）からしか同梱していなかった。
  実 replay 抽出の `data/replays/opp_decks/`（79個・実ラダーのメタ）を優先同梱に変更（重複除去）。
  推論の事前分布が初めて実データを反映＝上記ループが機能する前提を満たした。
- **検証**: `tests/test_determinize.py`（ダミー ID・cabt 非依存）で ①完全説明デッキが圧倒（e^-6 差）、
  ②可視札ゼロは一様、③完全一致無しでも最近似へ縮退しミラーに落ちない、④プール空なら従来 fallback、を確認。
- **次**: 実戦 A/B（opp_pool あり/なしの提出）で効果を測る。効けば「β のチューニング」「表サイド・使用
  トレーナー列など可視情報の重み拡張」「ターン跨ぎの belief 保持」へ拡張余地。

## 27. ratchet の実メタ化と高速化（探索の相手＝実メタランダム・gate 軽量）
- **見落としていた本丸**: ratchet の探索（league）の相手は**公式サンプル5デッキ**（`data/*.csv`）だった。
  gate だけ実メタにしても、**探索が的外れな相手に最適化していれば実メタ好adの候補が生成されず**、
  gate は弾くだけで前進しない。「デッキを強くする」なら**探索の相手こそ実メタ**でなければならない。
  ＝共適応ロックの相手プール版（[glossary: 共適応ロック]の破り方＝外部基準を探索にも当てる）。
- **対策（探索）**: 探索 seed を **gauntlet（実メタ）からランダムに SEARCH_SAMPLE=5 抽出**（`shuf`）。
  **top固定でなくランダム**＝毎実行で別の5＝特定デッキへの過適合を回避し実メタ全体に頑健化。
  ＋ champion を試験官に固定（継続性）。gauntlet 無ければ公式サンプルにフォールバック。
- **高速化（害なし）**: 探索の NN-MCTS sims を **64→32**（自前測定 sims32≈64＝強度差なし・§本節）。
  gate は new/best の**相対判定**なので **sims32・floor4・相手は実メタ上位8**に軽量化（floor は両者に
  等しく効き相殺）。GATE_GAMES=20・確認評価は据え置き（誤昇格対策）。**eval-deck（絶対強度の確定評価）は
  提出忠実 sims64/floor8・相手16 のまま**。＝「普段は速く・決め打ちは厳格」の層構成。
- **層構成でのカバー**: 探索5（狭・速）／ gate 8（相対フィルタ）／ eval-deck 16（絶対確定）。探索が
  ランダム5に過適合しても、より広い gate/eval-deck が昇格前に検出する。すべて実メタで一貫。
- **実測での訂正（2026-07-09）**: `time make ratchet-nn RATCHET_ITERS=15` = **1063分**（17.7h・gate 途中で停止）。
  分解すると **league ≈ 約64分/iter**（当初 ~21分見積もりは**3倍楽観**＝低スペックの1試合 ~8-11秒を過小評価）、
  旧 gate ≈ 約2h。**探索が律速**。→ overnight iters を 15→**7**（≈8h）に、無印(3)は≈3.5h と現実化。
- **gate の2プロファイル分離（実測を受けて）**: ① `make champion-gate`＝単独・厳格（相手12・16試合・約1〜1.5h）
  ② ratchet 内蔵 gate＝毎サイクル・軽量（相手6・12試合・約30〜45分）。**floor は 4→0**（相対判定で相殺・
  重さの本体＝終端ロールアウトを除去＝最大の効き）。両者とも確認評価は残す。eval-deck は提出忠実(64/8・16)のまま。
- **成果の実証（ratchet 初成功）**: 実メタランダム探索の出力が gate 初 eval で **new 最悪0.250/平均0.481 >>
  best 最悪0.100/平均0.390**＝実メタ相手に明確に上回る候補を生成（§27 の狙い＝探索を実メタにが機能）。
- 全て Makefile 変数で上書き可（SEARCH_SAMPLE/SEARCH_SIMS/GATE_OPPS/GATE_GAMES/RN_GATE_OPPS/RN_GATE_GAMES/GATE_FLOOR）。

## 28. Docker は「使い捨て（run --rm）」のまま — 持続コンテナにしない
- **問い**: `make` の各 Docker ターゲットは `docker compose run --rm dev <cmd>`＝毎回コンテナを生成・破棄する。
  一度 `up` して `exec` で叩く持続コンテナにまとめた方が速いのでは？
- **判断: 使い捨てのまま**。この構成では持続の利点がほぼ無く、むしろ欠点が出る。
- **なぜ利点が出ないか**:
  1. **起動回数が少ない**: ratchet-nn 1回で `run` は league（全 iter を1コンテナ内でループ）＋ gate の**2回だけ**。
     overnight（5〜6h）でも起動2回＝数時間に対し生成オーバーヘッド（〜1〜3秒）は誤差。
  2. **状態はホスト側マウントで永続**: `compose.yaml` が `.:/workspace`・`data`・`models` をバインドマウント。
     checkpoint やモデルは**コンテナでなくホストのファイル**に残る＝使い捨て間で resume が効く（league→gate で実証）。
     持続にしてもここは何も改善しない。
  3. **遅さの実体は torch import でプロセス単位**: `exec` でも `python` 起動ごとに発生。持続で減るのは
     コンテナ生成の数秒だけ。
- **むしろ使い捨てが良い理由（特に low-spec）**: ①コマンド後に RAM/GPU を解放（持続は常時抱える）②`make build`
  後に常に最新イメージを使う（持続は再起動まで stale）③up/down のライフサイクル管理不要 ④長い処理と別コマンドを
  独立起動できる（並行に強い）。
- **持続が効くのは「短いコマンドを高速に何十回も叩く」時だけ**＝この repo は軽い作業（lint/test/smoke）を
  **ホスト Python** に振り分け済み（Makefile の住み分け）ので該当しない。対話的に温めたい時は `make shell`。
- **既知の小不整合（未修正・任意）**: `make up` で持続 dev を起動しても `make exec` は `run --rm`（別の使い捨て）を
  使い up 済みコンテナを活用しない。rapid な対話デバッグを多用するなら「dev が up 中なら `compose exec`」へ賢化する
  余地はあるが、バッチ運用には不要なので現状維持。

## 29. 「0枚負け」の真因 — エネ加速ゼロ＋操縦の打ち回し欠落（ロール注入で対処）
- **実戦 replay の深掘り（分類でなくサイド差）**: nn の敗北29試合中 **16試合がサイド0枚**（自分が取れた
  サイド平均 0.7/6・ismcts は 1.5）。「僅差で競り負け」でなく**何もできず一方的に轢かれる**。しかも0枚負けは
  相手14デッキに分散＝**特定相性でなく汎用的な立ち上がり不全**。＝敗因分類の「その他」の正体（最終観測が
  終局を数手取りこぼす近似ラベルで、中身は「立ち上がれず殴られ負け」）。
- **構成の機能ロール分解で真因判明**: COMP_BOUNDS（poke/item/sup/…）は帯内だが、効果ロールで見ると
  **draw/search 17枚・energy_accel（展開）0枚**。アタッカーは 340HP・270打点の大型（要多エネ）なのに
  **加速ゼロ＝手貼り1枚/ターンでしか起動できず遅すぎる** → 速い実メタに轢かれる。energy_accel カテゴリは
  メタ全体で健全（特性/トレーナー24件）＝分類バグでなく本物の欠落。
- **根本原因＝凍結nnのテンポ盲目による共適応ロック**: ①COMP_BOUNDS が機能ロールを縛らない ②判定する
  凍結nnがテンポ/展開を評価できない（board-blind・§24）→「遅い」を罰せず、加速ゼロの遅いデッキへ収束。
  共適応ロックの新形態（操縦がテンポ音痴→テンポ不問デッキへ）。
- **対策1（デッキ）: 機能ロールの下限を注入**（`deck.MIN_ACCEL=2`・repair_composition）。実メタ頻出の加速
  トレーナーを**同カテゴリの冗長札と入れ替え**て最低枚数を強制（カテゴリ枚数は不変＝帯を崩さない）。
  §24 の「学べない知識は検証済み事前分布として注入」のデッキ版。検証: champion_best 加速0→2（60枚合法）。
- **対策2（操縦）: 打ち回しの順序を実装**（agents `_choose_main`）。①draw/search/**加速**（掘る＆エネ伸ばす）
  → ②進化 → ③**どうぐ装着** → ④エネ付与 → ⑤ベンチ → ⑥**スタジアム（場に無い時だけ）** → ⑦攻撃。
  **加速札を入れても操縦が打たねば腐る**ため両者は不可分。ABILITY/TOOL_CARD/PLAY を効果マスクで判定。
  検証: 加速注入デッキで6局→**加速札を14回プレイ**（連結が実戦で機能）。
- **凍結nnへの波及**: 提出は floored NN（floor=ヒューリスティック rollout の接地）。**heuristic を強くすると
  全 pilot の床が上がる**（floored NN・ISMCTS の rollout）。＝NN 再学習せずに操縦を底上げできる
  （凍結の制約下での「操縦の再学習」の代替）。
- **次**: この repair＋heuristic で ratchet を再探索（加速入りの速いデッキが探索される）。実戦 A/B で
  0枚負けが減るかを追跡（弱点シフトの検証）。
- **落とし穴（実測で判明・重要）**: 高速化で `GATE_FLOOR=0` にしたら、**gate の pilot が凍結nnだけになり
  加速を打たない**（加速を打つのは floor 経由の改良ヒューリスティック）。→ 加速デッキの利点が gate に
  見えず昇格できない・提出(floor8)ともミスマッチ。**floor は「pilot≥heuristic の接地」だけでなく
  「改良ヒューリスティックの手（加速/どうぐ）を実際に打たせる経路」でもある**。判定を軽くする時に
  floor を削ると、ヒューリスティック改良が全て無効化される。→ 教訓を受けて次項の構造変更。
- **構造変更: ratchet から gate を分離**（2026-07-10）。ratchet＝探索のみ（best を種に→champion_deck 出力）、
  昇格は **make champion-gate を随時**実行。利点: ①探索ループから gate の固定尾が消え **iter を稼げる**
  ②gate が毎回でなく随時なので **floor8（提出忠実・加速を打つ）に戻せる**（重くてよい）。探索は floor 不要
  （repair が全候補に加速を強制注入するので、floor 付き gate/eval-deck が正しく評価する）。

## 29b. 加速注入の実戦検証（1日目）— デッキ側仮説は不支持・機構は機能
- **検証設計が機能した例**: 同一新デッキ（加速入り昇格 best・全108試合で使用を replay から確認）に
  両操縦を載せた純粋な操縦 A/B ＋ 前日（旧デッキ）との日次比較。
- **機構は動いている**: 加速札は実戦の 63〜65% の試合で実際にプレイされた（終局捨て札から検出）＝
  「注入→操縦が打つ」の連結は本番でも機能。
- **だが結果は動かず（仮説不支持）**: 勝率 nn 0.373 / ismcts 0.421（前日 0.408/0.455 と同帯）。
  **0枚負け率 59%/58%・平均獲得サイド 0.7/1.0 で不変**。ベンチ切れ敗因比も戻った（nn 66%）。
  条件付き勝率も「加速を打った試合の方が良い」を示さない（nn 0.375 vs 0.368・ismcts 0.351 vs **0.550**）。
  → **「加速ゼロが0枚負けの主因」というデッキ側仮説は実戦で棄却方向**。§29 の操縦側改良
  （打ち回し順序）は教師強化として引き続き有効（floor/ISMCTS rollout の底上げ）。
- **示唆**: ①注入は tool 1種×2枚＝用量/質が不足の可能性はあるが、条件付き勝率が負なので
  盲目的な増量はしない。②0枚負けが両操縦で 19/19 と同一＝**操縦でなくデッキ/マッチアップ駆動**。
  頻出相手デッキ別成績: 2位 0.29(17戦)・3位 0.27(11戦)＝**頻出アーキタイプに構造的に轢かれている**。
- **次の仮説（低コスト・高情報）**: 進化探索の自作デッキには天井がある（判定操縦の歪み＋探索量）。
  **実メタの勝ち組デッキをそのまま自デッキ候補として測る（ネットデッキ検証）**——我々に勝率 0.7 超の
  頻出デッキ（例: 04621784・a8c57d4b）を eval-deck にかけ、best を上回るなら gate 経由で昇格。
  実メタ 191 デッキと対戦成績ログは既にあり、メタの淘汰圧を借りる方が探索より速い可能性。

## 30. 凍結解除ラウンド1（加速入り distill 1h）— 不採用だが仮説は生存
- **経緯**: §29 の前提整備（訓練デッキへ構成射影＝加速≥2・改良 heuristic 経由で教師 ISMCTS が加速を実演）
  の上で、ゼロから `distill-1h`（50 iter・best は iter19 で捕獲→以後過蒸留退行＝keep-best が正常動作）。
- **外部 A/B（同条件: 昇格 best デッキ・実メタ16・注入0.2・160試合ずつ）**:
  operative **mean 0.594 / worst 0.300** vs distill_best 0.537 / 0.100。ペア差 mean −0.057（SE≈0.07＝ノイズ圏）
  だが worst が明確に劣る（苦手2マッチで崩壊）→ 事前基準「mean ≥ operative」を満たさず**不採用**。
  `pvnet_distill_v24_candidate.pt` に退避（distill_best の存在＝自動採用の罠を解除・operative 解決に復帰）。
- **ただし仮説は棄却されていない**: distill_best は**ゼロから1h**で全系譜の産物 operative にノイズ圏まで迫った。
  伸び代は iter 数（旧系譜は overnight 120 iter 級）。→ `pvnet_distill.pt` から**継ぎ足し続行**し、内部 eval が
  伸びたら再 A/B。**新しい distill_best が生まれるたび、提出前に必ず外部 A/B**（存在＝自動採用に注意）。
- **同日の確定事項**: ratchet 昇格成功（加速入り探索デッキが確認評価込みで旧 best 超え・§29 パイプライン
  が通しで初機能）。gate 分離＋floor8 の公平判定も機能。

## 31. 差をつける推論時レバー2本 — 適応 sims・操縦の残り穴（にげる/昇格）
- **背景**: ネットデッキ（§29b）は他参加者も辿り着く＝土俵に上がるだけで中位止まり。差は
  「操縦の性能」から出る。replay 実測が示した未使用資源: **NN 提出は 600 秒中 ~550 秒を残していた**
  （ismcts は 62 秒まで使い切る）＝推論時間という全員平等の計算資源を9割捨てていた。
- **①適応探索（時間で考える NN-MCTS）**: `make_nn_mcts_agent(game_budget=...)` で有効化。自前時計＋
  「1sim×1det の単価 EMA（floor 込み）」から `plan_search` が今手の **(sims, dets)** を配分
  （残り予算 ÷ 残り決定数見込み・床=従来 64×2・天井 512×16・終盤の使い切り防止に残り決定数の床 8）。
  **dets（決定化の幅）優先**で配る: この net は sims 32≈64 で深さ飽和の実測があり、不完全情報では
  決定化平均（PIMC）の分散低減＝§26 ベイズ相手推定の多サンプル化の方が効くため。**提出のみ有効**
  （eval/gate は budget を渡さない＝固定・測定時間が爆発しない）。
- **①の検証で分かったこと（プローブの効用ふたたび）**: smoke の見かけ（max 0.3〜0.6s/手）が「発火せず」に
  見えたが、plan_search をスパイすると **(64,2)→(141,4)→(152,5)→(129,8)→(130,11) と正しくエスカレート**
  していた。真相＝この net の1評価は ~0.1ms と安く、単価 EMA が下がり続けて配分が拡大するランプ中だった。
  収束後は旧 128 units → cap 8192 units＝**1手あたり64倍の探索量**。単価は実測ベースなので、遅い提出
  環境でも自己適応してクロック超過しない（60s 予算 smoke で 2.2s/21手＝安全側も確認）。
- **②操縦の残り穴（floor が上がれば floored NN・ISMCTS rollout・蒸留教師の全部が上がる）**:
  - **にげる**: 従来 RETREAT を一切使わず「詰み active」のまま殴られ続けていた。保守条件
    （攻撃不能 かつ ベンチのエネ > active のエネ）でのみ交代＝にげるコストの浪費を防ぐ。
  - **昇格/交代先（TO_ACTIVE/SWITCH）**: 従来は**先頭固定**＝KO 後に空のたねを前に出していた。
    エネ最多→HP 残でランク付け（攻撃できる子を前に＝サイドレース直結）。
- **検証が実バグを捕獲（教訓）**: 昇格ランクの初版は option の `inPlayArea/inPlayIndex` で解決したが、
  E2E プローブで「9回発火・全て旧既定と同じ」→ フィールドをダンプすると**エンジンは `index` に
  ベンチ位置を入れ inPlayArea は None**＝解決が常に失敗する no-op だった。`index` ベースに修正後、
  21回中（ベンチに差がある13局面で）6回が旧既定と違う選択＝機能を実測確認。
  **「テストが通る」と「本番の形のデータで動く」は別**——実データの形をダンプして確認する作法。
- 判定: bench 回帰 0.910（劣化なし）・全52テスト green。実戦効果は次の提出 A/B で測る。

## 32. ネットデッキ実戦1日目 — デッキは当たり・操縦の差が主戦線に
- **ネットデッキは実戦でも効いた**: ismcts **0.544**（31/57・**初の勝率5割超えアーム**・前日0.421）、
  nn 0.449（前日0.373）。0枚負けも緩和（nn 59%→41%・敗北時獲得サイド 0.7→1.3）。§29b の
  「メタの淘汰圧を借りる」仮説が実戦で支持された。
- **操縦の差が明確化（3日連続・同一デッキ）**: ismcts > nn が3バッチ連続（0.455/0.408 → 0.421/0.373 →
  0.544/0.449）。同一デッキ2日合算: ismcts 0.482 vs nn 0.410。単日は ~1σ だが、3/3 の一貫性＋機構的
  説明（下記）で信頼に足るトレンド。**「これからは nn だけ」の逆＝ISMCTS が実戦の主力**。
- **機構的説明＝思考時間**: overage 実測で ismcts は **~408s/試合**を接地 rollout に使い、nn は **~28s**
  （§31 適応探索は本番でも稼働: 使用中央値 14.9→19.8→**28.2s** と §31 デプロイ日に増加。ただし
  cap 512×16 が律速で ismcts の 1/14 しか考えていない）。
- **次の含意**: ①最終提出の第一候補は現状 ISMCTS。②nn の追い上げは「cap 増し（dets16→64等）」の前に
  **「探索量が効くか」をローカル A/B（適応 ON vs OFF・同 net）で測る**（§21——evaluator の質が天井なら
  探索を積んでも閉じない）。③distill 継ぎ足し（ネットデッキのミラー訓練）は evaluator の質そのものを
  上げる経路として継続価値あり。

## 33. リーダーボード諜報と1位の方策クローン（デッキと操縦のセット借用）
- **発見（ユーザー）**: リーダーボードから**他チームの replay を DL できる**＝全参加者が無償アクセス可能な
  公開データ（External Data 規約に適合・Kaggle シミュ大会の定石）。`make top-replays` で分析。
- **1位のプロファイル（225 episode）**: 単一デッキ（opp_c921b9b2）・勝率 0.708・**思考時間中央値 ~3秒**。
  §32 の「思考時間＝差」仮説は頂点では不成立＝**強さの源泉はデッキ＋高速で堅い方策**。
  → 探索量（cap 増し）より**方策の質**へ投資、の裏づけ。
- **共適応の実測（ユーザーの予見どおり）**: 1位デッキ×我々の操縦 = **0.537/0.100** ＜ 現 champion
  （a8c57d4b）×同操縦 = 0.777/0.350。1位は同デッキで上位帯相手に 0.708 → **差 ≈ 操縦ギャップ**。
  デッキ単体の借用は不成立＝**操縦もセットで借りる**必要（行動クローン）。
- **教師方策クローンのパイプライン（汎用・チーム名指定制）**:
  `make teacher-extract TEACHER_TEAM=…`（局面→選択を one-hot π で抽出・冪等）→
  `make teacher-tune TEACHER_SAMPLES=…`（operative warm-start・**policy のみ** fine-tune＝§25 の教訓で
  value は触らない・検証 top-1 一致率で early stop）→ 外部 A/B（§30 の作法）。
- **episode プロトコルの解読（重要な実装知見）**: 決定＝`status=="ACTIVE"` の step の select、応答＝
  **次 step** の同席 action（1ステップ遅れ）。同 step 対だと**偶然 range 内の誤ラベルが4割混入**する罠。
  225 ファイル×14,602 決定で 100% 整合を確認してから抽出（「実データの形をダンプして確認」§31 の作法）。
- **抽出実績**: 1位 225 episode → **11,361 サンプル**（MAIN/ATTACK・勝ちラベル比率 0.702≒実勝率）。
- **運用注意**: ①replay JSON は teacher-extract 前に削除しない（value/デッキは npz/CSV 化済みだが
  行動クローンは生 JSON が必要）②`MY_TEAM` を Makefile に固定（自 JSON が無い日に最頻チーム＝他人を
  自分と誤認する事故が2回発生・恒久修正）。
- **判定基準（事前確定）**: (teacher net＋1位デッキ) が現行 (operative＋a8c57d4b)=0.777 帯を超えたら
  セット借用で提出交代。超えなければ現行維持（keep-best・損失なし）。
- **ラウンド1の結果（2026-07-13・不採用だが機構は実証）**: クローンは top-1 一致率 0.166→**0.470**
  （8 epoch・まだ上昇中）。同一プロトコル（floor8・20試合）で teacher＋1位デッキ=**0.588/0.050**・
  teacher＋現championデッキ=**0.700/0.150** → いずれも現行 0.777/0.350 に未達＝**現行維持**。
  ただし ①1位デッキの操縦は確かに改善（0.537→0.588@floor8・**0.681@floor0**）＝機構は機能、
  ②不足は教師データ量（225試合・一致率上昇中）＝1位は日々対局を生産するので**追いDLで継ぎ足し可能**
  （teacher-extract は冪等）。
- **floor がクローンを抑圧する疑い（設計知見）**: teacher ペアは floor0=0.681 > floor8=0.588。floor は
  「我々の heuristic rollout」を基準に NN 手を却下するため、**クローンが学んだ1位流の見慣れない手を
  凡手へ差し戻す**可能性。operative（heuristic の系譜）には守りとして働く床が、**分布の違う借り物方策には
  足枷**になりうる。クローン系 net を評価/提出する時は floor を弱める A/B を添えること。

## 34. Kaggle 提出枠の仕様と A/B 運用（実地で判明・恒久ノウハウ）
- **アクティブ提出は「最新2つ」**: 3つ目を提出すると**最古が自動で落ちる**。狙った2構成を並走させるには
  **提出順で制御**する（残したい構成を後に出す）。今回は teacher → ismcts の順で {teacher, ismcts} を実現。
- **再提出はレートがリセット**（同一 tar でも新規エージェント扱い・数日かけたクライムを失う）。
  ただし逆手に取れば**2構成の同時スタート＝公平な paired A/B** になる（レート差・遭遇帯差の交絡を除去）。
- **提出物の再ビルドは「ついで強化」の機会**: 同梱物（§26 相手推定プール等）は日々成長するので、
  枠を動かす時は必ず `make submission*` で作り直す（今回 79→262 デッキに無料強化）。
- **replay の A/B ラベルはフォルダ名**（data/replays/<variant>/）。新変種はフォルダを増やすだけ。
- **teacher 12h 中間（暫定・確定は1日データで）**: クローン枠は ismcts 枠より劣勢。ローカル判定
  （0.777 vs 0.588）と同じ向き＝**eval-deck の両席操縦交絡は結論を覆すほどではなかった**可能性
  ＝ローカル floor8/20試合プロトコルの信頼度が上がった（判定を早く・安くできる較正情報）。
- **事前登録した分解計画（1日データで実施・後知恵バイアス防止）**: teacher の負け方を3仮説で分類——
  ①**OOD大ポカ型**（floor0 の安全弁なし・短手数/0枚負け多発）→ floor4 で再提出
  ②**ライン切れ型**（一致率47%でコンボの線が切れる・手数が1位の対局と乖離）→ クローン継ぎ足し
  ③**相性型**（特定相手に集中負け・c921b9b2 が我々の帯に不適合）→ 撤退し nn-operative 枠へ差し戻し。

## 35. teacher 実戦1日目の判定 — 継ぎ足し続行・主因は「変換失敗」（2026-07-14）
- **決定**: 事前登録（§34）の分類は**②ライン切れの変種**＝クローン継ぎ足しで続行。ただし**停止基準を
  事前設定**: 継ぎ足し1回（追DL→extract→tune→再提出）後も teacher < ismcts−0.10 が続けば③扱いで
  枠を差し戻す（撤退のダラダラ延長を防ぐ）。
- **理由（実測）**: 同時スタート1日目 ismcts **0.579**(33/57) vs teacher **0.417**(20/48)。解剖の結果、
  短手数負け1件のみ（①OOD大ポカ否定）・相手集中なし（③相性否定）。負けの正体は**手数中央値149の
  長い試合で0枚のまま息切れ**（0枚負け45%）＋**デッキ切れ負け5**＝エンジン（ドロー/サーチ）は回すが
  サイドへの**変換ができない**。
- **機構仮説**: (a)一致率0.47＝決め所の一手で1位とズレる (b)**サブ選択（サーチで何を取るか）が未クローン**
  ＝エンジンデッキの核心技能が我々の汎用ルールのまま (c)value は operative＝このデッキの局面で分布外＋
  floor0 で接地なし。→ 継ぎ足し（a対策）に加え、効かなければ (b) のサブ選択模倣が次の弾。
- **却下した代替案**: floor4 即再提出（負け方が短手数でない＝OOD型でないため効果薄）・即時撤退
  （機構が特定できており継ぎ足しはほぼゼロコスト・1位は日々教師データを生産する）。
- **運用教訓**: others/ の生 JSON は「1位本人の手数・デッキ切れ率」という**基準値の算出**にも使う。
  teacher-extract 後でも、基準値を出してから消すこと（今回は次回の追DLで再取得）。

## 36. 教師交代 — 新1位 TeamA をクローンする（2026-07-15）
- **決定**: クローン教師を TeamB（旧1位）から **TeamA（新1位・0.734/263試合・単一デッキ
  a4066acd・思考中央値25s）** に交代。§35 の「TeamB 継ぎ足し」は打ち切り（stop 基準の発動前だが、
  より強い教師の出現は継ぎ足しの上位互換）。
- **理由**: ①勝率 0.734 > 旧1位 0.708 ②教師データが多い（261 episode→10,214サンプル抽出済み）
  ③デッキ a4066acd は**上位帯の支配的アーキタイプ**（poke19/sup15/ene7/種22 型は TeamG・
  TeamF 等も使用）＝このデッキを学ぶこと自体がメタ理解 ④TeamA は旧1位に 0.385 で負けて
  いた同型デッキ（33f5dae4）を**調整して**頂点を取った＝反復改善の実例。
- **基準値（本人・生JSON から算出済み＝ライフサイクル方針どおり）**: 勝利手数中央値132・敗北166
  （長い試合はこの型の仕様）・**負け試合でもサイド2〜4枚取る**（0枚負け17%・デッキ切れ4%）。
  旧クローンの 0枚45%・デッキ切れ18% と対照的＝§35「変換失敗」診断の裏づけ。クローン評価もこの
  基準（0枚負け率・デッキ切れ率が本人にどれだけ近づくか）で行う。
- **却下した代替案**: TeamB 継ぎ足し続行（教師の質・量とも TeamA が上位互換）・
  a4066acd のデッキだけ借用（§33 で実証済みの共適応＝操縦とセットでないと出ない）。
- **手順**: teacher-tune（出力は pvnet_teacher_teama.pt＝旧クローンと別ファイル）→ 一致率確認 →
  提出物（a4066acd＋teama net・floor0・注入0.2）→ smoke → teacher 枠を差し替え
  （提出順の仕様上、1回出すだけで最古＝旧 teacher 枠が落ちる）。判定はラダー A/B・
  停止基準は §35 と同じ（1ラウンド後も ismcts−0.10 未満なら差し戻し）。

## 37. クローン章の判定 — 2教師で同型失敗＝操縦（複合誤差）が原因・撤収（2026-07-16）
- **決定**: teacher_teama 枠を停止基準（ismcts−0.10 未満）発動で撤収。**行動クローン単体を操縦にする
  路線は closed**（クローンの用途は「探索の事前分布」へ降格＝下記の次弾）。空いた枠は
  **ismcts × a4066acd**（最強操縦×支配的メタデッキ＝デッキと操縦の分離テスト）に差し替え。
- **理由（2実験の収束）**: 教師もデッキも異なる2クローンが同型の失敗:
  TeamB クローン 0.417（0枚45%・デッキ切れ18%）→ TeamA クローン **0.444**（0枚40%・
  **デッキ切れ56%**・本人は4%）。共通項は操縦のみ。決定的な観察: クローンの試合は**本人より約30手
  短いのにデッキ切れ14倍**＝掘る手つきは真似たが**ペース配分と変換を再現できない**。
  ismcts（模倣なし・毎手プラン再計算）は同期間 0.58 帯で過去最高を維持。
- **機構＝行動クローンの複合誤差（covariate shift）**: 1手一致率 0.44 では、長い連鎖の途中で必ず
  「教師が見たことのない局面」（自分のミスが作る）に落ち、そこでは無案内→ミスが雪だるま
  （DAgger が発明された理由）。エンジンデッキ（100-150決定）はこの弱点を最大化する最悪の相性。
- **却下した代替案**: 教師データ追い足し（一致率の頭打ち 0.441 と複合誤差は量では解けない）・
  floor 付与（§33 でクローンを抑圧する実測）・第3教師（同じ機構で失敗する見込み）。
- **次弾（クローン資産 21k サンプルの正しい使い方）**: **方策事前分布として探索に注入**——
  PUCT の prior にクローン policy・葉の評価は接地ロールアウト。模倣は「候補手の順序付け」だけを
  担い、無案内な局面は探索が矯正する。§24「学べない知識は注入」の方策版。
- **成果（負けたが得たもの）**: ①ローカル判定プロトコルの信頼回復（ラダーと2連で同順）
  ②本人基準値との解剖手法 ③教師抽出パイプライン（冪等・汎用）④value_samples 87k への寄与。

## 38. AlphaGo 型 pilot — クローンは事前分布・評価は接地ロールアウト（2026-07-16）
- **決定**: §37 の次弾を実装。`nn_mcts` に `leaf_rollouts` モードを追加——**priors＝クローン net の手筋・
  葉の価値＝接地ロールアウト**（fallback 方策で終局まで・ismcts._rollout を duck-typing で共用）。
  AlphaGo (2016) の SL policy priors＋rollout evaluation と同構成。提出は
  [AlphaGo型×a4066acd, ismcts×a8c57d4b] の2枠同時スタートでラダー裁定（レートは1日で安定する実測）。
- **理由**: 行動クローンの複合誤差（§37）は「クローンを操縦そのものにする」ことが原因。順序付けだけ
  模倣し評価と矯正を接地探索に任せれば、無案内な局面でも探索が正す。ismcts の強さの源泉
  （接地・毎手プラン再計算）を保ったまま、プロの手筋で探索を有望な線に集中できる。
- **「葉を ISMCTS で評価」との違い（ユーザー質問への回答）**: 本設計の外側の木探索そのものが
  ISMCTS（決定化＋木＋ロールアウト）。葉に入れ子の ISMCTS を回すのは同じ計算の非効率な重複
  （内側の木は使い捨て・外側が育たない）。MCTS は訪問の多い葉を自動で展開する＝「有望な所だけ
  深く」は1本の木で構造的に起きる。
- **却下した代替案**: 入れ子探索（上記）・クローン単体操縦の継続（§37 で closed）・
  ローカル ISMCTS 比較を先に挟む（弱予算で非現実的・ラダーが1日で安定するため直接 A/B が速い）。
- **検証**: 54テスト green（合法手・壊れた評価器でも探索が回る＝葉値がロールアウト由来）。
  smoke: 67決定/99.3s（予算120s）＝ロールアウト価格を適応プランナーが正しく配分・クロック遵守。

## 39. デッキ切れの真犯人 — 山札リサイクル札の不使用（マイニングで特定・修正）（2026-07-17）
- **決定**: heuristic に ⓪山札リサイクル（残デッキ≤15 で最優先プレイ・_RECYCLE_AT）を追加し、
  後続の「山へ戻す」選択（TO_DECK/TO_DECK_BOTTOM）を**最大数戻す**に修正。カード判定は
  CardMeta.is_deck_recycle（効果文キーワード・**EFFECT_CATEGORIES には足さない**＝特徴量次元を
  変えると既存 net が全滅するため独立フィールド）。
- **理由（データの連鎖）**: ①サブ選択ペースのマイニングで「取る量」仮説が**棄却**（本人も全量掘る:
  サーチ100%・COUNT中央値1.00）→ ②deckCount 軌跡から本人は **36%の試合で山札を+5枚以上回復**
  （発動中央値=残13枚）・我々は7%→ ③効果文キーワードで**リサイクル札（id 1129・グッズ1枚積み）を特定**
  ＝デッキ切れ 4% vs 52% の差の正体。「掘りは全開・その代わり山を戻す」がこのアーキタイプの解。
- **E2E プローブがまた実バグを捕獲**: 修正 v1 はリサイクルを打てたのに**8回全て0枚戻しで空振り**
  ——後続選択が _generic_select の既定「最小数」に落ちていた。TO_DECK 系を最大数に修正して発動を実測。
  （§31/§33 に続き3例目:「テストが通る」と「実データ/実効果の流れで機能する」は別）。
- **却下した代替案**: サブ選択の量スロットル（データが棄却）・COUNT 抑制（本人72%全量＝軽微）。
- **波及**: heuristic の修正なので **rollout・ISMCTS・AlphaGo型の全系統が同時に改善**。
  提出仕様（最古が落ちる）により alphago 枠だけを1回の再提出で差し替え可能（ismcts のレートは温存）。

## 40. 操縦の行動差分の全量調査 — 差は「エンジン回転数」の1ループに集約（2026-07-17）
- **決定**: モグラ叩き（1差分ずつ）をやめ、`scripts/behavior_diff.py` で**測れる次元を全部並べて一度に比較**
  （ユーザー提案）。同一デッキ（a4066acd）の TeamA 897試合 vs 我々61試合＝共適応の交絡を最小化した比較。
- **結果（回/試合・同じ13ターンで）**: 特性 8.6 vs 4.0（最大差）・進化 12.1 vs 7.9・たね展開 8.6 vs 4.5・
  攻撃 5.6 vs 3.4。総アクション ≈55 vs ≈38＝**回転数1.4倍**。ベンチ数は同じ（4/4）＝密度でなく速度の差。
  T10獲得サイド 2 vs 1・掘る速度も本人が上（T10残17 vs 21）・リサイクル36% vs 7%（§39で修正済み）。
  フェッチ優先はズレ中程度（本人 id741 優先・我々 id305 過剰）。
- **統合診断**: 個別のルール差ではなく「**特性（ドロー）→手札→たね/進化→特性源が増える→…**」の
  エンジンループの回転数が本質。起点＝特性の活用。我々の heuristic は develop-mask（draw/search/accel の
  効果文）に載る特性しか起動せず、それ以外が腐っていた。
- **修正（一括バンドル）**: ①リサイクル（§39・済）②**特性の全面解放**＝dev 該当後に残りの全 ABILITY を
  起動（option に出る特性は合法・1ターン1回はエンジン管理＝空振りしない）。E2E: 提示時の採択率84%
  （起動拒否は解消・絶対数は盤面の特性源数に依存＝ラダーで検証）③フェッチ優先表の注入は次弾
  （本人の fetch 分布を優先順として _generic_select へ・デッキ固有 JSON 同梱の機構が要る）。
- **教訓**: 「全部見つけてから一括対処」は診断の質が違う——単発の異常値でなく**構造（ループ）**が見える。

## 41. フィールドスカウト — 上位帯の勢力図と「1位の倒し方」（2026-07-17）
- **手法**: `scripts/scout_field.py`（1パスで全チームの行動指紋を集計・`--vs TEAM --result loss` で
  「TEAM を倒した試合の相手側」だけを抽出＝敗因研究モード）。ユーザーの「相手側も分析できるか」への回答。
- **勢力図（897 episode・144チーム）**: TeamA 0.75 が独走。次点は 0.46〜0.60（小標本）。上位常連は
  ①エンジン型（poke19/sup15/ene7・a4066acd 族＝TeamA/TeamG/TeamF 等）②中速加速型（ene10/加速3・
  ee91143a 族＝TeamH/TeamB 旧）③**コントロール/スタール型**（poke12-13・初攻T9・リサイクル
  100%・65c6b47e / 88cc3fe1）の三つ巴。
- **1位の倒し方（TeamA の敗北70試合の勝者分析）**: 大口は ①同型ミラーの高火力勢（TeamG 51勝・
  思考135s＝深読み型）②中速加速型（TeamH 32勝・特性8.0/戦）③**スタール型が印象的**（TeamR 26勝・
  **リサイクル100%・初攻T9**＝エンジンを消耗戦で寝かせて勝つ）。ミラー（TeamE が a4066acd で17勝）も通る。
  旧1位 TeamB は**新デッキ（330bd82d・ene12/加速4）に乗り換えて**2勝＝メタは動き続けている。
- **含意**: ①上位はジャンケン構造＝「絶対王者デッキ」は無い ②我々の alphago 枠（エンジンミラー）は
  スタール型に食われる同じ弱点を持つ見込み ③**スタール型は我々の新装備（リサイクル§39）と相性が良い**
  候補＝次のデッキ実験の有力株（65c6b47e / 88cc3fe1 の eval-deck）。④思考時間は勝敗と単調関係なし
  （3s の TeamH も 135s の TeamG も勝つ）＝方策の質・型の相性が支配的、の再確認。

## 42. replay JSON の OOM 修正と消費済み JSON の自動破棄（2026-07-18）
- **きっかけ**: `make replays` が Error 137（OOM kill）で落ちた。原因は others/ の収穫が増えて
  replay が 1116 件・4.9GB に達し、`analyze_replays.py` が全 JSON を一括 load
  （`episodes=[(pth,_load(pth)) for pth in paths]`）してメモリ 15GB を超えたこと。
- **修正1（OOM）**: 全件配列をやめ paths を回して 1 件ずつ load→処理→破棄するストリーミングへ。
  メモリ常駐は 1 JSON に。実測でピーク RSS 15GB 超→**1.1GB**・4.9GB を 73 秒で完走。
  `--team` 未指定時のチーム推定も同様に軽量パス化（通常運用は `--team` 指定で追加コスト無し）。
- **修正2（自動破棄）**: 生 JSON を必要とする消費者（analyze→`episodes_log.csv`・
  replay-extract→`value_samples.npz`）はいずれも**走査した episode_id を状態ファイルに記録**する。
  よって「episode_id が状態ファイルに載る＝その消費者を通過済み」と判定できる。
  `scripts/prune_replays.py` は必須消費者を全通過した JSON のみ削除（dry-run 既定・`--apply` で実削除・冪等）。
- **破棄条件の設計判断**: 既定は **analyze＋value 両方済**（value サンプルを取りこぼさない安全側。
  nn 学習は凍結中でも将来再開に備える）。ファイル名 stem＝EpisodeId が全件一致することを実測で確認し、
  JSON を開かず stem で判定（無ロード・高速）。
- **自分の試合は温存**: alphago/ismcts/nn は behavior_diff / scout_field（§40/§41）が状態を残さず
  随時再読み込みするため keep-variants として既定で破棄対象外（`--include-own` で含める）。
- **日次フロー**: `make replays-daily`＝analyze→replay-extract→prune --apply（消費後に自動破棄）。
  初回適用で others の 237 件・0.96GB を解放（4.9G→4.0G）。残り others は value 抽出後に自動対象化。
- **教訓**: 「いつ生 JSON がまた要るか分からない」で全 replay を残すと OOM とディスク圧迫を招く。
  消費者が処理済み ID を永続化していれば、破棄可否は状態ファイルの集合演算で機械的に決められる。

## 44. replay-tune の3点比較 — value 焼き直しは退行・operative は評価予算で ISMCTS 超（2026-07-18）
- **実験**: 「凍結 NN を評価に使い続けるのは古い弱い部品では」（ユーザー）→ 安価な凍結解除レバー
  `make replay-tune`（value 頭のみ・policy 不変・実戦 z 187k）を検証。eval_net に `--pilot ismcts` を
  配線し（`make eval-net-ismcts`・torch 遅延 import でホスト実行可）、同一デッキ・同一16メタプール・
  各10試合の**3点比較**: ①operative 平均0.713 ②replay-tuned **0.599**（12/16 相手で劣後） ③ISMCTS
  (tb=0.3) 0.537。
- **判定1（②→ロールバック）**: 検証 MAE は 0.38→0.15 と大幅改善したのに強さは退行。
  **「実戦 z をよく当てる」≠「探索に良い盤面評価」**——サンプルは全チーム（弱い操縦の負け筋含む）の
  状態を含み、結果相関を学ぶほど自分の探索が到達する盤面の評価が歪む（§25 の懸念が実測で確定）。
  この形の replay-tune は closed。pvnet_replay は不採用（既定解決は distill_best > operative のまま）。
- **判定2（③の含意・評価操縦論争の決着）**: 評価に使える現実予算（0.3s/手）では ISMCTS は
  operative より弱い＝**「凍結 NN が評価を歪める」懸念は棄却**。実戦の ismcts>nn は ~400s/試合を
  注ぐ時間スケール依存の強さで、評価予算では出ない。評価は operative 継続・ISMCTS は地面確認用。
- **教訓**: ①value の校正（MAE）を net 採用判断に使わない——判断は必ず対メタ勝率（§25 の再確認）
  ②「弱い部品を使い続けていないか」は議論でなく3点比較で測れば1実験で決着する。
  ③NN 凍結は「測った上での凍結」へ格上げ。解除条件は「ISMCTS 実行を超える具体仮説」のまま。
- **注**: 単一 seed（160試合/行・ペア比較 12/16）。現状維持側の判断なので追試不要、
  もし将来②系を採用したくなったら複数 seed 必須。

## 45. 「誤デプロイ」誤報事件 — replay の行動ペアリングは steps[t+1]（2026-07-19）
- **事件**: v4/v3.5 初日データで「§43 が発火していない・アップロード物が旧 v3」と誤診断し
  再アップロードを要請→ユーザーがファイル名で正しいアップロードを確認→再調査で誤報と判明。
- **真相**: kaggle replay では **obs[t] への応答が steps[t+1].action に記録**される。
  Claude のアドホックプローブが同一 step でペアにした off-by-one が原因。正しいペアリングでは
  **v4 発火 14/14・v3.5 発火 17/17（100%）**（v3 基準 8%）＝§43 は本番で完璧に動作していた。
- **切り分けの副産物（有効だった手順）**: ①tar 内コードに実戦の失敗観測を直接食わせる
  （tar は正しいと証明）②episode 重複0＋セルフマッチ2件（2枠が別エージェントと証明）
  ③期待手が「1 step 後」に見つかり off-by-one が確定。
- **教訓**: ①**リポジトリの既存スクリプト（teacher-extract/scout/behavior_diff）は全て正実装
  だった**——行動ログ解析を書き下ろさず既存実装のペアリングを再利用する ②不可逆な対応
  （再提出要請）の前に、測定系自身の検証（既知の正解でのキャリブレーション）を挟む。
- **§43 初日の実勢（正データ）**: 発火は100%だが**機会が希少**（v4 14/64・v3.5 17/62 試合）。
  律速は「発火率」でなく**札の可用性**（60枚中1枚・サイド落ち~10%・掘りで喪失/埋没）。
  deck-out 試合比は 26%（v3）→15-16% に減少＝§43 は実効。次の可用性レバーはフェッチ優先度。

## 46. レート平衡の打開 — 系統天敵の特定とプールの実メタ同期（2026-07-19）
- **問題提起（ユーザー）**: レートは1日で平衡に達し勝ち負けを繰り返す＝同じ提出物での
  データ収集は非生産的。平衡から抜ける手を打つべき。
  → Elo 平衡の含意: 追加日数は「測定精度」を上げるが「レート」は上げない。上げるのは
  明確に強い提出物への交換のみ。
- **v4 の広レンジ（505-891）の診断**: 「メタられると極端に弱い？」（ユーザー仮説）を
  アーキタイプ別分解で検証。v4/v3.5 は閾値以外同一で、閾値が効く試合は~25%のみ→レート差・
  レンジ差は概ね Elo ランダムウォーク。ただし**系統共通の天敵**が実在:
  **9e3ece3f（エネ厚エンジン・poke17/ene14・出現35件で増加中）に alphago 系 4/18（0.22）**・
  負け筋はデッキ切れ（エネ14 が息切れせず消耗戦で勝つ）。旧 ismcts×a8c57d4b は 5/9 と耐えていた。
- **ローカルと実戦の乖離の正体＝判定プールの盲点**: 9e3ece3f は16デッキプールに不在
  →65c6 の 0.748 も天敵と未対戦の数字だった。対策: `make gauntlet-real` を判定の前に回して
  プールを実メタ頻度で同期し続ける（今回の刷新で 9e3ece3f がプール入り・カバー 2260/3152）。
  ratchet は内側ループで gauntlet を読まない（league.py:338＝速い5メタ）ため**走行中でも
  プール刷新は安全**と確認（運用: gauntlet-real は gate/eval の起動前ならいつでも可）。
- **65c6b47e の天敵チェック**: 保守的な ISMCTS 基準線（--pilot ismcts・ホスト・GPU 非接触）で
  9e3ece3f に 0.85・92985406 に 0.85（各20試合）＝**遅滞はエンジンの天敵も食う**。
  デッキ交換の根拠がプール盲点込みで補強された（確定判断は nn+floor8 の測定構成で追試）。
- **交換時の操縦の選定原則**: 局所測定と同じ構成（operative＋floor8＋盤面補正）で出す。
  クローン事前分布は**教師のデッキ（エンジン）の手筋**なので遅滞デッキにはミスマッチ
  ＝「測ったものを出す・測っていない組合せを出さない」。

## 47. 山札サーチの取得優先度注入 — fetch_priors（2026-07-20）
- **問題**: §43 のリサイクル強制手は発火100%だが機会が希少（v4 14/64 試合）。律速は探索でなく
  **札の可用性**（60枚中1枚・サイド落ち~10%・掘りで喪失/埋没）。行動差分では教師（1位）は
  サーチでキーカード（例: id741）を優先取得、我々はカテゴリ順のみで id305 過剰。
- **決定**: `_generic_select` の山札サーチ順位を「**教師の取得率（fetch_priors）降順 →
  従来カテゴリ順（たね>エネ>その他）**」の2段構えに。priors はデッキ別 JSON
  （`{cardId: 取得率}`）で提出物に同梱し（`build_submission.py --fetch-priors`）、
  未指定なら**完全に従来挙動**（既存 eval・提出物に影響ゼロ）。
- **データ経路**: `scripts/mine_fetch_priorities.py` が教師席のサーチ局面から
  取得率＝取った/提示された を集計。**冪等・永続**（episodes_log と同運用・
  state.json に蓄積→生 JSON 削除可）。ペアリングは steps[i+1] 正実装（§45 教訓の適用）。
  取得率の信頼条件: **提出デッキと同型の教師のみ**（取得率は教師のデッキ構成に依存）。
- **v1 の限界（次段候補）**: 文脈非依存の静的順位。残デッキ帯でペースを変える
  §39 の pacing（mine_subselects の帯別表）は別レバーとして分離。
- **検証計画**: priors あり/なし の local A/B（勝率＋リサイクル発火機会数）→ 実戦。
  データ源: others/ は消費済みのため教師 episode の再 DL が必要（ユーザー）。

## 48. KO 脅威の value 注入 — threat_bonus（2026-07-20）
- **問題提起（ユーザー）**: 操縦が「自分のやりたいことを無理やり通す」＝相手の盤面・狙い
  （進化・エネ付け＝育成）を見て受ける手を選べない。デッキ交換のレート検証は時間が
  かかりすぎ、操縦側の局所測定できるレバーが要る。
- **現状整理**: カード効果の「理解」はエンジンのシミュレーションが担保済み（探索は相手の
  ターンも実際にプレイさせて先読みする）。欠けるのは ①シミュレーション内の相手が我々の
  heuristic で動く ②heuristic 層が相手非依存 ③value が脅威に鈍い、の3点。
  レバー候補: B=脅威項の value 注入（本節）→ A=heuristic の受けの手 → C=相手席 rollout
  への §47 priors 適用、の順で安い方から。
- **決定（B を実装）**: `ko_threat(攻め側, 受け側, meta)`＝受け側アクティブの**残 HP** に
  有効打点（弱点×2）が届くワザを、**エネ充足度で重み付け**（不足0=1.0 / 不足1=育成中0.5 /
  不足2+=0・ベンチ攻撃役は昇格要で×0.5）した最大値。ユーザー提案: 「今打てる」と
  「今から育てる」を区別することで過剰反応と受け遅れの両方を防ぐ。
  注入は `v' = clamp01(v − α×(相手→自分 − 自分→相手))` の対称差（wrap_board_bonus と同型・
  「取られる前に受ける」と「先に取る/詰める」の両方向）。α=0 で完全に従来挙動。
- **近似の割り切り**: 色拘束・ワザ効果・どうぐ補正は見ない（枚数のみ）。正確な合法性は
  探索のシミュレーションが担保＝これは value への事前信号。CardMeta に attack_cost /
  card_attacks を追加（独立フィールド＝特徴量次元不変・§39 と同じ配慮）。
- **検証計画**: eval-net の `--threat-bonus` α スイープ（board_bonus α=0.2 決定と同一手順・
  40試合×数α）。ISMCTS には効かない（evaluator 非使用）＝nn 操縦専用。

## 49. 山札サーチの cardId 誤読バグ — §31 と同型の再発（2026-07-21）
- **発見経緯**: §47 fetch_priors のマイニングを TeamA（a4066acd・5episode）で実行したところ
  「対象札1種・id0」という不自然な出力→ replay 生データを実測ダンプして原因を特定。
- **真相**: 山札検索の `select.option` は **cardId を持たない**（実測: `{area, index,
  playerIndex, type}` のみ）。実カードは **`sel.deck[option.index].id`** で解決する必要がある。
  旧実装（`agents.py::_generic_select` の山札サーチ分岐・§31 由来）は `option.cardId or 0` を
  直読みしており、**常に 0（未解決）に落ちて「たね優先→エネ優先」のカテゴリ分岐が機能して
  いなかった**。ライブ対局も replay も同一のエンジン生 JSON スキーマ（cg ネイティブ出力を
  Kaggle がそのまま記録）のため、**本番の両提出枠（ismcts/nn）で実際に発火していなかった**。
- **§31 との関係**: §31 で捕獲した「昇格先ランクが `inPlayArea/inPlayIndex` を誤読し常に
  no-op だった」バグと**同型**（フィールド仕様の思い込み・実データ未確認）。§31 の教訓
  「テストが通ると本番の形のデータで動くは別・実データをダンプして確認する」が、**このとき
  山札サーチ分岐には適用されていなかった**（既存テストに当分岐のケースが無かった）。
- **修正**: `_generic_select`（agents.py）と `mine_fetch_priorities._count_search`
  の両方を `sel.deck[option.index].id` 解決に統一。fetch_priors（§47）の priors 参照も
  同じ解決を通るため合わせて直る。回帰テスト（実データ形状の SimpleNamespace）を追加。
- **影響の見立て**: 「たね優先はベンチ切れ（実戦敗因6〜7割）への直接対策」（§31 の主張）が
  実は無効化されていた可能性が高い＝**この修正は単なるバグ潰しでなく実戦強度に効きうる**。
  ただし take 数自体（maxCount まで取る）は変わらないため、影響は「何を取るか」の質のみ。
  次の a/b や次回提出で効果を確認する。

## 50. 山札サーチの役割別優先度マイニング — 教師プールの再検証（2026-07-21）
- **問題提起（ユーザー）**: §47/§49 で cardId 完全一致に釣られ、**自チームの対戦ログ
  （マッチメイキング相手＝実力の裏付けなし）を「教師」として使ってしまう誤り**が判明
  （65c6b47e 完全一致の TeamL/TeamK は実は自分の過去対局の相手だった）。
  cardId 単位は完全一致デッキが必要で非現実的・デッキタイプ自体が優先度と無関係な可能性も。
- **決定**: cardId でなく**役割カテゴリ**（たね/進化ポケモン・基本/特殊エネ・
  トレーナー(draw/search)・トレーナー(energy_accel)・山札リサイクル札・その他トレーナー・
  どうぐ・スタジアム）で集計する `mine_search_role_priors.py` を新設。デッキが違っても
  比較できるため、**教師プールは意図的に DL した上位帯（`data/replays/others/`）全員を
  プール**（デッキタイプ非依存で母数を稼げる）。`mine_fetch_priorities.py` の既定 `--dir`
  も `data/replays/others` に変更（自チーム対戦ログを教師として使う誤りの再発防止）。
- **実測（局面数1262・36チーム）**: overall では basic_energy が取得率0.56で突出したが、
  これは TO_HAND（手札に加える＝一般的な「サーチ」）以外のコンテキスト（TO_BENCH/ATTACH_TO
  ＝仕組み上ポケモン/エネしか出せない）混入によるアーティファクトと判明。TO_HAND に絞ると
  basic_energy と basic_pokemon/evolution_pokemon はほぼ拮抗（0.18〜0.26）。

## 51. サーチ効果内容×サポート使用の分離・候補数正規化・捨て札コスト分析（2026-07-21）
- **問題提起（ユーザー）**: §50 の overall 集計は「サーチ効果の種類」と「サポート使用有無」の
  2要因が混ざっている。分離すべき。また「山札からなんでも」の判定が甘いのではないか、
  型混在局面は候補数で正規化すべき、サーチのコストとして捨てる際の優先度も見るべき。
- **効果内容の分離**: 当初案（`option数==deck数`＝制限なし）は**実データで0件**（TO_HAND
  953件中でも0件）と判明。代わりに `sel.context`（エンジン公式フィールド）で分類
  （TO_HAND=75%・TO_BENCH=14%・ATTACH_TO=10%）。TO_HAND 内でも1局面の card_type が
  単一(85%)か複数混在(15%)かでさらに分割（複数混在＝「トレーナー全般」等の広いサーチの
  代理指標）。サポート使用有無は `State.supporterPlayed`（エンジン公式フラグ・自前の
  ターン追跡不要）で判定。2×2＝4バケット（tohand_{single,mixed}_{before,after}）。
  `tohand_mixed_before` は実測局面数11と極小（統計的に無意味・要追加データ）。
- **候補数正規化＝選好指数 lift**: 型混在局面は1局面の提示数が役割ごとに大きく偏るため、
  素の取得率は「候補が多いと薄まる」効果と混ざる。一様選択の帰無仮説での期待取得数
  `k×(offered_r/N)` に対する実際の比（lift）を計算。**tohand_mixed_after（局面数136・
  頑健）で実証**: trainer_drawsearch/trainer_other/basic_energy/special_energy は
  lift≈1.0（候補数なり＝優先度差はほぼ幻だった）、**basic_pokemon だけ lift=0.46で
  明確に忌避**——素の取得率の低さは「トレーナー系が候補数で水増しされていただけ」で、
  真の差は「たねポケモンが避けられる」ことだった。
- **捨て札コスト（ユーザー提案）**: `sel.effect`（発動中カードの Card・エンジン公式フィールド）
  の cardId で、DISCARD（context=8）選択が「サーチ効果のコスト」かを判定（discard→search が
  同一 effect.id で連続することを実測確認）。DISCARD は `sel.deck` でなく手札由来
  （`AreaType.HAND`・実測確認）なので `me.hand[option.index].id` で解決。
  discard_all（局面数59）: trainer_other が最も温存（廃棄率0.38）・stadium/basic_pokemon/
  basic_energy は廃棄されやすい。discard_search_cost（局面数10・参考程度）も同傾向。

## 52. `_generic_select` のたね優先を撤回 — エネ優先のみに（2026-07-21）
- **決定**: 山札サーチのカテゴリ順を「たね > エネ > その他」から**「エネ > その他
  （たねポケモン含む）」**に変更（`agents.py::_generic_select`）。§51 の lift 実測
  （局面数136・型混在局面で basic_pokemon の選好指数=0.46＝明確に忌避、トレーナー各種は
  lift≈1.0＝優先度差なし）に基づく。エネの上位維持は lift が否定していない（≈1.0で
  中立〜やや優先）ため据え置き。
- **影響範囲**: たね/エネ/トレーナーが同一サーチ局面で競合する場合のみ（TO_BENCH/ATTACH_TO
  は構造的にポケモン/エネ限定でトレーナーと競合しないため無風・実質 TO_HAND の型混在局面が
  対象）。fetch_priors（§47）は変わらず最優先（tier0）。
- **限界・要検証**: 根拠は TO_HAND の型混在局面（トレーナー全般サーチにポケモンが稀に
  混ざるケースが主）に偏っており、他文脈への一般化は未検証。次の a/b や次回提出で
  デッキ切れ率・ベンチ切れ率（§31 の当初の狙い）への影響を確認する。

## 53. threat_bonus α スイープ — α=0 対照実測と「単発レバー判断への保留」（2026-07-21）
- **α=0 対照実測**（`pvnet_operative.pt+board0.2+floor8`・40試合×16デッキ・§48 と同一
  プロトコル）: 最悪 **0.225**・平均 **0.399**。§48 冒頭に記録した α=0.1 の初回実測
  （最悪 0.150・平均 0.386）と同一条件で揃えると、**α=0.1 は α=0 より最悪・平均とも悪化**。
  「gate 実測（0.375/0.641）より悪化」という当初の懸念はプロトコル差（gate は別デッキ・別条件）
  ではなく、同条件対照でも threat_bonus 0.1 が下振れすると判明。
- **判断保留（ユーザー指摘）**: この branch には §49（cardId 誤読修正）・§52（たね優先撤回）
  など複数の変更が積んである状態で今回の対照を取っている。**単発レバー（threat_bonus）の
  α 単独スイープだけで採否を決めず、蓄積した変更が組み合わさった時の効果も別途見る**方針。
  α=0.2/0.3 の追加スイープと合わせて、fetch_priors（§47・データ待ち）が揃った段階で
  「§52 + threat_bonus 決定値 + fetch_priors」の合成構成を単体変更の対照と別に評価する。
- **現状**: threat_bonus は不採用と断定せず、α=0.2/0.3 のデータ収集を継続（ユーザー実行）。

## 54. 現提出物（65c6b47e）1日目 replay 分析 — variant 別集計の落とし穴と prune の副作用（2026-07-21）
- **variant フォルダ名（ismcts/nn）は操縦の識別にしかならず、デッキの識別にはならない**:
  `analyze_replays.py` の単純集計（フォルダ名別）は、`ismcts/` に旧旗艦
  （デッキ a8c57d4b・110件）と現提出物（65c6b47e・58件）が混在した状態で平均されていた。
  raw JSON から自チームのデッキ hash を再計算し、65c6b47e のみに絞って再集計する必要があった。
- **65c6b47e × 操縦別・実戦成績（1日目・重複なし）**: ismcts 28/57（0.491）・nn 26/49（0.531）。
  ローカル測定（§44: operative 0.713 > ISMCTS 0.537）ほどの差は実戦に出ていない＝§41
  「型が決め、操縦は実行度」に整合的だが、n=49-57 では有意差なしの範囲。デッキ切れは
  両枠とも低水準（ismcts 3/57・nn 1/49）＝遅滞デッキの狙い通り。
- **天敵 9e3ece3f との遭遇**: nn 0/4・ismcts は遭遇なし。ローカルの天敵チェック
  （nn=0.65 vs ismcts=0.85・§48 直前の実測）と逆方向だが n=4 でノイズ域＝要継続監視。
- **gauntlet-real 実行**: 実メタ 544 デッキ（新規収穫分含む）から16件選抜・全遭遇3374回中
  2370回をカバー。判定プールを最新実メタへ同期。
- **prune の副作用（要注意）**: `make replays-daily` の `prune_replays.py --apply` は
  「必須消費者」を `analyze`/`value` のみで判定し、`mine_fetch_priorities.py`（§47・cardId
  単位マイニング）を消費者として追跡していない。今回 `others/`（58 episode）がこの2つの
  消費のみで破棄対象となり削除された。§50/51 の役割別優先度（`data/fetch_priors/
  role_priors.json`）は削除前にマイニング済み・永続化済みで無事だが、**§47 の cardId 単位
  マイニングはこの58 episode に対して一度も実行されないまま失われた**。§47 をさらに
  進めるには others/ の再 DL が必要（ユーザー・Kaggle リーダーボードから）。
- **再発防止（実施済み・同日）**: `prune_replays.py` の既定 `--keep-variants` に `others` を追加
  （`alphago,ismcts,nn` → `alphago,ismcts,nn,others`）。自チームの試合を `others/` に置くことは
  ない前提（ユーザー確認）のため、`analyze`/`value` の消費だけでは破棄されず独立ライフサイクル
  として温存される。破棄したい場合は明示的に `--include-own`。`tests/test_prune.py` を新挙動に
  更新（デフォルトで others 温存／`--include-own` で consumed 分のみ破棄）。

## 55. fetch_priors（§47）初回マイニング — 提出デッキ一致教師が2チーム出現（2026-07-21）
- **追加 DL 分（others/ 241 episode・新チーム）を `mine_fetch_priorities.py` で初回マイニング**
  （state.json は今回新規作成・482席＝241episode×2席を新規集計）。78 team×deck 組を出力。
- **提出デッキ（65c6b47e）に完全一致する教師が2チーム見つかった**:
  - TeamJ（3 episode・11局面）: id344 取得率0.67（8/12）・id18 0.43（3/7）・
    id11/id14 は0（提示8/7回だが取得0）。id345 は提示4回で min-offered=5 未満のため priors 対象外。
  - TeamK（1 episode・5局面）: id345 取得率0.50（3/6）。id344 は提示2回で対象外。
  - 2チームは相補的（片方が薄い札をもう片方が補う形）だが、`mine_fetch_priorities.py` は
    team×deck 単位で別ファイル出力＝**チーム間の合算機能はまだ無い**（設計は「デッキが近ければ
    別デッキ由来でも部分採用可」という単一ファイル選択が前提）。
  - 依然として各チーム単体では局面数が薄い（§47 の「まだ少ない」は継続）。次提出で使うなら
    TeamJ側（episode数・局面数とも優位）を採用候補とする。
- **次の一手**: 次提出（§48 決着後）で `build_submission.py --fetch-priors
  data/fetch_priors/___________65c6b47e.json` を試すか、他デッキ由来を含めた部分採用も検討。
  さらに others/ の追加 DL で 65c6b47e 一致教師を厚くできれば理想。

## 56. threat_bonus α スイープ — ローカル判定と実戦の乖離を受けて実戦A/Bへ方針転換（2026-07-21）
- **α=0.3 実測**（同一プロトコル・pvnet_operative.pt+board0.2+floor8・40試合×16デッキ）:
  最悪 **0.200**・平均 **0.389**。
- **3点比較**: α=0（最悪0.225/平均0.399）> α=0.3（0.200/0.389）> α=0.1（0.150/0.386）。
  α に対してきれいな単調性は無いが、非ゼロの α はいずれもローカル判定でベースライン（α=0）を
  最悪・平均両方で下回った。
- **方針転換（ユーザー指摘）**: ローカル判定（16デッキ gauntlet）と実戦のスコアが乖離して見える
  （§54: ローカルは operative≫ISMCTS のはずが実戦は ismcts 0.491≒nn 0.531 で拮抗・天敵チェック
  もローカル nn=0.65 vs 実戦 0/4 で逆方向）。ローカル判定のスコア方向は正しい（勝率・高いほど
  良い・`eval_net.py` で確認済み）が、**このプールでの相対順位が実戦の相対順位と一致する保証が
  弱い**。ローカル a/b だけで不採用を断定せず、**実際に提出物へ組み込んで1日分の実戦データで
  判断する**方針に転換。
- **実戦テスト構成**（α=0.1・ユーザー選択。α=0.3よりローカルではやや劣るが最初に試した値で
  比較のベースラインが既にある）: nn 枠を `models/submission_stall_v2.tar.gz`
  （65c6b47e×nn+floor8+board0.2+threat_bonus0.1、**fetch_priors は非同梱**＝信号を分離）に
  差し替え。ismcts 枠は `models/submission_stall_ismcts_v2.tar.gz`（fetch_priors のみ同梱・
  TeamJ分）で別途検証（§47・§55）。1日経過後に §54 と同じ手法（自チームデッキ
  hash 再計算で variant 混在を回避）で再分析し、実戦での threat_bonus 効果を判断する。
- **構造的な原因の特定**: `src/evaluation.py::eval_deck_vs_meta`（eval-net/gate/ratchet 共通の
  判定基盤）は相手側デッキも**自分と同じ pilot（同じ評価関数・同じ threat_bonus 等）で操縦**
  している＝ローカル判定は「自分の現行実装どうしのミラー戦（デッキだけ違う）」であり、
  実際の他チーム Bot（挙動が全く異なる）との対戦を測っていない。相手の意図を読む機能
  （threat_bonus 等）ほどこの歪みの影響を受けやすいと考えられる。
- **保留中の論点（ユーザー）**: eval-net 的なローカルスコア判定を「動作確認のみ」に格下げし
  実戦データ駆動に一本化するかどうか。範囲は①nn ハイパラ（board_bonus/threat_bonus/floor-
  rollouts 等）のみか、②gate/ratchet（デッキ探索の採否判定）まで含めるか。**現時点では判断
  を保留**（ユーザー: 「まだ早い」）。今回の提出（v2・threat_bonus α=0.1）の1日経過後、
  実戦 replay データと合わせて再検討する。

## 57. v2 提出（fetch_priors×ismcts／threat_bonus×nn）1日経過の実戦結果（2026-07-22）
- **手法**: §54 と同じ（自チームデッキ hash 再計算で 65c6b47e に限定）に加え、episode_id を
  v1 期間の最大値（ismcts 87156407・nn 87158094・§54 時点のダウンロード上限）で分割し、
  「旧（v1・threat_bonus/fetch_priors 無し）」と「新（v2・cutoff 以降）」を比較。
- **Kaggle レート**（ユーザー報告）: ismcts 現692（最低446-最高707・上限に接近）・
  nn 現556（最低489-最高694・中位）。
- **ismcts × fetch_priors（TeamJ分）**: 0.491（28/57）→ **0.567（34/60）**
  （Δ+0.076）。**ベンチ切れ負けが 8/57(14%) → 2/60(3.3%) と明確に減少**（fetch_priors の
  当初の狙い＝§31/§47 と一致）。デッキ切れ負けも 3→2 で微減。天敵 9e3ece3f には新期間 0/3
  （n少）。レート692（上限707に近い）と整合的。
- **nn × threat_bonus α=0.1**: 0.531（26/49）→ **0.489（23/47）**（Δ-0.041・改善なし）。
  デッキ切れ・ベンチ切れとも横ばい、負けの大半は従来通りサイドレース負け。新期間で天敵との
  遭遇なし。レート556（中位）とも整合的（勝率5割前後＝レートの強いドリフトが無い状態）。
  **ローカル判定（α=0.1が3点中最悪）と実戦が今回は一致**——§56 で懸念した「ローカルが実戦の
  改善を過小評価する」パターンはこの回では確認されなかった（乖離があるのはむしろ pilot 間
  比較＝§54 の ismcts/nn 拮抗のほう）。
- **注意（交絡）**: 新旧は日をまたいだ比較（メタの自然変動を分離できていない）であり、
  同日 A/B ではない。n=47-60 は ±1標準誤差が ±6-7pt 程度＝ismcts の改善・nn の悪化とも
  単独では統計的に確定的ではないが、ismcts はベンチ切れ率という**機構レベルの裏付け**があり
  相対的に信頼度が高い。
- **判断**: fetch_priors（ismcts 枠）は据え置き継続。threat_bonus は α=0 に戻す方向で
  ユーザーと相談（次回提出で反映）。§56 の「ローカル判定を動作確認のみに格下げするか」は
  この回のデータでは決定打が出なかった＝pilot 間比較の乖離（§54）が主論点として残る。

## 58. 上位帯スナップショット比較 — 王者は2〜3日で回転・差は操縦の型（2026-07-22）
- **手法**: 現2位 TeamD 中心の新規 DL 374ep（現在の1000+帯）と TeamF 期 241ep（2日前）を
  mtime で分割し scout_field を両方に適用（スナップショット比較）。ユーザーの
  「2〜3日で1位が回る」仮説の検証。
- **回転は確定**: 王者系譜 a4066acd（TeamA）→ f0c14d49（TeamF）→ 4041254f（TeamE/TeamD）。
  **退位した王者は暴落する**（f0c14d49 0.65→0.20・a4066acd 0.75→0.11）＝「今日の王者クローン」
  は常に1周遅れで到着する負けレース。デッキ選定基準は「常在性×対王者相性×教師データ厚」へ。
- **常在デッキ**: 965525fe（TeamC・0.75→0.63・**王者連続撃破 18-0/21-0・帯内唯一の探索型
  76〜140s＝我々と同アーキ**）・ee91143a（0.54→0.60）。乗り換え本命=965525fe・対抗=4041254f
  （教師 TeamD314+TeamE70+TeamI30ep）。
- **操縦の帯共通項（デッキ・アーキタイプ不問）**: ①特性 7.9回/戦（33チーム中31が5回以上・
  我々1.8）②**ベンチへのエネ付与 4.3〜5.0回/戦（我々1.2・アクティブに8割集中）**
  ③敗時サイド2〜4（我々0〜1＝ワンサイド負け）。**差でないもの**: 思考時間（2s〜458sに勝者）・
  パス頻度（型依存: TeamC0.4 vs 遅滞型TeamQ4.6）・初攻T（型依存T3〜9）・サポ使用数。
- **機構仮説**: アクティブKO＝全エネ喪失→後続不在のテンポ崩壊が敗時サイド0〜1の正体。
  §40（特性解放）が実戦で 1.8 に留まるのは、修正が rollout 方策止まりで探索の価値信号が
  展開行動を評価しないため（盤面投資の見返りは rollout 地平線の外）。

## 59. attach_priors — エネ付与先の帯実測注入（§58 の実装・2026-07-22）
- **決定**: `mine_attach_policy.py` で **P(ベンチ付与 | アクティブの装着エネ枚数)** を帯実測から
  採掘（active/bench 両方が合法だった MAIN 決定のみ対象＝可用性交絡を除去）し、heuristic の
  エネ付与先を確率切替に変更。ベンチ内の付与先は「攻撃準備に最短の子」（最小不足エネ→装着数
  →HP）で一点集中。fetch_priors と同機構で全系統（heuristic/ismcts/nn/提出）に配線・
  未指定なら挙動不変。
- **採掘値（615ep・148チーム）**: エネ0枚:0.49 / 1枚:0.57 / 2枚:0.79 / 3+:0.77。
  勝ち組10チーム限定でも 0.53/0.48/0.82/0.76 とほぼ同一＝シグナル頑健（帯全体版を採用）。
  教師別も単調（TeamD/TeamE は2枚で ~100% ベンチ行き）。
- **検証**: 55テスト系 green＋E2E 実エンジン: ベンチ付与 **0→4.9回/戦**（帯実測4.3〜5.0と一致）。
  heuristic ミラー40戦×2デッキ: **0.72（65c6b47e）/ 0.67（4041254f）**。初回n=16の6勝10敗は
  小標本ノイズ（n=80合算で0.70）。
- **却下した代替案**: 条件付けを「最安ワザの不足エネ」にする（採掘と注入の対応が崩れる・
  枚数条件で教師別単調性が既に成立）・winners-only 採掘（差なし・nが小さいだけ）。
- **次**: ローカルミラーは §56 の歪み（自分同士の対戦）を持つため、採否は実戦 A/B で判断。
  nn 枠（現最古）の次回差し替えに同梱するのが §34 手筋と整合。

## 60. エンジン回転の診断と end_margin — 打ち切り早畳みの抑制（2026-07-22）
- **診断（§58 仮説の修正）**: 「提示された回数 vs 選んだ回数」を実戦 replay で採択率分析。
  ①特性の採択率は上位帯と同水準（我々38-40% vs TeamD52%/TeamC25%）＝「探索が特性を蹴る」
  仮説は**誤り**。②真の差は (a)**提示回数が1/4〜1/6**（特性4.8/戦 vs 18〜32）＝盤面が
  育たない上流問題（poke12 の遅滞デッキ性質が大・操縦調整では埋まらない→デッキ乗り換えの領分）、
  (b)**打ち切り手（攻撃36%・終了14%）を上位帯（16-17%・5%）の2〜3倍の率で採択**＝ターンを
  早畳みして行動機会が複利で消える。
- **裏取り（一致率分析・直近60ep）**: heuristic との MAIN 一致率67%。上書きの主体は同クラス内の
  対象変え（エネ付与→別エネ付与158件等）で無害だが、**終了への上書き1.4回/戦・攻撃へ0.9回/戦**
  ＝打ち切りの約1/3が探索由来。heuristic のたね展開案の上書き（採択25% vs 上位帯40-50%）も確認。
- **決定**: `end_margin`——アンカー逸脱ルール（§19）に非対称マージンを追加。heuristic が
  END 以外を提案しているのに探索が END へ逸脱する時だけ大きいマージン（例0.15・
  `--end-margin`）を要求。END は唯一「何も生まない」手＝展開後に終了しても失うものが
  ほぼ無い。heuristic 自身の END 提案（選択肢が尽きた正当な終了）には不適用・未指定は挙動不変。
- **却下した代替案**: 攻撃への逸脱も対象（リーサル/サイドレースの正しい即断を潰すリスク）・
  展開手への progressive bias 注入（設計が重い・まず1行レバーで実戦検証）。
- **検証**: 単体6テスト＋engine 疎通 green。ローカル対戦での効果測定は低予算では逸脱自体が
  起きず無意味（min_visits 未達）・高予算はホストで非現実的＝§56 の方針通り**採否は実戦 A/B**。
  注意: end_margin は ismcts 専用で、現 ismcts 枠は最新側スロット＝単発差し替え不可。
  投入はデッキ乗り換え（§58）と同じタイミングか次の2連アップロードになる見込み。

## 61. eval-deck のローカル判定が実戦と逆符号 — デッキ選定での格下げ確定（2026-07-24）
- **観測**: 実メタ較正済み gauntlet（663件から頻度選抜16デッキ）で eval-deck 3本
  （nn+floor8・各40試合）: 65c6b47e 平均0.708 / 965525fe 0.537 / 4041254f 0.440＝
  現デッキが最高値。しかし**対カウンター相性が実戦と正反対**: vs 9e3ece3f ローカル0.85↔実戦0.36・
  vs 04621784 0.75↔0.36・vs a4066acd 0.73↔0.00。一方 **vs a8c57d4b（我々の旧チャンピオン
  デッキ）だけは 0.30↔0.30 で完全一致**。
- **機構の確定（§56 の実証）**: eval_deck_vs_meta は相手側デッキも自分の pilot で操縦する＝
  「他人の操縦に依存するカウンターデッキ」を相手に置くと本来の脅威を再現できず過小評価。
  自分の操縦が扱えるデッキ（旧自デッキ）相手の時だけ正確。結果として**現デッキ（共適応済み）の
  スコアが構造的に膨らむ**＝乗り換え判断には一方向に偏った物差し。
- **決定**: デッキ選定について、ローカル eval-deck を「動作確認・大破綻検知」へ格下げ（§56 の
  保留論点に決着）。デッキ乗り換えの判断は**実戦帯の証拠**（上位帯での常在性・対王者相性・
  実戦A/B）で行う。頻度加重平均の併記（STATUS 次の一手5.）も本症状（符号逆転）は救えない
  ことを確認（加重後も 65c6b47e 0.727 で首位のまま）。
- **含意**: 「候補が基準線を上回ったら乗り換え」という事前ルールは片側検定としても機能しない
  （基準線側が膨張するため）。今後のデッキ実験は提出枠での1日 A/B が一次判定。

## 62. デッキA/B 15時間打ち切り — 965525fe 移植は不成立・attach_priors は機構作動/成果未達（2026-07-25）
- **観測**: 枠A（965525fe_nn_v1）17/43＝0.395・レート506（457〜600）／枠B（stall_v4・
  65c6b47e＋attach_priors）22/45＝0.489・レート559（381〜600）。両枠とも v3nn 基準（656）を
  下回り、ユーザーが15時間で打ち切り。健全性は確認済み: 自枠同士の対戦 0・枠A の自デッキ
  hash 再計算＝965525fe（配線正常）・overage 最小 433s（時間切れなし）。
- **デッキA/B の判定（枠A vs 枠B）**: カウンター群 {9e3ece3f,04621784,a4066acd,a8c57d4b} の
  遭遇率は枠A わずか **5%**（枠B 13%・v3期は約1/3）と最大の追い風だったのに 0.395 で最下位。
  負けは特定天敵でなく広い field に分散（その他21/26・時系列でも改善傾向なし 前半0.43→後半0.36）。
  「デッキが制約」仮説なら A≫B が予測されるところ**逆**＝TeamC の帯（1000+）で常在の
  王者キラーも、我々の操縦では field にすら勝てない。**§61（eval-deck の共適応バイアス）と対**:
  デッキは単体で移植できず、操縦との共適応込みで初めて強さが出る。**965525fe 乗り換えは棄却**。
- **attach_priors 単独A/B（枠B vs v3nn・§59 の判定）**: 配線は完全作動——P(ベンチ)
  0.06→0.56・ベンチ付与 0.4→3.8回/戦・バケット別も採掘 priors {0.49/0.57/0.79/0.77} に追随。
  **ベンチ切れ負けは 33%→9%（負け33中11→23中2）と激減＝近因は修正**。しかし敗時サイド
  0〜1 のワンサイド負け率は **78%→78% で不変**＝「アクティブ全積みがワンサイド負けの主因」
  仮説は成果面で反証（エネを散らしてもベンチが生き残るだけで、勝ち筋には変換されない）。
  勝率 0.489 は v3nn の 0.441 より名目上界だが帯が下（559<656・15hで一度も600超えなし
  ↔ v3ペアは1日で751/825到達）＝強さは**中立〜やや負圧**。害の証拠もなし。
- **決定/含意**: ① 965525fe は棄却・デッキ候補の評価軸に「操縦適合」（自分の pilot で
  扱える形か）を追加——探索型デッキは特に移植リスクが高い。② attach_priors はベンチ切れ
  削減の実利があるため即撤回はしない（維持/撤回は次の提出構成の設計時に判断・撤回するなら
  明示比較後）。③ 65c6b47e 続投なら v3nn（fetch のみ）が現時点の最良既知構成。

## 63. 棄却済みレバーのデッキ交絡棚卸し — 全レバーはコード温存・再試条件を明記（2026-07-25）
- **背景（ユーザー提起）**: 「悪化で打ち切ったレバーは、デッキとの相性が悪かっただけでは」。
  棚卸しの結果、**全レバーが build_submission.py の旗として温存**（既定 off か係数0）・
  過去提出 tar も全て models/ に保存＝任意の過去構成を再現可能と確認。
- **デッキ交絡が残る棄却判定**: ① **threat_bonus（§48/§53/§57）**——65c6b47e（遅滞）でしか
  測っていない。遅滞は KO を急がない戦略＝KO 脅威の価値上乗せが操縦と喧嘩する説明が立つ。
  **攻撃型デッキ乗り換え時に再試**。② **recycle（§43）/leaf_rollouts（§38）**——悪化棄却でなく
  alphago デッキ退役で舞台ごと消えた（recycle は発火100%実証済み・該当札を積むデッキで即再利用可）。
  ③ **floor0 vs floor8**——未決着のまま現構成 floor8（クローン計測由来の疑いのみ）。
- **運用**: 今後レバーを棄却する時は「どのデッキ・どの構成で測ったか」を判定に併記する
  （§62 のデッキ側と対で、レバー判定は構成条件付きの主張として扱う）。

## 64. 型対抗戦 24h 判定 — F2 撤収・F4 は基準線未達も相性構造の改善を実証（2026-07-27）
- **結果**: 018cecb2（F2）レート434・0.395＝**撤収確定**（帯不適合: 下位帯に多い
  a986f125 型に 5/11・上位帯実測 0.59 が我々の帯では再現せず＝ユーザーが提起した
  「上位帯で強い≠今の帯で強い」の実証例）。4041254f（F4）レート569・0.553
  （後半0.42）＝**65c6 基準線 651 に約80pt 未達**。
- **ただし F4 は質的な収穫が2つ**: ①旧カウンター 04621784 に 2/3（65c6 は 2/12）＝
  デッキ乗り換えで相性の壁が消えることを実証 ②同型ミラー 4/6＝我々の操縦
  （ismcts+end_margin+priors）は同じデッキを使う他チームに競争力がある（§62 の
  「移植失敗」とは別物）。fetch 教師 6ep という明確な薄さも残っている。
- **教訓（§61/§62 系列の完結）**: 型行列（Layer 1）は**相性構造の予測には有効**
  （04621784 への勝ち・ミラー競争力は予測通り）だが、**帯全体のレート予測には不十分**
  ——帯の遭遇分布（下位帯の雑多メタ）が上位帯と別物のため。デッキ選定は
  「型行列×帯の遭遇分布の加重」で読むべき（帯加重スコアリング構想）。
- **選択肢**（ユーザー判断待ち）: A) 両枠 65c6 復旧（651 回収・カウンター天井も回収）
  B) F4 続投で改善（fetch 増強・dev_margin）C) ハイブリッド（枠1=65c6 復旧・
  枠2=F4 改善版）。

## 65. 操縦 A/B on 4041254f — ismcts 再勝利・nn 系レバー凍結継続／勝率は指標として不適と確定（2026-07-28）
- **結果（レート）**: ismcts_v2 **557**（454〜735）vs nn_v1 **430**（330〜682）＝**+127 で
  ismcts 勝ち**。65c6 での +100（§57 期）と同符号・同規模＝**操縦の序列はデッキに依らない**。
  → nn 専用レバー（threat_bonus 再試・floor0/8・recycle・§63）は**解禁条件を満たさず凍結継続**。
  次のレバーは ismcts 系の dev_margin。
- **勝率は逆符号**（nn 0.462 > ismcts 0.382）だが**これは指標の誤り**: レートが低い側は弱い
  相手と当たるため、生勝率は相手強度と交絡する。決定的証拠として、**ismcts_v1 と v2 は
  opp_decks 以外バイト同一のビルド**なのに勝率 0.549→0.382 と大きく振れ、**レートは
  569→557 とほぼ一致**。→ **1日〜50戦の勝率は判定に使わない。レートを一次指標とし、
  勝率は敗因・相性の内訳を読むための補助に格下げ**（§61/§62 の判定手順を更新）。
- **副次的確定①（デッキ）**: F4（4041254f）×ismcts は独立2回の実測が 569/557 と収束＝
  真値 ~565。65c6b47e の 651 に**約85pt 劣後**が確定（§64 の暫定判定を追認）。
- **副次的確定②（nn の効率論拠の反証）**: 同一窓で nn は 1試合 108s 消費に対し ismcts は
  539s（5倍）だが、**消化試合数は nn 39 < ismcts 55**。Kaggle の試合割当は思考時間に
  比例しない＝「nn は時計1/10なのでデータが多く貯まる」という §54 以来の実利論拠は**不成立**。
  nn を採る理由は「強さで勝つ」以外に残っていない。
- **配線の事後検証**（ホストに torch 無しで動的プローブ不可だった件）: 実戦 replay から
  P(ベンチ付与) nn 0.46 / ismcts 0.67（素の 0.06 比で明確に発火）・特性採択 4.6/4.8回/戦・
  初攻撃T中央 8/6 を実測＝両枠とも同梱物は設計通り作動していた。
- **運用事故と復旧**: ユーザーのコピー操作で 4041254f の replay 14件が
  `data/replays/965525fe_nn/` へ誤配置され、うち4件はそこにしか無かった（10件は重複）。
  削除依頼だったが**唯一の4件は 4041254f/ へ退避してから重複のみ削除**し、
  episodes_log.csv の誤ラベル4行を訂正（.bak 保存）。965525fe は §62 当時の 17/43 に復元。
  教訓: 破棄指示でも中身を確認してから実行する（§54 の prune 事故と同型の回避）。

## 66. 終盤戦略の転換 — 思考時間短縮の棄却・小レバー検証の打ち切り・床の回収（2026-07-28）
- **問い（ユーザー）**: 残り13〜20日。検証より「上位帯に倣って思考時間を短くし対戦数を稼ぐ」
  べきか。
- **棄却（実測による反証）**: §65 の操縦 A/B が同一期間・同一デッキの対照実験になっていた——
  nn は 1試合 **53s** で **39戦**、ismcts は **428s**（8倍）で **55戦**＝**速い方が29%少ない**。
  過去7提出でも「試合数/idレンジ」は ismcts 23.8〜30.4・nn 16.9〜40.9 で速度と無相関。
  Kaggle の試合割当は提出物単位のスケジューラ律速で、こちらが速く終えても供給は増えない。
  時間切れリスクも不在（残クロック最小 60s＝設計マージン `game_budget=540/600` 通り・
  クロックを使い切った92戦の勝率0.54）。**短縮は探索の質を削るだけで見返りゼロ**。
- **ただし直感の核は正しい**: TeamB は 2.4s/戦で 1000+。重い探索は上位帯との差の正体
  ではない（§41/§46 の再確認）。我々の540秒はその差を埋める買い物になっていない。
- **本当の制約は時間ではなく効果量**: 13〜20日 ÷ 1実験1日 ＝ まだ10〜17実験できる。問題は
  **この1週間の変更がほぼ全て中立か悪化**だったこと（65c6×ismcts+fetch **692** →
  +attach+end_margin 651／デッキ3種 506・565・434／nn 操縦 430〜559）。加えて
  §65 で**レバーの効果量（±10〜20）が測定ノイズ（±25）以下**と判明＝小レバーの単独 A/B は
  原理的に検出不能。
- **決定**: ①**dev_margin / end_margin 単独 / attach 単独の A/B は実施しない**（前日の推奨を
  撤回）②**床の即時回収を最優先**（当時の両枠 557・430 は基準線 651 を下回り、放置が毎日
  順位を損なっていた）③残りの枠容量は効果量±100級の賭けにのみ使う ④最終2〜3日は
  勝った構成を両枠に置いて平衡させる。
- **実装**: 上記を1手で満たす2連＝ v5a（fetch のみ・692再現）× v5b（full・651再現）。
  両枠とも最良候補で床を回収しつつ、「692 と 651 のどちらが本物か」に同時対照で答える。
  同じ構成を2つ出すより厳密に優れる。

## 67. v5a/v5b 同時対照 — attach＋end_margin は +117・歴代最高717／§66 の効果量推定を訂正（2026-07-29）
- **結果**: v5b（fetch＋attach＋end_margin）**717**（498〜821・**歴代最高**。従来最高は v2 期の692）
  vs v5a（fetch のみ）**600**（369〜679）＝**+117**。同一期間・同一デッキ（両枠とも自デッキ
  hash 再計算で 65c6b47e 純度100%）・単一の束のみ差異＝交絡なし。
- **機構の分離も教科書的**: v5a は P(ベンチ付与)**0.26**・END上書き**1.63回/戦**（＝レバー off の
  従来挙動）、v5b は **0.65**・**0.59回/戦**（＝on）。設計通りに効いた上で強さに変換された。
- **§66 の効果量推定を訂正**: 「レバー効果（±10〜20）は測定ノイズ（±25）以下＝小レバーの
  A/B は無意味」は**誤り**。誤因は**期間をまたいだ比較**（v3 647→v4 651）から効果量を推定した
  こと——帯の回転（§62/§64）で交絡していた。**同一期間の同時対照でのみ効果量は測れる**
  （§65 で確立した「レートが一次指標」に、この条件を追加する）。
  → dev_margin の A/B 打ち切り（§66 決定①）は**撤回**。end_margin と同族のレバーで、
  今回の +117 の一部を担っている可能性が高い。
- **未解決（この設計の限界）**: attach と end_margin を束で投入したため寄与の分離は不可。
  なお **ベンチ切れ負け率は 33%→34% と不変**＝attach の当初の狙い（ベンチ切れ防止・§59）は
  今回も成果面で再現せず、+117 の主因は end_margin 側（打ち切り抑制＝行動機会の複利）か、
  attach の別経路（後続アタッカーの生存）と推定される。分離には専用枠が必要だが、
  §66 の「床を守る」原則から**現時点では分離しない**（717 の枠は触らない）。
- **次**: v5a 枠（600・明確に劣後）を **v5b＋dev_margin** に差し替え、**v5b を同時対照**とする
  単一変数 A/B。勝てば上積み、負けても 717 の枠は無傷。

## 68. dev_margin 単一変数 A/B — 機構は設計通り・強さは −64 で不採用（2026-07-30）
- **結果（同時対照）**: v6（v5b＋**dev_margin=0.10**）24h **652**（537〜742・24/51＝0.471）
  vs v5b（同時・48h）**716**（498〜821・前日 30/59＝0.508）＝**−64**。**dev_margin は不採用**。
- **副産物: 24h 判定の妥当性が確定**。v5b は 24h **717** → 48h **716** と平衡（レンジも同一）。
  以後の A/B は 24h 時点のレートで判定してよい（§67 の「同時対照」条件と併せて運用）。
- **機構は設計通り作動**（heuristic 再提案との突き合わせ・v5b 59戦 vs v6 51戦）:
  - heuristic の**展開提案（PLAY/EVOLVE）の採択率 0.566 → 0.622**（+5.6pt）
  - 展開提案からの**逸脱 7.68 → 6.22 回/戦**（−19%）
  - END 上書き **0.59 → 0.27 回/戦**（うち「展開提案 → END」の逸脱が 21/59 → 12/51）
  - MAIN 決定数は 38.6 → 38.4/戦・実行動比率も PLAY 0.354→0.367 / END 0.187→0.177 と整合
- **想定リスク（攻撃遅延）は発生していない**: 初攻撃 step 中央値 56→57・攻撃 3.9→3.8 回/戦・
  展開手 15.8→16.2 回/戦・**未攻撃試合はむしろ減少**（14/59 → 8/51）。
  交絡も小: カウンター群遭遇 0.17→0.20・overage 最小 62s（両arm 同一）。
- **解釈（end_margin との非対称性）**: 「機構が効いたのに強さが落ちた」＝**探索が展開手から
  逸脱していたのは概ね正しかった**。END は唯一「何も生まない」手なのでアンカー固定が安全
  だったが、PLAY/EVOLVE は heuristic の**固定優先順**（draw/search→進化→エネ→たね）から
  出る提案で局面依存性を持たない。ここに 0.10 のマージンを課すと、探索の正しい評価まで
  抑圧される。**§60 の「第二の逸脱」は逸脱そのものが問題ではなかった**——
  「逸脱の多さ」を機構の異常と読んだ推論が誤りで、END だけが特殊だった。
- **含意**: heuristic アンカーの強制は「何も生まない手」に限る。今後アンカー系レバーを
  足すなら、対象は END 同様「後回しにしても失うものが無い／機会が複利で消える」ことが
  **手の性質から言える**ものに限定する（提案率・採択率の乖離は根拠にしない）。
- **コードの扱い**: `dev_margin` は既定 off・未指定なら挙動不変・テスト8本つきのため
  **削除せず残置**（再試の余地と、上記の記録の再現性のため）。提出物では指定しない。

## 69. サーチ順の是正2点 — 実装したが現デッキでは発火機会ゼロ（2026-07-30）

§68 の枠を使う実験として `_generic_select` のサーチ順を2点是正した。**実装・テストは
完了したが、実戦 replay での効果測定の結果「現デッキでは選択がほぼ変わらない」ため、
これ単独を実験枠に載せる価値はない**と判断した（レバーではなく正当性の修正として残す）。

### 変更内容
- **(a) fetch_priors の tier0 ゲートを `prior > 0` に**: 旧実装は「priors に載っているか」
  だけで判定しており、教師が提示されて一度も取らなかった札（取得率 0.0）まで最優先していた。
- **(b) TO_HAND のカテゴリ順を `supporterPlayed` で分岐**: 上位帯実測の選好指数 lift が
  同じ basic_pokemon で正反対だった——**サポート未使用 1.93（8/49）／使用後 0.46（1/21）**。
  §52 は after バケットの知見をサーチ全体へ一般化していたので、採掘元の条件へ限定し直した。

### 実測（v5b 59戦・v6 51戦の全サーチ局面で旧/新の選択集合を再計算）
| | サーチ局面 | (a) で選択が変わる | (b) で選択が変わる |
|---|---|---|---|
| v5b | 325 | **1**（0.02回/戦） | **0** |
| v6 | 271 | **1**（0.02回/戦） | **0** |

- **(a) が効かない理由**: 65c6b47e の priors で 0.0 だった **id11/id14 は SPECIAL_ENERGY**。
  tier0 から落ちても行き先は tier1（エネ）＝元々ほぼ同順位だった。§68 で数えた
  「0.0札とエネの競合 2.15回/戦」は**競合の回数であって選択が変わる回数ではない**
  （エネ同士の tie-break に落ちるだけ）。
- **(b) が効かない理由**: **我々の TO_HAND サーチは 229/229 すべて `supporterPlayed=True`**。
  この デッキのサーチはすべてサポート駆動で、before バケットが構造的に存在しない
  （TO_BENCH 35/68・EVOLVES_TO 8/28 には before があるが、採掘バケットが TO_HAND 限定
  なので分岐対象外）。教師側の before 標本はアイテム/特性サーチを持つ**別デッキ由来**。

### 教訓（測定の順序）
**「機構が動くか」を replay で先に測ってから枠に載せる。** §68 では機構が動いた上で
強さが落ちたが、今回は載せる前に「そもそも動かない」ことが分かった。提案率・競合回数など
**近傍の統計を効果量の代理にしない**——(a) の見積もり 0.2〜0.4回/戦は競合回数由来で、
実際に選択が変わるのは 0.02回/戦（10〜20倍の過大評価）だった。
- **横展開**: 新レバーは投入前に「旧実装と新実装で**出力が変わる局面数/戦**」を
  実 replay で再計算して確認する（フェイク観測のテストは配線の確認にしかならない）。

### 扱い
- コードは残置（正当性としては新実装が正しい・テスト5本追加・挙動不変性も確認済み）。
- **デッキを変えた場合や fetch 教師を厚くした場合には効きうる**ため、その時点で再測定する。
- 枠は end_margin の値スイープ単独に使う（§70 で判定）。

## 70. end_margin 値スイープ 0.15→0.30 — 機構は飽和・差はノイズ域／最高値の正体（2026-07-31）
- **条件**: v7a(0.15) と v7b(0.30) の2連アップロード。**両枠とも 600 スタート・同一期間・
  main.py の end_margin 値のみ差分**＝これまでで最もクリーンな単一変数 A/B。
- **機構は飽和**: END 上書き **0.48 → 0.02 回/戦**（58戦で残り1件）。END 採択率は
  0.195→0.201 と不変＝残る END は heuristic 自身の正当な提案。**この系統のレバーは打ち止め**。
- **強さ**: v7b **684**（0.500・29/58） vs v7a **637**（0.411・23/56）＝+47。ただし内訳は
  カウンター群 3/18(0.167) vs 6/15(0.400)・**その他 0.526 vs 0.535（ほぼ同一）**＝
  **差は小標本のカウンター相性に集中し、+47 は有意と主張できない**。
  コスト無し・機構を完成させるため **0.30 を採用**するが、効果量は「悪くない」まで。
- **レートの最高値は初期の振れ幅である（重要）**: v7a は**開幕5連勝**→瞬間最高 **928**
  （歴代最高）だが累積純勝は5戦目の +5 が最大で最終 −10。v7b は開幕3連勝→最高 833・
  最終 ±0。**連勝の長さと瞬間最高値が対応**＝提出直後の不確実性が大きい時期の更新幅。
  過去の 821/825/707 も同型（§57 の「707 は観測レンジ上限にすぎない」と整合）。
  → **最高値を成果として読まない。一次指標は 24h 時点の現在値のみ**（§65 に条件を追加）。
- **平均を押し下げているのは相性構造**: カウンター群 {9e3ece3f, 04621784, a4066acd, a8c57d4b}
  の遭遇率が **17%（v5b 期）→ 26〜32%（今期）** に上昇。そこでの勝率が 0.17〜0.40 なのに対し
  **その他相手は両枠とも 0.53**。通算 9e3ece3f **1/8**・a4066acd **2/10**・04621784 5/13。
  探索マージン系（end_margin/dev_margin）は §68/§70 で打ち止め＝**次に平均が動くとすれば
  相性構造（デッキ or 相性特化の操縦）だけ**。
- **方法論の確定**: 「両枠とも新規・同時スタート・単一変数」の 2連が最良の実験形式。
  期間の交絡（§67）も投入時期の交絡（§68 で v5b 48h vs v6 24h だった点）も同時に消える。

## 71. 相性特化の操縦 — 検知は実現可能・介入対象は未特定（2026-07-31・枠不使用の事前測定）
§69 の教訓（投入前に発火機会を測る）に従い、枠を使う前に replay で実現可能性を測った。

### (1) 型は我々の勝率を明確に分離する（＝作用させる価値がある）
`archetype_matrix` の family 別に自枠の実戦成績を集計（65c6b47e×ismcts 全期 n=275）:
**F4 0.319(69) / F0 0.355(62) / F1 0.650(40) / F5 0.889(9) / 未収録 0.558(86)**。
v7 期のみ（n=114）でも **F4 0.303(33) / F0 0.321(28) / F1 0.733(15)** と再現＝ノイズでない。
- **F0 は我々自身の型**（65c6b47e→F0）＝**同型（ミラー系）に負け越している**。
- カウンター群4種は型が割れる（9e3ece3f/04621784→F4・a4066acd→**F0**・a8c57d4b→F1）＝
  「カウンター群」というくくりより **型（F0/F4）の方が予測力が高い**。
- **F0+F4 は v7 期の 54%** を占める＝介入対象として十分大きい。

### (2) 検知は早期に可能（既存の配線で足りる）
`pick_opponent_deck` と同じ重み `exp(-beta*misses)` を 872 候補に対して計算し family へ集約、
期待勝率 `E[win]=Σ P(f)·winrate(f)` が 0.40 未満を「不利」と判定した実測:
- **検知率 54/61 = 0.89**・**誤検知 9/53 = 0.17**
- **検知の早さ 中央 6 決定目**・**検知後に残る決定 中央 24 回（試合の 83%）**
- top1 family の一致 **104/114 = 0.91**（不利側は 61/61）
→ **新規の推定機構は不要**。信念の集約と閾値だけで実用水準。

### (3) ただし「検知後に何を変えるか」は behavior 側に手掛かりが無い
不利(F0/F4) 61戦 と 有利 53戦 で自分の MAIN 行動を対比:
- **行動比率はほぼ同一**（PLAY 0.372↔0.364・END 0.202↔0.197・ATTACK 0.106↔0.095）
- 違うのは**量**: MAIN 決定 **35.3↔43.1回/戦**・**自ベンチ数@10決定 1.85↔2.62**
  （@30決定では 3.22↔3.24 と収束）
→ **「選ぶ手が違う」のではなく「序盤に盤面が育たない・手数が来ない」**。
⚠️ **因果は未確定**——負けた試合は短いので、決定数もベンチ数も*結果*である可能性がある。
§62 で attach_priors がベンチ切れを 33%→9% に減らしても勝ちに変換されなかった前例もあり、
「序盤ベンチを厚くすれば勝てる」は同型の推論として警戒が要る。

### 判断
**検知（レイヤ1）は投資に値するが、介入（レイヤ2）は設計待ち**。介入候補を決めたら、
実装前に §69 と同じ手順で「その介入で選択が変わる決定数/戦」を replay で再計算すること。

## 72. 探索ハイパラの内部プロファイル — 掃引候補は iters_per_det だけと確定（2026-07-31）
枠を使う前に、実戦と同じ予算（10〜13s/手）で実局面を探索し内部統計を実測した
（`ismcts_aggregate` が生の訪問統計を返すので局所で測れる）。

### 実測（v7b の実戦局面 6件・競った局面 4件×3シード）
- **1手あたりの determinization 数は 8〜11本**（iters_per_det=40 時・中央 11.5）。
  相手デッキの事後分布は **872 候補**あるので、8〜11 標本はかなり薄い。
- **`min_visits=15` は一度も拘束していない**: 全局面で**全 root 候補が 15 visit を超過**
  （訪問 中央 18.5〜203）。→ **下げても上げても no-op。掃引の価値ゼロ**。
- **`select_margin=0.05` もほぼ関与していない**: 探索が heuristic を上回るときの価値差は
  **+0.08〜+0.28**（価値は勝率 [0,1]）で 0.05 の遥か上。0.02 に下げて増えるのは僅差＝
  ノイズの逸脱だけ。→ **§70 時点の「§68 を根拠に select_margin を下げる」推奨は撤回**。
  §68 の「逸脱は概ね正しかった」は事実だが、その逸脱を作るのは margin でなく価値推定側。
- **`c=1.4` もほぼ無効**: 40反復を 12〜26 候補に配るため訪問がほぼ一様（最大27/中央24）＝
  UCB が exploit に入る前に反復が尽きる。**現状の木はほぼ根だけ（深さが無い）**。
- **`max_rollout_depth` は下げると悪化**: 300→80 で det は 11.1 本に増えるが、シード間の
  手の一致率が **0.33** と最悪（打ち切り時のサイド差代理が粗い）。
- **`iters_per_det` だけが実際に配分を動かす**: 40→det 8.2本 / **15→22.2本** / 120→2.8本。

### 併せて判明した、より重要な事実
**シード違いで同じ手を選ぶ率が 0.33〜0.50**（候補10以上の競った局面）。
**競った局面の半分は乱数次第で選ぶ手が変わる**＝現予算での探索信号はかなり弱い。
個別ノブの問題ではない。※局面4件×3シードなので配置間の差は有意でない
（断定できるのは一致率の絶対値が低いことだけ）。

### 決定
掃引候補は **`iters_per_det` 40→15 の1本**に縮小。他のノブに枠は使わない。

## 73. (A) ベンチ優先の事前確認 — 条件分岐は不要と判明・常時適用へ（2026-07-31）
- §71 の (A)（不利検知時にたねベンチ展開をエネ付与より前へ）を replay で事前確認した結果、
  **反転する提案は 不利 1.11回/戦・有利 1.19回/戦とほぼ同じ**（heuristic の ATTACH 提案
  5.7/6.6 回/戦 のうち、たね展開も同時に合法だった局面）。
  → **機会は相手に依らない＝条件分岐する意味がない**。**常時適用の heuristic 変更**とし、
  §71 の検知レイヤは (A) には使わない（「機会が相手依存な介入」が見つかった時に再利用）。
- 1.11回/戦は §69 の 0.02 とは違い実質的（end_margin 0.48〜1.63・dev_margin 1.46 と同帯域）。
  ただし**序盤15決定内では 0.39回/戦**しか反転せず、狙うベンチ差（@10決定 0.77体）に対して
  射程が足りない＝**効果は控えめと予想**。
- 実装: `bench_first`（`make_heuristic_agent`/`make_ismcts_agent`/`--bench-first`・既定 off）。

## 74. v8 2連（2026-07-31・ビルド＋プローブ済み）
- **base は v7a 構成（end_margin=0.15）**（ユーザー判断）。§70 で 0.30 を「採用」としたが
  **+47 は有意でなく確立していない**ため、通算115戦の実績がある 0.15 系統を base に据える。
  0.30 側には §68 型のリスク（END 上書きを完全に消す＝正しい逸脱まで潰す）もある。
  **end0.15 vs 0.30 は未決着のまま残す**（残り日数からこの小さな問いに枠を割かない）。
- ① `submission_stall_ismcts_v8a_benchfirst.tar.gz` — base＋`bench_first=True`（§73）
- ② `submission_stall_ismcts_v8b_iters15.tar.gz` — base＋`iters_per_det=15`（§72）
- **§34 検証**: v8a↔v8b は **main.py の1行のみ差分**。v7a↔v8x は agents.py/ismcts.py の
  `bench_first` 配線（既定 False＝挙動不変）と opp_decks 840→872。
- **配線プローブ済み**: 両本とも end_margin=0.15・fetch 4値・attach 4値が到達し、
  v8a は bench_first=True・v8b は iters_per_det=15。heuristic の順序反転も同梱物で実挙動確認。
- **注意**: 両枠とも実験＝同時対照が無い。読めるのは A vs B の優劣のみで、
  base（v7a=637）との比較は期間跨ぎ（§67）になる。

## 75. v8 2連 24h 決着 — bench_first は「発火するが効かない」・iters15 は中立（2026-08-01）
- **レート（24h・一次指標＝現在値）**: v8a(bench_first) **651**（600〜834）／
  v8b(iters_per_det=15) **675**（470〜770）。勝率 0.434(23/53) vs 0.508(30/59)。
- **ノイズを落とした指標＝「カウンター群以外の相手の勝率」は3構成で不変**:
  v7a 0.526 / v7b 0.535 / v8b 0.520。**v8a だけ 0.419**（−0.10・約1.4σ）。
  → **iters_per_det=15 は中立**（base 637→675 の差はカウンター遭遇 0.32→0.15 の帯の回転で
  全額説明でき、実力側は同値）。**bench_first はむしろ弱い負圧**。
- **機構は明確に発火した（§69 と違い no-op ではない）**: 「たね展開とエネ付与が同居する
  ターンでたね展開が先」の比率 0.081(v7a)/0.094(v8b) → **0.506(v8a)**。
  反転機会も 1.80→0.85回/戦。
- **にもかかわらず狙った指標が動かない**: 自ベンチ@10決定 **1.49**（v8b 1.73・v7a 1.67 より
  むしろ低い）・たね展開回数 **2.2回/戦で完全同値**・T5/T10 ベンチ中央値も同値。
  → **順序を変えても打てるたねの総数は増えない**（律速は札の可用性であって順番ではない）。
  §71 の「不利な相手では序盤の育ちが遅い」は**結果であって原因ではない**ことの傍証。
- **結論**: `bench_first` は**不採用**（旗は既定 off のまま温存）。`iters_per_det=15` は
  中立・無害につき据え置き（探索の内部プロファイル §72 上は唯一動く旋回だが強さには出ない）。
- **帯の変化**: カウンター群の遭遇 26〜32% → **15〜19%** に後退。代わりに **F4 王者デッキ
  4041254f が 2%→8%**（65c6 系全期 6/20=0.30）・cd4fd5bb 0/4 が新顔。

## 76. 【一部撤回】にげる（retreat）の「機会が無い」判定 — 測り方の誤り（2026-08-01）
> **⚠️ 撤回**: 下記の「機会が無い」という結論は **§78 で撤回**した。機会を
> 「HP半減以下のアクティブ」という**自分の仮説の条件で絞って**数えており、
> 「取られるとまずいから先に退避する」という本来の用途（＝満タンでも逃げる）を
> 母数から外していた。HP 条件抜きなら **にげる合法は 7.9回/戦**（採択率 1.4%）で、
> 上位帯は **0.96回/戦**逃げている（我々 0.13）。**§78 を読むこと。**
> 「3連続で機会が無い＝操縦は打ち止め」という一般化も同時に撤回する。

- §75 を受け、**まだ一度も使っていない合法手**を探して retreat を実測した。
  - HP半減以下のアクティブで意思決定 5.8〜6.3回/戦、うち **にげるが合法 1.30回/戦
    （カウンター群）/ 2.26回/戦（その他）**、実際に逃げたのは **採択率 1.8%**。
  - heuristic は §31 以来 **「攻撃できない active の救済」でしか RETREAT を提案しない**
    （`agents.py` 手順6）＝ダメージを負った「攻撃できる」active は逃げない設計。
- **だが機会が無い**: 被KO（サイド献上）は **3.48回/戦（カウンター群）/2.61（その他）**ある一方、
  **直前が危険域だった被KO は 0.69回/戦**、そのうち**にげるが合法だったのは 0.15回/戦**。
  ＝ **KO の 80% は半分以上 HP が残った状態からの一撃**で、にげるでは原理的に防げない。
  介入の上限効果は 0.15 サイド/戦（§69 の 0.02・§43 の希少機会と同じ水準）。
- **一般化（今期の最重要の教訓）**: §69（サーチ順・0.02回/戦）→ §73/§75（bench_first・
  発火するが指標不動）→ §76（retreat・0.15回/戦）と、**操縦の限界的な意思決定を直す介入は
  3連続で「機会が無い」に突き当たった**。盤面は「決定の質」ではなく**デッキが出せる駒**で
  決まっている。§70 の「平均を縛るのは相性構造」と同じ結論に、別ルートから到達した。
- **残る道は自デッキの部分改造だけ**: 他人のデッキを丸ごと借りる形（§62 965525fe・
  §64 4041254f/018cecb2）は**すべて操縦不適合で失敗**。未試行なのは
  **65c6b47e を数枚だけ入れ替える最小改造**＝操縦適合を保ったまま相性を動かす唯一の経路。
  ローカル eval-deck は §61 で選定に使えないため、判定は実戦 A/B（24h・単一変数）。

## 77. v9 2連（2026-08-01・ビルド＋プローブ済み）
- **§70 の最良形式**（両枠とも新規・同時スタート・単一変数）で **end_margin 0.15 vs 0.30 の
  未決着（§74 で持ち越し）を潰す**。iters_per_det=15 は §75 の通り中立につき両枠に載せる
  （＝差分から除外）。`bench_first` は両枠 off（§75 不採用）。
- ① `submission_stall_ismcts_v9a_end015.tar.gz` — base＋iters15＋**end_margin=0.15**（対照）
- ② `submission_stall_ismcts_v9b_end030.tar.gz` — 同＋**end_margin=0.30**（実験）
- **§34 検証**: v9a↔v9b は **main.py の end_margin 値 1行のみ差分**（diff -rq で実測）。
  v8b↔v9a は **opp_decks（872→900）以外バイト同一**。
- **配線プローブ済み**（両本）: deck.csv sha256[:8]=cd7bfdfa・end_margin 0.15/0.3・
  iters_per_det=15・bench_first 未指定（既定 off）・fetch 4値・attach 4値・opp_decks 900。
- **判定**: 24h・両枠同時スタート。§70 の「機構は飽和（END 上書き 0.48→0.02回/戦）」が
  正しければ**差はノイズ域に収まる**はずで、その場合は end_margin 系統を正式に打ち止めにする。

## 78. 操縦の意思決定面の監査 — 「機会が無い」判定は測り方が誤っていた（2026-08-01）
**発端（ユーザー指摘）**: 「にげるをそもそも状況問わず選べていないのでは。壁を出して他を
育てるような状況判断ができていない可能性は」。→ 検証したところ**指摘が全面的に正しく**、
§76 の「機会が無い」判定は**自分の仮説の条件（HP半減以下）で母数を絞った**誤りだった。
過去の同種の判定も同じ穴の可能性があるため、**決定タイプ単位で体系的に監査**した。

### 監査の方法（今後の標準にする）
「我々の仮説の条件下で機会を数える」のをやめ、**同じ決定タイプで上位帯（`others/` 1230戦）と
選択の分布を比べる**。`_generic_select` のフォールバックは「先頭から必要数」なので、
**先頭取り率 1.000 は『一度も選んでいない』の指紋**になる。帯も 1.000 なら
「選ぶ余地の無い決定」＝無視してよい（対照群として機能する）。

### 結果: 1試合あたり約10回の決定を我々は「決めていない」
| 決定タイプ | 我々/戦 | 帯/戦 | 我々先頭率 | 帯先頭率 |
|---|---|---|---|---|
| CARD/**DISCARD** | 0.53 | 0.38 | **1.000** | 0.153 |
| CARD/**TO_BENCH** | 0.92 | 0.94 | **0.990** | 0.537 |
| CARD/**SETUP_ACTIVE_POKEMON** | 0.30 | 0.46 | **1.000** | 0.544 |
| ENERGY/**DISCARD_ENERGY** | 0.29 | 0.47 | **1.000** | 0.508 |
| CARD/EVOLVES_TO | 0.44 | 0.01 | **1.000** | 0.750 |
| CARD/TO_ACTIVE・SWITCH（昇格） | 2.73 | 4.21 | 0.48/0.59 | 0.39/0.37 |
| CARD/TO_HAND（サーチ） | 5.88 | 11.56 | 0.584 | 0.399 |
| SKILL/SKILL_ORDER（対照） | 0.29 | 0.44 | 1.000 | **1.000** |
| YES_NO/IS_FIRST（対照） | 0.51 | 0.50 | 1.000 | **0.987** |
- `DISCARD`/`SETUP_ACTIVE_POKEMON` は `_generic_select` に分岐が無く、末尾の
  `return list(range(count))` に落ちている（コードと実測が一致）。
- 帯にあって我々に 0.00回/戦 の面（ATTACH_FROM 2.37・ACTIVATE 1.93・DAMAGE_COUNTER 2.26 等）は
  **そういう効果のカードを我々のデッキが積んでいないだけ**＝操縦の問題ではない。

### 昇格（TO_ACTIVE/SWITCH）は完全固定だった — 最大の発見
- 我々は**エネ差がある 415 局面すべて（1.000）で最大エネの個体を昇格**。無エネ候補が
  居た 398 局面でも壁を出したのは **0回**。§31 の `promo_score`（エネ最多→HP残）そのもの。
- 上位帯は 最大エネ 0.684 / **最小エネ（壁）0.274**（無エネ候補がいた局面の 28% で壁）。
  チーム差も明確: TeamD 0.321・TeamB 0.435 ↔ TeamF 0.170・TeamC 0.162
  ＝**選び分けている**（我々の 0.000 は「方針」ではなく「未実装」）。
- **原因**: ISMCTS は `_MCTS_SELECT_TYPES = (MAIN, ATTACK)` しか探索せず
  （`ismcts.py:38/387`）、昇格は「自明/非戦略的」として heuristic 即決。
  **探索が一度も触らない決定**なので heuristic のルールが 100% そのまま出る。
- にげるも同様: HP 条件抜きの合法機会は **7.9回/戦**（99% は攻撃も同時に選べる局面）・
  採択率 1.4%。上位帯は **0.96回/戦**逃げる（合法機会自体も 25.0 vs 7.2回/戦）。

### 過去の判定の再監査
- **§69(b)「TO_HAND は 229/229 すべて supporterPlayed=True＝before 分岐が構造的に無い」**:
  我々は今も **666/666 = 1.000**。だが上位帯は **0.642**（TeamC は 0.362）＝
  before 局面は帯には普通に存在する。§69 の「教師の before は別デッキ由来（アイテム/特性
  サーチの有無）」という説明はデッキ構成の話としては筋が通るが、**我々の打順（サポートを
  先に打つ heuristic）由来である可能性を排除できていない**。判定は「保留・要再検証」に格下げ。
- **§75「順序を変えてもたねの総数は増えない」は事実だが結論が浅かった**:
  たね展開は **我々 2.16回/戦 vs 帯 5.05回/戦（2.3倍差）**。**総数そのものが半分以下**＝
  bench_first の狙い（盤面の育ち）は正しく、手段（同一ターン内の順序入替）が的外れだった。
- **行動量が全面的に帯の半分**: MAIN決定 35.9 vs 42.2・**特性 3.82 vs 7.59**・
  **サーチ 2.95 vs 7.19**・攻撃 3.30 vs 4.46。一方 **END 採択率は 0.128 vs 0.034（3.8倍）**。
  §60 で「END の総量は heuristic の正当な提案・盤面の育ちはデッキの領分」と片付けたが、
  **打てる手を打たずに終えている可能性**が再浮上した（提示 vs 採択の切り分けは次項）。

### 教訓（§69 の「測定の順序」を更新する）
- §69 は「近傍の統計を効果量の代理にするな（提案率でなく出力が変わる局面数を数えろ）」
  と書いた。これは正しいが**不十分**だった。出力が変わる局面数も、
  **自分の現行方策の条件付きでしか数えられない**（現行が一択なら、その一択が作る状態しか
  観測できない＝生存者バイアス）。
- **追加する作法**: 「機会が無い」と結論する前に、**同じ決定を上位帯がどう扱っているか**を
  必ず並べる。帯も一択なら本当に機会が無い。帯が選び分けているなら、それは
  **機会の不在ではなく機構の不在**である。

## 79. HP・相手指定・育て方の監査 — 壁は薄く、`SWITCH` に実バグ（2026-08-01）
§78 の監査面をユーザーの追加仮説で掘った結果。**採掘した「帯の真似」より、壊れている
実装の修正の方が価値が高い**という結論になった。

### 進化のダメージ引継ぎ（エンジン実挙動を実測で確認）
`30/70 → 110/150`・`20/110 → 100/190`・`70/70 → 150/150`＝**ダメージは引き継がれる**
（回復しない）。ただし残 HP は maxHP の増分だけ増えるので**進化そのものは損ではない**。
論点は機会費用（進化札を傷んだ個体に使うと、大きい最終進化の枠を削られた個体で消費する）。
- **実測では差が無い**: 進化で傷んだ個体を選ぶ率 我々 **0.071** / 帯 0.104、
  「無傷が居たのに傷を選択」0.054 / 0.066、エネ付与が HP半減以下へ 0.070 / 0.058
  （進化済み×半減以下 0.028 / 0.030）。**我々の方がやや良い**＝伸び代なし・見送り。
- コード上は HP を見ていない（進化は `rng.choice`・`_pick_bench_attach` の HP は第3優先）が、
  **実局面ではほぼ全候補が無傷**なので差が出ない。

### 昇格の壁化（§78 の第一候補）は層別すると薄い
| | 壁 | 最大エネ |
|---|---|---|
| 最大エネ=1・劣勢 | 0.280(n=579) | 0.235(n=650) |
| 最大エネ=1・互角 | 0.388(n=183) | 0.423(n=291) |
| 最大エネ=2・劣勢 | 0.250(n=52) | **0.413**(n=421) |
- 効く可能性があるのは「1エネしか育っていない×劣勢」の +0.045（1.7σ）だけ。
  **育った個体がいる時は壁が明確に悪い**＝§31 の「エネ最多を出す」は条件付きで正しい。
- 帯の壁率 0.274 は**行動の模倣としては再現できるが、勝率の裏付けが無い**。→ 保留。

### 育て方（エネの配り方）は弱点ではない
@20決定: エネ持ち体数 1.36 / 最大集中 1.56 / 総エネ **2.04**（帯 1.23 / 1.18 / 1.58）。
@30決定: 1.69 / 1.98 / 2.90（帯 1.83 / 1.67 / 2.77）。序盤はむしろ我々が厚く、
`_pick_bench_attach` の一点集中（§59）は効いている。

### **実バグ: `SWITCH` の相手指定を自分の場として解決していた**
- `TO_ACTIVE/SWITCH` の option には `playerIndex` があり、**相手のベンチを指す決定
  （引きずり出し）が存在する**: v8 期の実測で `SWITCH` 145件中 **55件が相手側**
  （全期 **0.48回/戦**・n=108）。
- ところが `_generic_select` の `promo_score` は `playerIndex` を見ず、候補を
  **無条件に自分のベンチ**として解決している（`bench = me.bench` 固定）。
  ＝相手を引きずり出す時、**自分のベンチのエネ数で相手を順位付け**していた。
  解決に失敗した候補は末尾送り＝実質「先頭を選ぶ」に退化する。
- **実害を実測**（タイを除き、基準が差を付ける局面だけで評価）:
  | 基準 | 差あり局面 | 我々の最良選択率 | ランダム基準 |
  |---|---|---|---|
  | 残 HP 率 | 16 | 0.438 | 0.304 |
  | **エネ数**（最頻・73%） | **79** | **0.456** | **0.577** |
  | prize（ex 等） | 43 | 0.419 | 0.351 |
  最頻の軸で**ランダム基準を下回る**＝エネの乗った相手を引きずり出しがち。
- §49（cardId 誤読）と同型の「実データを確認せずに書いた解決処理」。`CardMeta.prize_value`
  （ex=2/megaEx=3）は既存なので、修正に新データは要らない。

## 80. 献上サイドの61%は「3サイドのエンジンを前に置いたまま殴られている」（2026-08-01）
ユーザーの「コイン技のせいで長期戦が運になるのでは」という仮説を検証する過程で、
**今期最大の欠陥**が見つかった。仮説自体は否定されたが、その検算が発見につながった。

### コイン仮説は否定（ログ実測）
`LogType.ATTACK/COIN/HP_CHANGE` で実測（226戦）:
| ワザ | 使用 | コイン | ダメ0率 | 中央 | 平均 |
|---|---|---|---|---|---|
| attack479（120・非コイン） | 446 | – | **0.000** | 120 | 146.5 |
| attack478（サーチ・0ダメ） | 140 | – | 1.000 | 0 | 0 |
| attack1092（200・コイン） | 130 | 227投（表率 0.427） | **0.031** | 200 | **225.7** |
→ コイン技は**不発型ではなく上振れ型**（200 は確定・コインは追加打点）。運の寄与は小さい。

### 本当の欠陥: 3サイドのドローエンジンが前で死ぬ
- 提出デッキのポケモンは3種のみ。**id756 は prize_value=3・HP300・にげる3・
  ドロー特性（我々の特性使用の 75%＝2.97回/戦）を4枚**。
- **献上サイド 2.98/戦のうち id756 が 1.81（61%）**。KO 件数では 136/401（34%）だが
  サイド3枚なので寄与が跳ね上がる。**KO された場所はアクティブ 130 / ベンチ 6**。
- **逃がす手段は持っている**: デッキに自分用の入替札（id1123）が4枚。
  「756 アクティブ×入替札が手札」は **6.66回/戦**あるのに、実行は **0.59回/戦（8.8%）**。
  にげるも 756 アクティブ時に合法 1.89回/戦で採択 **2.3%**。
- **相手側の理屈と一致**: 上位帯の gust 基準は prize 最大（ランダム比 **+0.239**・§79）＝
  **相手は我々の3サイドエンジンを狙って引きずり出している**。我々はその選択が
  バグで壊れており（§79）、逆に自分のエンジンは逃がしていなかった。

### 実装（`evacuate_prize`・既定 off）
- `CardMeta.is_self_switch`（cardId→bool）を追加。「switch」かつ「your active」を含み、
  **かつポケモンでない**カード＝手札から PLAY して自分のアクティブを下げられる札。
  ポケモン6種は同種テキストを持つが特性（ABILITY 起動）なので除外（テストで検出）。
- `_choose_main` に `_find_evacuation` を追加（掘る/特性の後・進化の前）:
  ①アクティブが 2サイド以上 → ②ベンチに交代先が居る → ③`ko_threat`（§48）が閾値以上 →
  ④**こちらが相手アクティブを倒せるなら攻撃優先**（リーサル）→ ⑤入替札 → にげる の順。
- **投入前の発火量実測**（§69/§78 の作法・v8 期112戦）:
  | 閾値 | 退避を提案 | 選択が変わる | 手段 |
  |---|---|---|---|
  | 0.3 / 0.5 | 1.15/戦 | **1.07/戦** | 入替札128 / にげる1 |
  | 0.7 / 0.9 | 0.83/戦 | **0.77/戦** | 入替札92 / にげる1 |
  `ko_threat` の重みが 0.5/1.0 に量子化されるため実質2段階。**0.7＝相手が今 KO できる時だけ**。
  bench_first 1.11・dev_margin 1.46・end_margin 0.48 と同じ実質的な帯域で、
  かつ狙う損失（0.58 KO/戦×3サイド）と量が一致している。
- テスト10本（`tests/test_evacuate_prize.py`）・全体 135 passed・ruff clean。

### デッキ側の選択肢（同時に検討・我々のエネ＝色1＋無色で成立するもの）
- **D1（最小改造・推奨）**: id756 **4→2**＋**id675 ×2**。id675 は**プール唯一の
  「1サイド・たね・ドロー特性」**（HP110・にげる1）。献上サイドを約半減しつつドローを維持。
  失うのは HP300 の壁と 200 打点の半分。
- D2: id756 4→0・id675 ×4（prize3 が消滅・壁を完全に失う）。
- D3: id756 ×2 ＋ アタッカー ×2（id304=140/無色3・HP150・にげる4 / id311=120/1エネ・条件付き）。
- ⚠️ **退避レバーとデッキ改造は代替関係**（756 を抜くと退避の対象が消える）。
  A/B は単一変数を守るため、**先に退避（安く加算的）→ 次にデッキ**の順で回す。

## 81. デッキ再設計 — 壁は本物・律速はエネ・3サイド札は割に合わない（2026-08-01）
ユーザーとの対話で提出デッキ（65c6b47e）を数値で解剖した。**操縦の議論から始まったが、
最終的にデッキ側の構造欠陥が主因**と判明した。

### 判明した事実（すべて実測）
- **id345 は ex への完全な壁**: 特性の構造は
  `prevent all damage done to this pokémon by attacks from your opponent's pokémon {ex}`。
  実測で **ex の攻撃が id345 に当たった時の平均ダメージ 17**（375回）に対し、
  **通常の攻撃は 99**（270回）、**ex が id756 に当たると 170**（183回）。
  相手の攻撃 1527回のうち **ex 由来が 610回（40%）**。
- **相手は壁を「破る」のでなく「どかす」**: id756（3サイド）を gust で引きずり出して倒す。
  上位帯の gust 基準は prize 最大（ランダム比 +0.239・§79）と一致。
  id345 のアクティブ滞在率は **43%** しかない。
- **id756 は割に合っていない**: 226戦で **攻撃 130回 < 被KO 136回**。
  献上サイド 2.98/戦のうち **1.81（61%）** がこの札。ベンチ滞在 91%・平均エネ 0.43。
- **律速はエネであって色でも札でもない**: デッキのエネは13枚・攻撃コスト3 →
  **理論上限 4.3回/戦**（実測 3.30）。**id345 がアクティブのターンの 66%（812/1402）は
  エネが3枚に届かず撃てない**。一方 **色事故は 1.5%**（3枚揃って草が無いのは 469中7）。
  ＝「無色スロットは何でも払える」ため色は詰まっていない（ユーザー指摘の検証）。
- **OHKO の分水嶺は 170**: 相手アクティブ860体の最大HP は中央130・75%点210。
  1発圏内に入る比率は **120→0.45 / 140→0.62 / 170→0.72 / 250→0.78**＝
  **120→170 で跳ね、その先は伸びない**。現行主力 id345 は 120＝0.45 しかカバーしていない。
- **死に札が約10枚**: heuristic は draw/search 以外のトレーナーを打たない設計のため、
  id1147（回復・採択 **0.071**）・id1197（**0.065**）・id1087（**0.041**）が腐っている
  （対照: サーチ系 id1122/1225/1086 は 0.55〜0.58）。
- **長期化のコストは小さい**: 自分のデッキ切れ負け **12件/226戦（5.3%・ターン中央38）** に対し
  相手デッキ切れ勝ちは **56件（24.8%・ターン27）**＝**11ターンのマージン**がある。
  負けはむしろ短期戦（負け試合の総ターン中央 **13**・獲得サイド 1.06）。

### 却下した案と理由
- **コイン技が長期戦を運任せにしている**（ユーザー仮説）→ **否定**。id756 の 200 は確定で
  コインは上振れ（実測平均 225.7・ダメ0率 3.1%・§80）。
- **傷んだ個体の進化/エネ付与を避ける** → 帯と差が無く伸び代なし（§79）。
- **昇格の壁化（最小エネを出す）** → 層別勝率で薄い（§79）。
- **アタッカーの色を変える（id971 等）** → 色事故が 1.5% しかない上、
  **壁とアタッカーはアクティブ枠を奪い合う**。壁を前に置く必要がある以上、
  その個体が殴れること（id345 の 120）は追加コスト0の利益＝色替えは割に合わない。
- **id304（140/無色3・たね）を追加** → にげる4＋自傷でユーザーが却下。妥当。
  草＋無色・1サイド・副作用なしで id345 を上回るのは **2進化のカード（id214/id348）だけ**で、
  負けが13ターンで決まる現状には間に合わない。

### 採用した設計: サイド2枚の大型アタッカー（id125）へ組み替える
ユーザーの「倒せるならサイド3枚でもよい」を受けて 1サイド縛りを外して探索した結果、
**id125** が全条件を満たした:
- **サイド2**・**HP300**（id756 と同じ体格で1枚軽い）・**180ダメージ/無色3**（色縛り無し・OHKO 0.72）
- **ワザ効果: 手札が6枚になるまで補充**＝id756 のドローエンジン役を代替
- **特性: 自分のポケモン間で基本エネを移動**＝「壁が前・燃料が後ろ」の構造問題への解答
- 1進化（進化元 id124 は HP120 のたね）・にげる4
（対抗馬 id1031 は 210ダメ・HP330・弱点/抵抗と相手の効果を無視するが**サイド3**で id756 と同じ重さ）

### デッキ変種（`data/decks_variant/`・追跡外）
| | 構成 | 3サイド札 | エネ | 攻撃上限 |
|---|---|---|---|---|
| 現行 | 344×4,345×4,756×4 / エネ13 | 4枚 | 13 | 4.3回 |
| D7（最小改造） | 756を2枚に・id1197削除・基本草エネ+6 | 2枚 | 19 | 6.3回 |
| **D9** | **344×4,345×4,124×4,125×4** / エネ17 | **0枚** | 17 | 5.7回 |
| **D8'** | D9 の id1087×2 を **id756×2** に | 2枚 | 17 | 5.7回 |
- **D9 と D8' の差分は `id1087×2 ↔ id756×2` の1点のみ**＝ドローエンジンを残すかの単一変数。
- **機構スモーク green**（heuristic 同士20戦×3デッキ）: 全て完走・エンジンエラー0・
  進化は 20/20 で発生し**初出ターン中央3**（現行と同じ）・初攻撃ターン中央3〜4。
  ※ §61 によりローカル勝率は判定に使わない（機構が回るかの確認のみ）。

### 合意した提出計画（ユーザーと確定）
| | 枠A | 枠B | 単一変数 |
|---|---|---|---|
| 現在 | v9a（end0.15） | v9b（end0.30） | end_margin |
| ① | v10a（退避 off） | v10b（`evacuate_prize=0.7`） | 退避レバー（§80） |
| ② | **D9** | **D8'** | ドローエンジン 0枚 vs 2枚 |
| ③ | ②の勝者（対照） | 次の課題 | 未定 |
- **② の両枠は ① で勝った操縦に揃える**（デッキを単一変数に保つ）。
- ② には現行デッキの対照が無いため、**v9/v10 期の機構指標をベースラインに使う**:
  献上サイド 2.98/戦・OHKO率 0.45・攻撃 3.30回/戦・id345 滞在率 43%・自デッキ切れ負け 5.3%。
  レートは期間跨ぎ（§67）なので一次指標にしない。
- **早期中止基準**（②）: 15h 時点で初攻撃 T25 超 or 自デッキ切れ負け 20% 超なら引き上げ。
- ③ の「次の課題」候補（§78 の監査から機会順）: DISCARD の選択（0.53回/戦・先頭率 1.000 vs 帯 0.153）／
  `SWITCH` 相手指定バグ修正（0.48回/戦・§79）／にげる全般（合法 7.9回/戦・採択 1.4% vs 帯 3.8%）。

## 82. v9 2連 24h 決着 — end_margin は打ち止め・同一ビルドの期間跨ぎノイズは ±110（2026-08-03）
**v9a（end0.15）785 / v9b（end0.30）715**。両枠とも 600 スタート・同時・単一変数（§70 の最良形式）。

| | v9a（0.15） | v9b（0.30） |
|---|---|---|
| レート現在値 | **785**（600–867） | 715（600–890） |
| 全体勝率 | 34/68 = 0.500 | 30/67 = 0.448 |
| カウンター群遭遇 | 0.18（12戦） | 0.16（11戦） |
| **その他相手の勝率** | 33/56 = **0.589** | 27/56 = 0.482 |
| デッキ切れ負け | 1（1.5%） | 5（7.5%） |

- **v8 と違い帯の回転では説明できない**（遭遇率 0.18 vs 0.16 でほぼ同一）。レート・全体勝率・
  非カウンター勝率の3指標すべてで v9a が上＝方向は一貫。
- **ただし有意ではない**（非カウンター差 +0.107・z=1.14／全体 +0.052・z=0.61）。
  **v7 は 0.30 が +47 で勝ち、v9 は 0.15 が +70 で勝った＝逆符号**。§70 の「機構は飽和」が正しく、
  **end_margin 系統は正式に打ち止め**。base は実績と単純さから **0.15 に固定**。

### 副産物: 期間跨ぎ比較のノイズ床を実測（§67 の定量化）
**v9a は v8b と opp_decks（872→900）以外バイト同一**なのに **675 → 785（+110）**。
- 同一バイナリでも期間が違えば **100pt 級**に動く＝**期間跨ぎのレート比較は 100pt 未満では無意味**。
- **785 を「歴代最高」として成果に読まない**（v5b 717・v8b 675 と同じ床の上振れ側）。
- 同時対照（両枠新規・同時スタート）以外の A/B は今後も採らない。

## 83. v10 2連 24h 決着 — 退避は「提案の70%を探索が上書き」して設計値の1/3しか出ない（2026-08-03）
**v10a（退避 off）640 / v10b（`evacuate_prize=0.7`）658**。差 +18 はノイズ。

| | v10a | v10b |
|---|---|---|
| レート現在値 | 640（516–745） | 658（400–697） |
| 全体勝率 | 28/54 = 0.519 | 29/53 = 0.547 |
| カウンター群遭遇 | 0.28（9/15 勝） | 0.19（4/10 勝） |
| その他相手の勝率 | 19/39 = 0.487 | 25/43 = 0.581 |
（v10a の全体 0.519 は 04621784 に **7/9** という異常な引きに支えられている＝通算 2/12・3/8 の相手）

### 機構チェック: 提案と採用を分離（§75 と同じ作法）
replay の MAIN 局面で `_find_evacuation` と `_choose_main` を再実行し、実際に打たれた手と突き合わせ:

| | v10a | v10b |
|---|---|---|
| 退避を提案する局面 | 1.33/戦 | 1.06/戦 |
| **実戦で採用** | 0.06/戦（**4.2%**） | 0.32/戦（**30.4%**） |
| 探索が上書き | 1.28/戦（95.8%） | 0.74/戦（69.6%）|
（上書き先は v10b で PLAY 21・ABILITY 14・ATTACH 2・EVOLVE 1・END 1）

- **レバーは効いている（採択 4.2%→30.4%）が、投入前実測 0.77回/戦 に対し実効は 0.26〜0.32回/戦**。
- **原因は構造的**: `_find_evacuation` は heuristic の提案にすぎず、`_select_action` は
  `select_margin=0.05` を超えれば自由に逸脱できる。**退避には end_margin のような保護が無い**。
- 成果側: 献上サイド **3.22→3.02/戦**・被KO 1.93→1.77/戦。しかも v10b は id756 の
  アクティブ滞在率が **26%→34% と高かった**（晒される機会は多い）のに献上は減っている＝
  方向は設計どおりだが量が小さく n≈53 では判定不能。
- **判定: §80 の仮説は否定されていない——レバーでは十分に検証できなかった**。
  保護への格上げ（1行）は可能だが、同じ仮説をデッキ側が桁違いに強く検証する
  （レバーの実効 −0.20/戦 に対し、D9 は id756 の献上 **1.87/戦 を構造的にゼロ**にする）ため、
  **枠は ② のデッキ A/B に使う**（ユーザー合意）。保護案は ③ の候補として温存。

### v10 期のベースライン（②は同時対照が無いのでこれを基準にする・n=107）
献上サイド **3.12/戦**・被KO 1.85/戦（アクティブ 182 / ベンチ 16）・攻撃 **3.76回/戦**・
初攻撃ターン中央 **6**（未攻撃 17%）・試合ターン中央 19・**id345 滞在率 57% / id756 30%**・
自デッキ切れ負け **2.8%**（3/107）・overage 最小 61s。
※ §81 時点（v8 期）の 2.98/3.30/43%/5.3% から更新。以後の比較はこの v10 期の値を使う。

## 84. ② デッキ A/B（D9 vs D8'）ビルド（2026-08-03・プローブ済み・アップロード待ち）
- ① `models/submission_d9_ismcts.tar.gz` — **D9**（3サイド札ゼロ・deck sha256[:8]=e264c506）
- ② `models/submission_d8e2_ismcts.tar.gz` — **D8'**（D9 の id1087×2 → id756×2・2d24430a）
- **操縦は両枠同一**: end_margin=0.15・iters_per_det=15・**evacuate_prize=0.7**
  （事前に決めた「①の勝った操縦に揃える」に従い、名目上勝った v10b 側を採用）
  ＋ fetch_priors 4エントリ（TeamJ 65c6b47e 分）・attach_priors・opp_decks 966。
- **§34 検証**: 展開して再帰 diff＝**`deck.csv` 以外は全ファイル一致**。デッキ差分も
  `id756: 0→2 / id1087: 2→0` の1点のみ（60枚・prize≥2 は 4枚 vs 6枚）。
- **配線プローブ（両本 green）**: `ptcgbot.ismcts.make_ismcts_agent` に end_margin=0.15・
  iters_per_det=15・evacuate_prize=0.7・opp_pool 966・deck 60枚・attach 4値が到達。
- **煙テスト（ismcts 実対局・予算20s）**: D9 92手 / D8' 94手で完走・最大1.6s/手・エラー0。
- **注記**: D9/D8' では prize≥2 が**主力アタッカー id125**（HP300）なので退避レバーの意味が
  変わる（エンジンを逃がす → アタッカーを下げる）。D8' 側は「756 を最良条件（退避あり）で
  評価する」構図になるため、「756 を残す価値があるか」を問う実験としては成立する。
- **早期中止基準**: 15h で初攻撃 T25 超 or 自デッキ切れ負け 20% 超なら引き上げ。

## 85. ② デッキ A/B 24h 決着 — D8' > D9・§81 の再設計は自分の指標で反証（2026-08-04）
**D9（3サイド札ゼロ）588 / D8'（＋id756×2）608**。単一変数は `id1087×2 ↔ id756×2`。

| | D9 | D8' |
|---|---|---|
| レート現在値 | 588（588–822） | 608（453–762） |
| 全体勝率 | 20/49 = 0.408 | **26/49 = 0.531** |
| その他相手の勝率 | 14/37 = 0.378 | **20/36 = 0.556**（z=1.54） |
| **ベンチ切れ負け** | **18/49（37%）** | 6/49（12%）（**z=2.94・p≈0.003**） |
| T10 ベンチ枚数 中央 | 2.0 | 3.0 |
| 終局ベンチ枚数 中央 | 2 | 4 |

- 勝率差は有意でないが**負け方は有意に違う**: D9 の 29 敗中 18 がベンチ切れ。
  **id756 は「3サイドのドローエンジン」であると同時に HP300 の頭数**で、抜くと
  ポケモン 18→16・たね 10→8 になって場が保たない。**「3サイド札は割に合わない」は誤り**。

### §80/§81 の仮説そのものが反証された（現行デッキとの比較）
| | 現行 65c6b47e（v10 期・n=107） | D9 | D8' |
|---|---|---|---|
| 献上サイド | **3.12/戦** | 3.49 | 3.51 |
| 被KO | **1.85/戦** | 2.82 | 2.65 |
| サイド/KO | 1.69 | **1.24** | 1.32 |
| 攻撃 | 3.76回/戦 | 4.04 | 4.22 |
| ベンチ切れ負け | 8.4% | 37% | 12% |
- **狙いどおり「1回の KO で取られるサイド」は 1.69→1.24 に下がったのに、殴られる回数が
  1.5倍になって献上サイドは逆に増えた**（3.12→3.5）。HP300 の壁と頭数の喪失がそのまま被KO に化けた。
- **§80 の「献上サイドの61%＝id756」は事実だったが、因果が逆だった**——id756 が前で
  受け止めていたから比率が高かった。**デッキ側の探索はここで打ち止め・65c6b47e に復帰**。
- 早期中止基準は非該当（初攻撃T中央 4・デッキ切れ負け 6%）・overage 最小 51〜57s で正常。

## 86. §79 の `SWITCH` 相手指定バグを修正（`fix_switch_target`・§34 の A/B 用に既定 off）（2026-08-04）
- **修正内容**: `_generic_select` の `promo_score` で `option.playerIndex` を見て所有者を解決し、
  相手を指す候補は**相手のベンチ**から選ぶ。基準も反転させ、上位帯と同じ
  **取れるサイド最大**（ランダム比 +0.239・§79/§80）→ 同値なら**残 HP の少ない方**。
  自分の場（昇格/交代先）は従来どおり「エネが乗り HP の残る子」。
- **フラグにした理由**: バグ修正だが、単一変数の A/B（§34）を守るため両枠を同一コードから
  ビルドできるようにする。既定 off＝挙動不変。
- **投入前の発火量実測**（§69/§78 の作法・v10 期 107戦）:
  | | v10a | v10b |
  |---|---|---|
  | TO_ACTIVE/SWITCH 決定 | 2.76/戦 | 2.74/戦 |
  | うち相手指定 | 0.31/戦 | 0.36/戦 |
  | **選択が変わる** | **0.24/戦** | **0.19/戦** |
  §79 の見積り 0.48回/戦 より小さい（相手指定のうち選択が変わるのは約2/3）。
- **⚠️ 効果量の期待値は低い**: bench_first 1.11・end_margin 0.48・退避（提案）1.06 と、
  **すでにノイズ域と判定された全レバーより発火が少ない**。それでも枠を使う理由は
  ①主目的が 65c6b47e の床回収であること ②サブ選択は探索の上書きを受けない（下記）ため
  発火＝そのまま実行になること ③正当性の修正としてコードに残す価値があること。
- **サブ選択は 100% 実行される**（今回判明した構造）: `_MCTS_SELECT_TYPES` は MAIN/ATTACK のみで、
  それ以外の select は heuristic の返り値がそのまま打たれる。replay 実測でも
  **実戦の手と旧 `_generic_select` の一致 294/294（100%）**。退避（採用30%）と違い上書きされない。
- テスト8本（`tests/test_switch_target.py`）・全体 143 passed・ruff clean。

## 87. v11 2連（2026-08-04・ビルド＋プローブ済み・アップロード待ち）
- ① `models/submission_stall_ismcts_v11a_control.tar.gz` — **対照＝床回収**
  （65c6b47e・end0.15・iters15・evacuate0.7＝v10b と同構成）
- ② `models/submission_stall_ismcts_v11b_switchfix.tar.gz` — 同＋**`fix_switch_target=True`**
- **§34 検証**: v11a↔v11b は **main.py の1行のみ**差分。v10b↔v11a は §79 の追加分
  （フラグ未指定＝不活性）と opp_decks 966→993 以外**バイト同一**（main.py もバイト同一）。
- **配線プローブ済み**（両本）: deck sha256[:8]=cd7bfdfa・end_margin0.15・iters15・
  evacuate0.7・fix_switch_target（②のみ True）・opp_pool 993・fetch 4・attach 4値。
- **判定**: 24h・同時対照。①が 640〜785 の帯に戻れば床の再現も確認できる。

## 現在地と次の判断（2026-07-07）
- **提出（Kaggle・3枠並走中）**: ①ISMCTS ②floored NN＋盤面補正α0.2（エネ33チャンピオン）
  ③同（実メタ構成の repaired デッキ）。実戦成績: nn 0.464 > ismcts 0.395、nn_repaired は初期 n=18 で
  0.333 だが**ベンチ切れ負け 63%→25%**＝故障モードは狙い通り修正（耐久を勝ちに変換できるかが焦点）。
- **原因究明の遷移（この判断ログの本筋）**: 「NN が学ばない」の真因を段階的に切り分けてきた:
  ①弱教師（TB=0.25）→ ②one-hot 雑音説は自己一致率 0.85 で棄却 → ③**過大容量**（right-size で解決・§14訂正）
  → ④自己蒸留の空転（根ノイズ＋fresh 種・§18）→ ⑤**EMA 凍結バグ**（per-iter に decay0.999・§22追記）
  → ⑥**self-play の z 汚染**（全手サンプリング→偽相関の学習・§23）。⑤⑥は「プラトー」に見えた期間の
  真因で、**過去の「種超え」判断は過大評価だった**と遡って訂正。対策が毎回正反対なので
  「診断で切り分けてから動く」（§21）が最重要の作法。
- **NN の現在**: 実用系は seed（旧系譜最良・v2.3 形式変換済み）＝提出中の net と同水準。v2.3（盤面特徴）
  の学習は「温度スケジュール＋収集補正OFF＋カナリア」の対策入り improve で**再検証中**。
  カナリアが再度沈むなら self-play 撤退→distill 継ぎ足しで v2.3 を学ばせる（教師固定＝安定）。
- **デッキ軸**: league は構成射影（COMP_BOUNDS・実メタ帯）内で探索する新基準に移行済み。
  ローカル評価は共適応ロックで序列を覆せないため、デッキの裁定は実戦 A/B（nn vs nn_repaired）に委任。
- **次の判断**:
  1. 対策入り improve の判定（カナリア維持＋同一 run 差分 B−A ≥ 0）→ 成立なら self-play 本格運用へ。
  2. nn_repaired の実戦 n≈50 でデッキ A/B の中間判定。
  3. どちらかで改善が確定したら最良構成（net×デッキ×注入有無）で再提出・issue #2/#4 更新。

## 規約と公開計画
- **Competition Data**（cabt エンジン・カードデータ・抽出デッキ）は再配布禁止・コミット禁止・追跡外。
  競技終了後は**速やかに削除**する義務（作業ツリーの `data/` `src/cg/`）。リポジトリ自体の削除義務はない。
- **Pokémon Elements**（カード名・効果文・デッキ等）はコード/コミット/ノート/このドキュメントに**書かない（＝公開しない）**。
  ただし**モデルの学習/推論にローカルで使うのは許諾の範囲内**（L73）＝効果文を読んで数値カテゴリ化するのは可
  （[design 13](#13-効果文を読んで数値カテゴリ化するnn-v2規約上ok)）。「使ってよい・出さないだけ」。
- **公開計画**（競技終了後）: ① Competition Data 削除 → ②履歴に Pokémon Elements が無いか最終確認
  （現状クリーン）→ ③ OSI ライセンス（MIT 推奨・勝者は必須）を付与 → ④ Kaggle フォーラムで告知 →
  ⑤ リポジトリを public 化 → ⑥ public+Free で使える **Wiki** へこの学習ノートを移植。
  ※ 競技期間中は public 化しない（公開共有は Kaggle フォーラム経由のみ・私的共有はチーム外禁止）。
